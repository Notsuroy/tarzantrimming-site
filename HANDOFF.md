# tarzantrimming.ca: HANDOFF

> Source of truth for project state. Read before making any non-trivial change.

## Project context

Personal-brand website for a friend rebranding his arborist side-business from "The Ontario Arborist" to "Tarzan Trimming". Built as a gift / practice project by Suroy.

- **Domain:** `tarzantrimming.ca` (registered at Porkbun 2026-05-29 by friend, ~$18/yr; nameservers pointed at Cloudflare, zone active)
- **DNS:** Cloudflare zone `8dccf00d34ed0f68f554bc24a66a54ad` (Suroy's account `d5cadc4c53223e8b3ef7846fdbbffd8e`). Apex + `www` CNAME both proxied at `tarzantrimming.pages.dev`.
- **Hosting:** Cloudflare Pages project `tarzantrimming`, deployed via direct-upload (wrangler), NOT linked to GitHub yet. To enable auto-deploy on push, connect the repo in the CF dashboard later.
- **GitHub:** [Notsuroy/tarzantrimming-site](https://github.com/Notsuroy/tarzantrimming-site) (public)
- **Live URLs:**
  - https://tarzantrimming.pages.dev (always works, auto-issued cert)
  - https://tarzantrimming.ca + https://www.tarzantrimming.ca (custom domain, SSL provisioning ~5-30 min after first DNS attachment)
- **Stack:** Astro 5 + Tailwind 3
- **Predecessor:** `theontarioarborist.ca` (kept live during transition, will 301 redirect to new domain in 6-12 months once new domain has authority)

## Brand positioning

> "Owner-led tree pruning and limb removal across south Ontario. Trained at Davey Tree commercial, working for residential customers on evenings and weekends. Honest quotes, clean work, no high-pressure pitch."

## Design system

Carried over from vicjimassage with a new palette suited to outdoor / arborist context.

**Palette** (see `tailwind.config.cjs`):
- Primary: deep forest green `#2D4A36`
- Warm accent: bark brown `#8B6F47`
- Cool secondary: canopy-sky blue `#5B7B8C`
- Highlight: soft moss green `#9DAE7F`
- Background: warm cream `#FAF7F0`
- Text: deep slate `#2A2E2A`

**Typography:**
- Display: Cormorant Garamond (serif, headings)
- Body: Inter (sans, paragraphs)

**Copy rules (non-negotiable):**
- Never use em dashes (U+2014) or en dashes (U+2013). Use periods, commas, or "to" for ranges. Run `grep -rPn "[\xe2\x80\x94\xe2\x80\x93]" src/` to verify zero hits before pushing.
- No certification claims (ISA, etc) that the owner does not actually hold.
- No "24/7" or "always available" claims. Owner works at Davey during the day.
- Pricing language is fact-only; "Quote on site" placeholders until friend picks pricing strategy.

## Pages built (v1)

- [x] `/`: Funnel homepage (Hero, ProblemPromise, MiniAbout, ServicesGrid, PhotoGallery, ReviewsWall, FAQ, FinalCTA)
- [x] `/services/`: Detailed service menu (pruning, limb removal, full removal as "coming soon"). Offered-service cards now link to their dedicated detail pages.
- [x] `/services/tree-pruning/`: Dedicated indexable service page (focus keyword "tree pruning"). Built 2026-06-09 (cron).
- [x] `/services/limb-removal/`: Dedicated indexable service page (focus keyword "limb removal"). Built 2026-06-09 (cron).
- [x] `/service-areas/`: Service Areas hub (focus keyword "tree service south Ontario"). One page covering all 7 serviceCities with honest, region-accurate per-city blurbs (tree species + property/terrain context, no fabricated bylaw thresholds), `Service` JSON-LD with 7-city `areaServed` + `BreadcrumbList`, internal links to both service pages + about + contact. Built 2026-06-09 (cron). Deliberately a single editable hub, NOT 7 thin per-city doorway pages: city list is still friend-unconfirmed (launch TODO #2) and doorway pages are off-brand + SEO-risky for a 12-day-old domain. Promote to dedicated per-city pages later once cities are confirmed and the domain has authority.
- [x] `/about/`: Owner-led story + what-we-will-and-will-not-do
- [x] `/contact/`: Phone, text, email, service area
- [x] `/404/`: Custom not-found page (recovery links to home, both service pages, services menu, about, contact). Built 2026-06-09 (cron). Astro emits `dist/404.html`, which Cloudflare Pages serves with a proper 404 status for unmatched routes (replaces the previous soft-404 homepage fallback).

JSON-LD `LocalBusiness` + `FAQPage` schema on homepage (FAQPage built from the 8 visible `faq.json` items, `areaServed` covering the 7 service cities). Each service detail page emits `Service` + `BreadcrumbList` JSON-LD.

**Service detail page architecture:** `src/pages/services/[slug].astro` generates one page per non-`comingSoon` service in `services.json` (via `getStaticPaths`). It reuses the already-approved `description` / `process` / `goodFor` copy from `services.json`; per-page H1 + meta-description live in a `seoMap` inside the route file (kept out of `services.json` so the `/services/` menu rendering is untouched). Each page also carries a per-service FAQ: a slug-keyed `faqMap` in the route file drives BOTH a visible `<details>` accordion AND `FAQPage` JSON-LD from one array, so schema and visible text never drift. FAQs are honest (no certs, no 24/7, quote-only pricing) and mirror language already in `services.json`. To add FAQs for a future service, add an entry to `faqMap` keyed by its slug. Full tree removal is intentionally NOT given a page (it is `comingSoon` and not actually offered, so no rankable page is published for it). When full removal becomes a real service, flip `comingSoon: false` in `services.json` and add its H1 + meta to the `seoMap`; the page and sitemap entry will follow.

## Components inherited from vicjimassage-site (rebranded)

- `Header` (sticky, with phone CTA chip)
- `Hero` (gradient + headline + dual CTA)
- `ProblemPromise` (3 you/us pairs)
- `MiniAbout` (owner photo placeholder + story)
- `ServicesGrid` (3 services from `services.json`)
- `PhotoGallery` (6 photo placeholder slots, awaiting friend's shots)
- `FAQ` (8 front-of-funnel items in `faq.json`)
- `FinalCTA` (forest section with call + text-photo CTAs)
- `Footer` (3-column: brand, contact, service area)
- `PhoneFloat` (floating mobile-friendly call CTA; replaces vicjimassage's `WhatsAppFloat`)

## TODOs before launch (blocking on friend's input)

1. **Phone number** · currently using TOA's `(647) 788-8733`. Confirm friend wants to keep it or use a different number.
2. **Service cities** · currently inheriting TOA's 7-city list. Confirm with friend.
3. **Pricing strategy** · `services.json` is set to `quote-only` (no public ranges). Friend can switch to `public-ranges` for stronger CTR if comfortable committing to ranges.
4. **Hours** · currently labeled "By appointment. Evenings and weekends." Confirm exact availability given Davey day-job schedule.
5. **Tagline** · placeholder is `"Tree pruning and limb removal across south Ontario."` Friend can swap to his own one-liner.
6. **Insurance claim** · FAQ states "carries commercial general liability insurance". Confirm before publishing. If not yet bound, change the FAQ answer to "Insurance is being bound; certificate available before any job".
7. **Day-job employer** · copy now says "commercial tree crews" generically. If friend confirms he actually works at a specific named company AND is comfortable naming it, swap "commercial crews" / "a commercial crew" back to the real name. Initial 2026-05-29 draft had Davey Tree by name; removed when Suroy could not confirm whether the friend works there or it was second-hand info.
8. **Owner photo** · `MiniAbout` has a placeholder block where the owner photo goes. Friend to send.
9. **Job photos** · `PhotoGallery` has 6 placeholder cards. Friend to send before/after pruning shots.
10. *(Reviews section removed entirely in v1 since friend has no reviews yet. To re-add later: copy `ReviewsWall.astro` and `reviews.json` back from `vicjimassage-site` reference, simplify, and re-import in `pages/index.astro`. Honest path: only re-add when there are at least 3 real customer reviews.)*

## TODOs Suroy needs to do (infrastructure)

1. Create GitHub repo (`Notsuroy/tarzantrimming-site`?). Push initial commit.
2. Cloudflare: add `tarzantrimming.ca` zone, point NS at Cloudflare, create Pages project, connect to GitHub repo.
3. DNS records: apex + `www` to Pages, MX records for email forwarding (or Migadu mailbox).
4. Email: set up `info@tarzantrimming.ca` (Cloudflare Email Routing to friend's inbox, OR Migadu mailbox with SPF/DKIM if friend wants real outbound).
5. Update `src/data/site.json` `contact.email` from `TODO_EMAIL` to the real address.
6. Google Search Console verification (DNS TXT or HTML file).
7. Submit sitemap to GSC and Bing once `tarzantrimming.ca/sitemap.xml` exists.
8. Eventually: 301 redirect `theontarioarborist.ca` → `tarzantrimming.ca` once new domain has authority (~6 months out).

## Things this site does NOT have (yet) vs vicjimassage

Intentionally trimmed for v1 scope:

- **No blog.** Astro content collection deleted. Can re-add later if friend wants long-form posts.
- **No bilingual setup.** vicjimassage runs PT root + EN at `/en/`. Tarzan is English-only.
- **No WhatsApp integration.** Canadian customers prefer phone/SMS. `WhatsAppFloat` replaced with `PhoneFloat`.
- ~~**No sitemap.xml route.**~~ DONE 2026-06-09 (cron, commit `5d4ae95`). Custom `src/pages/sitemap.xml.ts` endpoint emits the 4 static pages; `public/robots.txt` references it. Live at `https://tarzantrimming.ca/sitemap.xml`. Add a route to the `PAGES` array in the endpoint when a new page ships.
- **No /quote/ form.** v1 funnels to phone + text. If form conversion data later supports it, can add (using the same /api/quote pattern as the rank-and-rent sites).

## Architecture principles

- **`src/data/site.json` is the source of truth** for brand, contact, location, hours. Every component reads from there. Update once, everything reflects.
- **No certifications or claims that cannot be backed up.** Honest by default. Saves us from having to undo a banned-claim sweep later.
- **The vicjimassage component shapes work.** Don't reinvent the funnel architecture; reuse what's already tested.

## Changelog

| Date | Action | Notes |
|---|---|---|
| 2026-05-29 | Initial scaffold | Cloned vicjimassage architecture, stripped bilingual + blog scope, rebranded palette (forest/bark/sky/moss/cream), wrote new English-only components, built 4 pages (home, services, about, contact), 88K dist, zero em/en dashes. Awaiting friend's input on the 9 TODOs above before launch. |
| 2026-05-29 | Removed ReviewsWall + reviews.json | Friend has no real customer reviews yet. Section dropped from homepage rather than left dormant. Will re-add only when there are at least 3 real reviews to show. |
| 2026-05-29 | Soften day-job copy, no named employer | Suroy could not verify if friend works at Davey Tree or that was second-hand info. Customer-visible copy now says "commercial tree crews" generically. Easy to swap to a named employer once confirmed. |
| 2026-05-29 | Hide all contact CTAs | Phone fields in site.json blanked; Header/Hero/FinalCTA/Footer/PhoneFloat/contact.astro all conditional-render based on presence. Once friend confirms phone + email, populate src/data/site.json contact block and the whole site lights back up. |
| 2026-05-29 | First deploy to Cloudflare Pages | Project `tarzantrimming` created via API, dist/ direct-uploaded via wrangler. Apex + www CNAME records created and attached to Pages project. Repo is on GitHub but NOT yet wired to auto-deploy. Live at https://tarzantrimming.pages.dev. Custom domain SSL provisioning. |
| 2026-06-04 | Competitor name conflict noted + proactive partnership plan documented | Discovered Tarzan Tree Removal (Barrie/Simcoe County, ON, Mark Mederak, 5+ years operating, Landscape Ontario Young Entrepreneur 2019, Barrie Advance Readers Choice 2020-2022, ISA Certified Arborist). Friend Nathan reviewed the conflict and chose to proceed with Tarzan Trimming. Strategy: proactive partnership outreach BEFORE any cease-and-desist letter, framed as new-entrant introduction with referral exchange offer (we focus pruning + limb removal west-GTA / Kitchener corridor; they focus full removal + Barrie/Simcoe). Outreach deferred until tarzantrimming.ca has 60-90 days of authority + a few demonstrated leads to back the partnership credibility. Risk profile (with proactive outreach folded in): cease-and-desist ~10%, forced rebrand ~3-5%, real referral partnership ~25-35%. Sudbury, Fort Mac, etc rank-and-rent sites' lead-handoff playbook (e.g. Moira / Valley / Fort Repairs) provides the operational template. |
| 2026-06-04 | nathan@tarzantrimming.ca via Migadu planned, awaiting DNS records from Suroy's Migadu dashboard | Friend's email will be `nathan@tarzantrimming.ca` (not `info@` -- explicitly requested). Plan: Migadu mailbox (uses existing Micro plan, tarzantrimming would be 7th domain), DNS records added to Cloudflare zone, Nathan hooks up via Gmail Settings -> "Check mail from other accounts" (IMAP) + "Send mail as" (SMTP) so he never leaves his Gmail inbox. Blocked on Suroy adding the domain in Migadu dashboard and pasting the 3 DNS records (MX + SPF TXT + DKIM TXT) for the Cloudflare API to set up. Cloudflare Email Routing alternative considered and rejected because Nathan's replies would come from his personal Gmail address rather than nathan@tarzantrimming.ca, breaking the brand. |
| 2026-06-09 | SEO launch foundation: canonical URL fix + sitemap + robots.txt (cron, commit `5d4ae95`) | First autonomous-routine deliverable after tarzantrimming entered cron scope. (1) **Critical fix:** `astro.config.mjs` `site` was still the vicjimassage clone value `https://vicjimassage.com.br`, so every page emitted `<link rel="canonical" href="https://vicjimassage.com.br/...">` and the homepage JSON-LD `url` pointed at the Brazilian massage site. Changed to `https://tarzantrimming.ca`. Verified dist: all 4 canonicals + JSON-LD url now correct, zero vicjimassage refs remain. (2) Added custom `src/pages/sitemap.xml.ts` (4 static pages, fixed honest lastmod) + `public/robots.txt` with Sitemap directive. (3) Shipped via wrangler direct-upload (deploy `b3e3e075`, GitHub auto-deploy still not connected), apex poll OK attempt 1, 6-URL deploy-state sweep 6/6 HTTP 200 BAD=0, CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged. **Unblocks** infra TODO #6/#7 (GSC verification + sitemap submission now possible). Note: CF Managed robots.txt prepends AI-bot blocks above our content, but the `Sitemap:` line is present (Google reads it anywhere in the file). |
| 2026-06-09 | Dedicated service detail pages: tree-pruning + limb-removal (cron, commit `7b80ac9`) | **P2 structural-gap deliverable.** The two launch services had rich, friend-approved copy in `services.json` but only rendered inside the single combined `/services/` menu, so neither had a dedicated indexable URL to rank. Added `src/pages/services/[slug].astro` (getStaticPaths over non-`comingSoon` services) producing `/services/tree-pruning/` and `/services/limb-removal/`. Each page: focus-keyword H1, 143/145-char meta description, reused description/process/goodFor copy, "Where we work" + "What it costs" + "Related service" + closing sections, sticky CTA aside, 5 unique internal-link targets (home, /services/, /about/, /contact/, sibling service), and `Service` + `BreadcrumbList` JSON-LD. Linked the `/services/` menu cards to the new pages ("Full details" + name link) for crawl. Added both URLs to `sitemap.xml.ts` PAGES (6 pages now). Build clean 6pg, dist sweep em=0/en=0/24-7=0, meta lengths 143/145 in range. Shipped via wrangler direct-upload (deploy `640dcb7e`, GitHub auto-deploy still not connected), apex content-match poll attempt 1 (~10s), 6-URL deploy-state sweep 6/6 HTTP 200 all ≥5KB BAD=0, CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged. No IndexNow (vicjimassage-derived site has no indexnow script, by design). Site is 11 days old + not yet GSC-verified, so no GSC demand data drove keyword choice; structural-gap rule applied. |
| 2026-06-09 | P0 health audit + head-meta completeness fix (cron, commit `58727fe`) | First P0 audit on tarzantrimming since entering cron scope. **Audit result: clean** — all 4 live pages 200, correct titles, canonical fix from `5d4ae95` confirmed live (every canonical = `tarzantrimming.ca`, 0 vicjimassage leftovers), 0 em/en dashes, 0 banned 24/7 claims, sitemap + robots live. **One gap found + fixed:** `theme-color` meta was absent site-wide (not in src or dist), and `og:url` was missing from the OG block. Added both to `src/layouts/BaseLayout.astro` head: `<meta name="theme-color" content="#2D4A36">` (brand forest green) + `<meta property="og:url" content={canonical}>`. Build clean 4pg; dist sweep theme-color 4/4 + og:url 4/4 (correct per-page URLs) + em=0/en=0/24-7=0. Shipped via wrangler direct-upload (deploy `0835bcbe`, GitHub auto-deploy still not connected), apex content-match poll 1 (~10s), 4-URL deploy-state sweep 4/4 HTTP 200 ≥4KB theme-color present BAD=0, CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged. P0 GSC pipeline/cannibalization sub-checks intentionally skipped: domain is 11 days old, not yet GSC-verified, so NULL `seo_*_daily_kpi` = no-data-yet (expected), not a stale-pipeline failure — no false flag raised. No quote form exists (phone/text funnel) so qf-email/markFieldError P0 checks are N/A by design. |
| 2026-06-09 | Custom 404 page (cron, commit `f4ea7f4`, wrangler deploy `34390b03`) | **P4/UX deliverable.** No 404 page existed, so any unmatched URL returned the homepage at HTTP 200 (CF Pages soft-404 fallback per `feedback_cloudflare_pages_unknown_url_serves_home.md`) — bad for SEO (soft 404s) and gives lost visitors no recovery path. Added `src/pages/404.astro` (BaseLayout, brand-coherent: cream bg, forest H1 "This branch leads nowhere.", 6 recovery cards linking home + both service detail pages + services menu + about + contact, "Back to home" button). Astro built `dist/404.html` (7.2KB); CF Pages now serves it with a proper 404 status for unmatched routes. Build clean 7pg; dist sweep em=0/en=0/24-7=0; all 6 links resolve to real built pages. Shipped via wrangler direct-upload (GitHub auto-deploy still not connected). Also folded in the T04:14 P2 run's uncommitted HANDOFF changelog edits (the `7b80ac9` service-detail-pages doc rows that prior run left dirty in the working tree). |
| 2026-06-09 | Homepage FAQPage schema + fix site-wide broken og:image (cron, commit `0b994de`, wrangler deploy `d4576a2d`) | **P3 structured-data + meta-completeness deliverable.** Two real gaps found while auditing head/schema: (1) the homepage renders 8 substantive FAQ Q&As (`faq.json` via `FAQ.astro`) but emitted only `LocalBusiness` JSON-LD, so no `FAQPage` rich-result eligibility. Added a second JSON-LD block in `index.astro` that maps `faqData.items` → `FAQPage`/`Question`/`acceptedAnswer` (schema matches visible accordion content, Google-compliant). (2) `BaseLayout.astro` `og:image` pointed at `/photos/hero.jpg`, but `public/photos/` did not exist, so every page's OG image 404'd. Generated a brand-coherent `public/photos/og-default.png` (1200x630, forest-green card, brand name + tagline + 7 cities, Arial via PIL) and repointed `ogImage` to it; also added `og:image:width`/`height` + `twitter:image` for completeness. Build clean 7pg; dist JSON-LD parse 2/2 blocks valid (`LocalBusiness`+`FAQPage` with 8 questions), og:image+twitter:image resolve to `og-default.png` (present in `dist/photos/`, 40KB), em=0/en=0/24-7=0. Shipped via wrangler direct-upload (GitHub auto-deploy still not connected). When the friend sends a real hero photo, replace `og-default.png` or repoint `ogImage`. |
| 2026-06-09 | Service Areas hub page (cron, commit `606da92`, wrangler deploy `1e59f0aa`) | **P2 structural-gap deliverable.** Site served 7 cities but had zero local-targeting content beyond the homepage `areaServed` JSON-LD, and no area page existed. Built `src/pages/service-areas.astro`: H1 "Tree Pruning and Limb Removal Across South Ontario", 153-char meta naming all 7 cities, one H2 + honest blurb per city (Toronto/Mississauga/Oakville/Burlington/Hamilton/Kitchener/Whitby) grounded in real tree species + terrain (silver/Norway maples on tight Toronto ravine lots, Lorne Park maturity in Mississauga, Niagara Escarpment edge at Burlington, Carolinian zone at Kitchener, etc.), plus "Not on the list?" + "How the work goes" sections. `Service` JSON-LD (serviceType "Tree Care", 7-city `areaServed`, LocalBusiness provider) + `BreadcrumbList`. Wired into Header nav (site-wide via BaseLayout, desktop + mobile) and `sitemap.xml.ts` PAGES (7 pages now). **Honesty discipline:** deliberately ONE editable hub, not 7 thin per-city doorway pages, because the city list is still friend-unconfirmed (launch TODO #2) and doorway pages are SEO-risky for a new owner-led domain. The Oakville blurb references its stricter private-tree bylaw in general terms only (no specific size/permit threshold asserted) and carries an inline `<!-- FRIEND-VERIFY -->` comment; flagged to MORNING_TASKS for owner confirmation. Build clean 8pg; dist em=0/en=0/24-7=0; both JSON-LD blocks parse (Service 7-city + Breadcrumb); pushed `0d1663a..606da92` first attempt; wrangler direct-upload deploy `1e59f0aa`; apex content-match poll 2 (~20s); 9-route deploy-state sweep all 200 + healthy bytes BAD=0, /nope route correctly 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success. No IndexNow (site has no indexnow script by design). Skills not invoked (structural-gap page build, no single-keyword GSC target on an unverified 12-day-old domain). |
| 2026-06-04 | GitHub + Cloudflare Pages deploy live with hidden contact info | Repo `Notsuroy/tarzantrimming-site` created and pushed (5 commits as of session end). Cloudflare Pages project `tarzantrimming` connected via direct upload (not yet linked to GitHub for auto-deploy -- user can flip that on later via dashboard). Custom domains `tarzantrimming.ca` + `www.tarzantrimming.ca` attached, CF auto-created the CNAME records, SSL provisioned. All contact CTAs (Header phone chip, Hero buttons, FinalCTA, PhoneFloat, Footer column, contact page) conditional-render based on `site.contact.phoneE164`/`email` being non-empty; right now they're blank so the site shows "Launching soon" framing instead of bad/wrong contact info. Once Suroy populates `src/data/site.json` contact block, every CTA lights up. Davey Tree reference softened from explicit "trained at Davey" to generic "commercial tree crews" since Suroy couldn't confirm whether Nathan actually works at Davey or it was friend-of-friend info. ReviewsWall component + reviews.json deleted entirely (rather than left dormant) since Nathan has no real customer reviews yet. |
| 2026-06-10 | P3 meta-description audit: unique in-range descriptions for 4 pages (cron, commit `f322d3f`, wrangler deploy `8ce14103`) | **P3 SEO deliverable.** Audit found 4 pages (`/`, `/services/`, `/about/`, `/contact/`) all emitting the SAME 185-char `site.seo.defaultDescription` (duplicate + over the ~155-char Google truncation limit) because none passed a `description` prop to BaseLayout, falling back to the default. The 3 newer pages (tree-pruning 143, limb-removal 145, service-areas 153) already had unique in-range descriptions. Added a unique, page-specific `description` prop (140-142 chars each) to the 4 stale pages: home (brand + free-estimate CTA), services (two-services framing), about (owner-led arborist story), contact (get-in-touch + free quote). og:description follows automatically (BaseLayout uses the same `desc` var). Build clean 8pg; dist meta-desc 4/4 unique 140-142 + service/area pages unchanged (143/145/153); dist sweep em=0/en=0/24-7=0. Pushed `5411131..f322d3f` first attempt; wrangler direct-upload deploy `8ce14103`; apex content-match poll attempt 1 (~35s); 7-page deploy-state sweep all 200 + healthy bytes BAD=0, sitemap 200, /nope route correctly 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success. No IndexNow (site has no indexnow script by design; meta-only change, no new URLs). P0 health audit run first this cascade = CLEAN (7/7 pages 200 unique titles, 404 works, theme-color/og/favicon present, H1 niche+region, 0 em/en/24-7); GSC/cannibalization sub-checks skipped (domain ~12 days old, not GSC-verified, no-data expected). |
| 2026-06-10 | Breadcrumb schema parity on /about/, /contact/, /services/ (cron P3, commit `58a32cd`, wrangler deploy `24e7ba5a`) | **P3 structured-data deliverable.** Audit found the three 1-level pages (`/about/`, `/contact/`, the `/services/` menu) emitted NO JSON-LD and NO breadcrumb, while the deep pages (`/services/[slug]/`, `/service-areas/`) already had both a visible breadcrumb `<nav aria-label="Breadcrumb">` and matching `BreadcrumbList` JSON-LD. Ported the exact deep-page pattern to all three: a visible `Home / <Page>` breadcrumb nav at the top of each first section + a 2-item `BreadcrumbList` script (`new URL('/', Astro.site)` absolute item URLs) before `</BaseLayout>`. Now every non-home page on the site carries breadcrumb structured data + a visible trail (home keeps LocalBusiness+FAQPage). Build clean 8pg; dist JSON-LD parse 3/3 BreadcrumbList valid (about→[Home,About], contact→[Home,Contact], services→[Home,Services]) + visible nav present 3/3; sitewide sweep em=0/en=0/24-7=0. Pushed `bb3ca0f..58a32cd` first attempt; wrangler direct-upload deploy `24e7ba5a`; apex content-match poll attempt 1 (~10s, `aria-label="Breadcrumb"` live on `/about/`); 8-route deploy-state sweep all 200 + healthy bytes BAD=0, /nope route correctly 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success. No IndexNow (site has no indexnow script by design; no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical template-schema parity fix, no single-keyword GSC target on the unverified 12-day-old domain). |
| 2026-06-10 | Created `blog-pipeline.md` (cron P5, commit pending) | **P5 content-pipeline seed + drift fix (doc-only, no deploy).** Cascade for this run dead-ended: P0/P3 on cooldown (both ran 09:11 same day), P-EXPAND/P-REFRESH need GSC data (12-day unverified domain has none), P1 all friend/Suroy-blocked, P2 no structural gap (services intentionally limited to the 2 offered, areas are one editable hub by design). Real finding: MORNING_TASKS L19 (2026-06-09 T20:22) claimed "all 4 rank-and-rent sites + tarzantrimming have [blog-pipeline] files now" but `tarzantrimming-site/blog-pipeline.md` did NOT exist on disk. Created it, matching the kamloops/fortmac pipeline format: 5 candidate posts (1 when-to-prune, 2 dangerous-limb signs, 3 oak-wilt summer-pruning timing, 4 pruning cost factors, 5 storm-limb cleanup), all angled informational/seasonal/cost (not commercial head terms) and mapped to the 2 offered services. All `[ ] approve` (awaiting Nathan, owner-led honesty discipline). Each carries a Friend-verifies line (seasonal availability, hydro-line boundary, oak-wilt practice, quote-only pricing language, no-emergency-response framing). File documents that the blog SYSTEM still does not exist (`src/pages/blog/` absent), so the first approved topic triggers a 2-run P-PIPELINE sequence (infra build, then draft). 0 em/en dashes verified. No `npm run build` / deploy / cache purge (repo doc, not under src or public). Flagged to MORNING_TASKS: drift correction + Nathan should review/approve topics + per-topic friend-verify items. Also folded in the prior 09:11 run's uncommitted HANDOFF meta-description changelog row. |
| 2026-06-10 | Fix `/about/` heading-hierarchy skip (cron P3, commit `ae58bd7`, wrangler deploy `0d89ef6a`) | **P3 accessibility/SEO deliverable.** Heading-sequence audit across all 7 content pages found exactly one violation: `/about/` emitted `h1 -> h3 -> h3 -> h2(FinalCTA) -> h3 -> h3`, an H1->H3 skip because the two info cards ("What we will tell you upfront" / "What we will not do") used `<h3>` directly under the H1 with no intervening H2. Promoted both card headings from `<h3>` to `<h2>` (styling is class-based `text-xl`, so zero visual change). Sequence now `h1 h2 h2 h2 h3 h3`, no skips. All other pages (home/contact/services/service-areas/both service detail) already had clean hierarchies and no `<h4>` anywhere (no Footer-h4 defect on this vicjimassage-derived template, unlike the rank-and-rent Astro template). Build clean 8pg; dist sweep em=0/en=0/24-7=0. Pushed `0d0cbef..ae58bd7` first attempt; wrangler direct-upload deploy `0d89ef6a`; apex content-match poll attempt 1 (~10s, `What we will tell you upfront</h2>` live on `/about/`); 8-route deploy-state sweep all 200 + healthy bytes BAD=0, /nope route correctly 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success. No IndexNow (site has no indexnow script by design; no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical heading-tag fix, no single-keyword GSC target on the unverified 12-day-old domain). P0 not run this cascade (last P0 09:11 same day, inside 12h cooldown). |
| 2026-06-13 | Service-page FAQ accordion + FAQPage schema on both service pages (cron P3, commit `82d95ac`, wrangler deploy `0945e9f6`) | **P3 structured-data + content parity deliverable.** P0 on 12h cooldown (last 03:15 same day, skipped). Cascade below: P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (15-day domain, no GSC data); P1 friend/Suroy-gated; P2 no structural gap. **Real gap found:** both service detail pages (`/services/tree-pruning/`, `/services/limb-removal/`) carried only `Service` + `BreadcrumbList` JSON-LD with no FAQ content or `FAQPage` schema, the same service-page parity gap closed earlier on the Sudbury/Fort Mac rank-and-rent service pages. Added a slug-keyed `faqMap` in `[slug].astro` driving BOTH a visible `<details>` accordion (matching the homepage `FAQ.astro` styling) AND `FAQPage` JSON-LD from one array (guaranteed schema/visible parity). 5 honest, owner-led FAQs per service: pruning = best season + oak-wilt timing, cost (quote-only), does-pruning-hurt-tree, how-much-canopy-at-once, cleanup; limb-removal = limb-vs-full-removal, turnaround (mirrors services.json "same-day or next-evening", no 24/7), over-roof/driveway rigging, near-power-lines (defers to hydro, honesty), cost. No certs claimed, no 24/7, pricing stays quote-only. Since both pages share the route file, one edit brings both to parity. **Verify:** `npm run build` clean 8pg; dist sweep em=0/en=0/24-7=0 sitewide; FAQPage parse 5 Qs each page + 0 missing-visible-answers (full schema/visible parity). Pushed `f6cfd25..82d95ac` first attempt; wrangler direct-upload deploy `0945e9f6`; apex content-match poll attempt 1 (~10s, needle "When is the best time to prune trees"); 8-route deploy-state sweep all HTTP 200 healthy bytes BAD=0 (sitemap.xml 842B is a small XML file, false-positive on byte threshold) + /nope 404; service pages grew ~13KB→~18KB with FAQ content; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true; desktop+mobile screenshot clean render. No IndexNow (site has no indexnow script by design; content-only, no new URLs). No synthetic test (no quote form). Skills not invoked (structured-data/content parity port mirroring the proven sister-site service-page pattern, no single-keyword GSC target on the unverified 15-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged). |
| 2026-06-13 | Enrich homepage LocalBusiness JSON-LD (cron P3, commit `24fedd3`, wrangler deploy `7f5fcc1e`) | **P3 structured-data deliverable.** P0 health audit run first = CLEAN (8/8 live URLs HTTP 200 Chrome-UA, unique titles, /nope→404, 0 em/en/24-7; GSC/cannibalization sub-checks skipped — domain 15 days old, not GSC-verified, no-data expected; contact block still fully blank in `site.json` so site converts zero leads = known launch-TODO #1 + MORNING_TASKS L64, not re-flagged). Cascade below P0: P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (no GSC data on unverified 15-day domain); P1 all friend/Suroy-gated; P2 no structural gap (2 services intentionally limited, area hub by design); image-alt P3 N/A (site has **0 `<img>` tags**, SVG/CSS only). **Real gap found:** homepage `LocalBusiness` JSON-LD was thin — only name/description/url/areaServed/address/priceRange. Added Google-recommended `image` + `logo` (both → existing `/photos/og-default.png`), a stable `@id` (`https://tarzantrimming.ca/#business`, valuable given the Tarzan Tree Removal Barrie name conflict — strengthens entity identity), and a `hasOfferCatalog` of the **2 real offered services only** (tree-pruning + limb-removal with absolute URLs + serviceType + 7-city areaServed; `full-tree-removal` `comingSoon:true` deliberately EXCLUDED for honesty — built by filtering `services.services.filter(s=>!s.comingSoon)` so it stays honest automatically). `telephone` stays conditionally absent (blank contact). **Verify:** 1 build-fail self-corrected (`services.filter` → `services.services.filter`, JSON shape is `{services:[...]}`); `npm run build` clean 8pg; dist JSON-LD parse: LocalBusiness 12 keys incl @id+image+logo+hasOfferCatalog(2 svc, telephone absent) + FAQPage 8Q both valid; sitewide dist sweep em=0/en=0/24-7=0. Pushed `2532134..24fedd3` first attempt; wrangler direct-upload deploy `7f5fcc1e`; apex content-match poll 1 (~10s, needle `hasOfferCatalog`); 8-route deploy-state sweep all 200 healthy bytes BAD=0 + /nope 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only schema change, no new URLs). Skills not invoked (mechanical entity-schema enrichment, no single-keyword GSC target on the unverified 15-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged L60/L64/L68). |
| 2026-06-13 | FAQ accordion + FAQPage schema on `/service-areas/` (cron P3, commit `7a54e03`, wrangler deploy `0e01fd80`) | **P3 structured-data + content-parity deliverable — closes the last FAQ-less indexable page on the site.** P0 health audit run first this cascade = CLEAN (8 live URLs HTTP 200 Chrome-UA, unique titles, sitemap 200, /nope → 404; GSC/cannibalization sub-checks skipped, domain 15 days old + not GSC-verified = no-data expected; contact block still blank = known launch-TODO #1, not re-flagged). Cascade below P0: P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (no GSC data on unverified 15-day domain); P1 friend/Suroy-gated; P2 no structural gap. **Real gap found:** the homepage and both service detail pages all carry an FAQ accordion + `FAQPage` JSON-LD, but the `/service-areas/` hub carried only `Service` + `BreadcrumbList` JSON-LD with no FAQ. This was the tarzan analog of the portfolio-wide blog-FAQPage-parity rollout running across the 4 rank-and-rent sites today (tarzan has no blog, so the area hub is the right target). Added 5 honest, owner-led FAQs deliberately about WHERE we work (coverage/cities, travel-fee, just-outside-the-area, timing, permits), distinct from the service-technique FAQs on the two service pages. One `faqs` array drives BOTH a visible `<details>` accordion (matching the service-page styling) AND `FAQPage` JSON-LD (guaranteed schema/visible parity). No certs, no 24/7 ("not a middle-of-the-night emergency service" + defers active-on-a-house/power-line cases to hydro/emergency services), quote-only pricing, and the permit answer stays general (no specific bylaw threshold, mirrors the published Oakville blurb). Bumped `/service-areas/` sitemap lastmod 06-09 → 06-13 per the freshness-on-edit rule. **Verify:** `npm run build` clean 8pg 861ms; dist `/service-areas/` 12.7KB → 19.5KB; FAQPage parse = 5 Questions, all 5 answer texts present on page (full parity, 0 schema-only answers); 6 `<details>` = 5 FAQ + 1 mobile-nav (expected); sitewide dist sweep em U+2014 = 0 / en U+2013 = 0; banned-24/7 visible-copy = 0 (the 3 regex hits are all `rgb(250 247 240)` = brand cream `#FAF7F0` in `about.css`, a numeric false-positive, not page copy). Pushed `36c4b55..7a54e03` 1st attempt; wrangler direct-upload deploy `0e01fd80` (GitHub auto-deploy still not connected); apex content-match poll attempt 1 (~10s, needle "Questions about where we work"); 8-route deploy-state sweep all HTTP 200 healthy bytes BAD=0 (sitemap 842B small XML expected) + /nope → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only change, no new URL). No synthetic test (no quote form). Skills not invoked (structured-data/content-parity port mirroring the proven sister-site FAQ-parity pattern, no single-keyword GSC target on the unverified 15-day domain, per `feedback_seo_skill_invocation_required.md` exemption). Every indexable tarzan page now carries an FAQ + FAQPage schema. No new MORNING_TASKS (all tarzan blockers already flagged L60/L64/L68). |
| 2026-06-13 | Per-page sitemap lastmod (cron P3, commit `48c6606`, wrangler deploy `3936a162`) | **P3 freshness-signal deliverable — closes the shared-lastmod defect class on tarzan.** `src/pages/sitemap.xml.ts` emitted a single `LASTMOD = '2026-06-09'` for all 7 URLs, so Google read a flat freshness signal that understated the pages genuinely edited since (home + both service pages changed 2026-06-13, about/contact 2026-06-10) and overstated the rest. Exact defect class hardened across the rank-and-rent sites (`feedback_sitemap_static_paths_anchor_dates.md` + `feedback_p_refresh_must_bump_lastupdated.md`) but never ported to this vicjimassage-derived site. **Site pick:** tarzan won the 3-way co-min `[auto]`/24h tie (4, tied kamloops+fortmac; sudbury 5, belleville 6) — all three tied sites have 0 actionable open issues (kamloops has no Open Issues section; fortmac items 3/4/5 = GSC/citations/competition all Suroy-blocked; tarzan TODOs friend/Suroy-blocked) → tie-break #2 oldest last-touch: tarzan 08:14 < fortmac 09:15 < kamloops 10:25, and tarzan was the only co-min site touched >4h ago (clean HARD-RULE-9 headroom). **Cascade:** P0 skip (last 08:18 ~5h, <12h cooldown); P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (15-day unverified domain, no GSC data); P1 friend/Suroy-gated; P2 no structural gap → **P3.** **Fix (1 file):** replaced `PAGES[]` + scalar `LASTMOD` with a `PAGE_LASTMOD` map of honest git-last-commit dates per page source (home/services/tree-pruning/limb-removal 06-13, service-areas 06-09, about/contact 06-10). Applied the hub-from-leaf rule per `feedback_sitemap_static_paths_anchor_dates.md`: `/services/` hub date = max of its two service leaves (06-13) even though services.astro itself last changed 06-10, so the hub never reports staler than content it links to. Added a MAINTENANCE comment: bump a page's date here in the same commit as any future content change. **Verify:** `npm run build` clean 8pg; dist `/sitemap.xml` shows per-page dates (4× 06-13, 1× 06-09, 2× 06-10), no shared stamp; sitewide dist sweep em U+2014 = 0 / en U+2013 = 0 / banned-24-7 = 0. Pushed `0bd90b8..48c6606` 1st attempt; wrangler direct-upload deploy `3936a162` (GitHub auto-deploy still not connected); apex content-match poll attempt 1 (~10s, needles `2026-06-10` + `2026-06-13` — neither existed in the old uniform sitemap); 8-route deploy-state sweep all HTTP 200 healthy bytes BAD=0 (sitemap 842B = small XML, expected) + /nope → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only change, no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical sitemap-endpoint fix, no single-keyword GSC target on the unverified 15-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged L60/L64/L68). |
