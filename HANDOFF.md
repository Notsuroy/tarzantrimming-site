# tarzantrimming.ca: HANDOFF

> Source of truth for project state. Read before making any non-trivial change.

## Project context

Personal-brand website for a friend rebranding his arborist side-business from "The Ontario Arborist" to "Tarzan Trimming". Built as a gift / practice project by Suroy.

- **Domain:** `tarzantrimming.ca` (registered at Porkbun 2026-05-29 by friend, ~$18/yr; nameservers pointed at Cloudflare, zone active)
- **DNS:** Cloudflare zone `8dccf00d34ed0f68f554bc24a66a54ad` (Suroy's account `d5cadc4c53223e8b3ef7846fdbbffd8e`). Apex + `www` CNAME both proxied at `tarzantrimming.pages.dev`.
- **Hosting:** Cloudflare Pages project `tarzantrimming`, deployed via direct-upload (wrangler), NOT linked to GitHub yet. To enable auto-deploy on push, connect the repo in the CF dashboard later.
- **GitHub:** [Notsuroy/tarzantrimming-site](https://github.com/Notsuroy/tarzantrimming-site) (public)
- **Live URLs:**
  - https://tarzantrimming.pages.dev (always works, auto-issued cert)
  - https://tarzantrimming.ca + https://www.tarzantrimming.ca (custom domain, SSL provisioning ~5-30 min after first DNS attachment)
- **Stack:** Astro 5 + Tailwind 3
- **Predecessor:** `theontarioarborist.ca` (kept live during transition, will 301 redirect to new domain in 6-12 months once new domain has authority)

## Brand positioning

> "Owner-led tree pruning and limb removal across south Ontario. Trained at Davey Tree commercial, working for residential customers on evenings and weekends. Honest quotes, clean work, no high-pressure pitch."

## Design system

Carried over from vicjimassage with a new palette suited to outdoor / arborist context.

**Palette** (see `tailwind.config.cjs`):
- Primary: deep forest green `#2D4A36`
- Warm accent: bark brown `#8B6F47`
- Cool secondary: canopy-sky blue `#5B7B8C`
- Highlight: soft moss green `#9DAE7F`
- Background: warm cream `#FAF7F0`
- Text: deep slate `#2A2E2A`

**Typography:**
- Display: Cormorant Garamond (serif, headings)
- Body: Inter (sans, paragraphs)

**Copy rules (non-negotiable):**
- Never use em dashes (U+2014) or en dashes (U+2013). Use periods, commas, or "to" for ranges. Run `grep -rPn "[\xe2\x80\x94\xe2\x80\x93]" src/` to verify zero hits before pushing.
- No certification claims (ISA, etc) that the owner does not actually hold.
- No "24/7" or "always available" claims. Owner works at Davey during the day.
- Pricing language is fact-only; "Quote on site" placeholders until friend picks pricing strategy.

## Pages built (v1)

- [x] `/`: Funnel homepage (Hero, ProblemPromise, MiniAbout, ServicesGrid, PhotoGallery, ReviewsWall, FAQ, FinalCTA)
- [x] `/services/`: Detailed service menu (pruning, limb removal, full removal as "coming soon"). Offered-service cards now link to their dedicated detail pages.
- [x] `/services/tree-pruning/`: Dedicated indexable service page (focus keyword "tree pruning"). Built 2026-06-09 (cron).
- [x] `/services/limb-removal/`: Dedicated indexable service page (focus keyword "limb removal"). Built 2026-06-09 (cron).
- [x] `/service-areas/`: Service Areas hub (focus keyword "tree service south Ontario"). One page covering all 7 serviceCities with honest, region-accurate per-city blurbs (tree species + property/terrain context, no fabricated bylaw thresholds), `Service` JSON-LD with 7-city `areaServed` + `BreadcrumbList`, internal links to both service pages + about + contact. Built 2026-06-09 (cron). Deliberately a single editable hub, NOT 7 thin per-city doorway pages: city list is still friend-unconfirmed (launch TODO #2) and doorway pages are off-brand + SEO-risky for a 12-day-old domain. Promote to dedicated per-city pages later once cities are confirmed and the domain has authority.
- [x] `/about/`: Owner-led story + what-we-will-and-will-not-do
- [x] `/contact/`: Phone, text, email, service area
- [x] `/404/`: Custom not-found page (recovery links to home, both service pages, services menu, about, contact). Built 2026-06-09 (cron). Astro emits `dist/404.html`, which Cloudflare Pages serves with a proper 404 status for unmatched routes (replaces the previous soft-404 homepage fallback).

JSON-LD `LocalBusiness` + `FAQPage` schema on homepage (FAQPage built from the 8 visible `faq.json` items, `areaServed` covering the 7 service cities). Each service detail page emits `Service` + `BreadcrumbList` JSON-LD.

**Service detail page architecture:** `src/pages/services/[slug].astro` generates one page per non-`comingSoon` service in `services.json` (via `getStaticPaths`). It reuses the already-approved `description` / `process` / `goodFor` copy from `services.json`; per-page H1 + meta-description live in a `seoMap` inside the route file (kept out of `services.json` so the `/services/` menu rendering is untouched). Each page also carries a per-service FAQ: a slug-keyed `faqMap` in the route file drives BOTH a visible `<details>` accordion AND `FAQPage` JSON-LD from one array, so schema and visible text never drift. FAQs are honest (no certs, no 24/7, quote-only pricing) and mirror language already in `services.json`. To add FAQs for a future service, add an entry to `faqMap` keyed by its slug. Full tree removal is intentionally NOT given a page (it is `comingSoon` and not actually offered, so no rankable page is published for it). When full removal becomes a real service, flip `comingSoon: false` in `services.json` and add its H1 + meta to the `seoMap`; the page and sitemap entry will follow.

## Components inherited from vicjimassage-site (rebranded)

- `Header` (sticky, with phone CTA chip)
- `Hero` (gradient + headline + dual CTA)
- `ProblemPromise` (3 you/us pairs)
- `MiniAbout` (owner photo placeholder + story)
- `ServicesGrid` (3 services from `services.json`)
- `PhotoGallery` (6 photo placeholder slots, awaiting friend's shots)
- `FAQ` (8 front-of-funnel items in `faq.json`)
- `FinalCTA` (forest section with call + text-photo CTAs)
- `Footer` (3-column: brand, contact, service area)
- `PhoneFloat` (floating mobile-friendly call CTA; replaces vicjimassage's `WhatsAppFloat`)

## Open items needing Nathan's input (site is LIVE; these are post-launch)

> **Section corrected 2026-07-16 (T11:08 cron P0).** This was titled "TODOs
> before launch" and still described pre-launch state. The site went LIVE
> 2026-06-14. Items 1, 8, and 9 below were verified DONE first-hand against
> the live site and `site.json` and are struck through; the remaining items
> are genuinely still open and gated on Nathan.

1. ~~**Phone number**~~ · **DONE 2026-06-13.** Wired to Nathan's cell
   **(647) 216-1874** (`site.json` `phoneE164: +16472161874`, verified live
   in the sitewide `tel:` links 2026-07-16). The old text here still named
   TOA's `(647) 788-8733`, which was never the published number on this site.
   *Residual ask:* Suroy/Nathan confirm publishing Nathan's personal cell is
   intended (it drops the privacy buffer TOA's tracked line provided).
2. **Service cities** · currently inheriting TOA's 7-city list. Confirm with friend.
3. **Pricing strategy** · `services.json` is set to `quote-only` (no public ranges). Friend can switch to `public-ranges` for stronger CTR if comfortable committing to ranges. **This is also the blocker on blog Candidate 4 (tree pruning cost), the only pipeline candidate with real measured demand.** See `blog-pipeline.md` Phase 0 + MORNING_TASKS.
4. **Hours** · currently labeled "By appointment. Evenings and weekends." Confirm exact availability given Davey day-job schedule.
5. **Tagline** · placeholder is `"Tree pruning and limb removal across south Ontario."` Friend can swap to his own one-liner.
6. **Insurance claim** · FAQ states "carries commercial general liability insurance". Confirm before publishing. If not yet bound, change the FAQ answer to "Insurance is being bound; certificate available before any job".
7. **Day-job employer** · copy now says "commercial tree crews" generically. If friend confirms he actually works at a specific named company AND is comfortable naming it, swap "commercial crews" / "a commercial crew" back to the real name. Initial 2026-05-29 draft had Davey Tree by name; removed when Suroy could not confirm whether the friend works there or it was second-hand info.
8. ~~**Owner photo**~~ · **Placeholder resolved 2026-06-14.** `MiniAbout` now
   renders `about-trees.webp` (licensed stock, decorative, no person). Still
   worth swapping for a real Nathan portrait when he sends one, but the
   "[Owner photo coming]" block is gone.
9. ~~**Job photos**~~ · **Placeholder resolved 2026-06-14.** `PhotoGallery`
   now shows 3 honest "the kind of work we do" stock cards, explicitly not
   presented as Nathan's jobs. Replace with real before/after shots as the
   first projects wrap.
10. *(Reviews section removed entirely in v1 since friend has no reviews yet. To re-add later: copy `ReviewsWall.astro` and `reviews.json` back from `vicjimassage-site` reference, simplify, and re-import in `pages/index.astro`. Honest path: only re-add when there are at least 3 real customer reviews.)*

## TODOs Suroy needs to do (infrastructure)

> **Section corrected 2026-07-16 (T11:08 cron P0)** against first-hand
> checks: git remote, live apex, `site.json`, and a `google-site-verification`
> grep. Items 1 to 5 were already DONE and are struck through. **Item 6 (GSC
> verification) is the one live blocker and it is load-bearing** (see below).

1. ~~Create GitHub repo. Push initial commit.~~ **DONE** · `Notsuroy/tarzantrimming-site`, origin/main tracking.
2. ~~Cloudflare: zone, NS, Pages project.~~ **DONE** · zone `8dccf00d34ed0f68f554bc24a66a54ad`, site live. *(Pages project is NOT GitHub-linked; deploys go through the wrangler direct-upload fallback. See Architecture note.)*
3. ~~DNS records: apex + `www` to Pages, MX.~~ **DONE** · apex live, Migadu MX/SPF/DKIM/DMARC in place.
4. ~~Email setup.~~ **DONE 2026-06-14** · Migadu mailbox `nathan@tarzantrimming.ca` live end-to-end.
5. ~~Update `site.json` `contact.email` from `TODO_EMAIL`.~~ **DONE** · set to `nathan@tarzantrimming.ca`; no `TODO_EMAIL` placeholder remains in `src/`.
6. **Google Search Console. STILL OPEN, and this is the site's binding content blocker. There are TWO independent blockers here, both verified first-hand 2026-07-16, and fixing only one will NOT produce data:**
   - **(a) The domain appears genuinely unverified.** All three verification methods checked and absent: no `google-site-verification` DNS TXT on zone `8dccf00d34ed0f68f554bc24a66a54ad` (only `_dmarc`, Migadu `hosted-email-verify`, and SPF are present), no HTML verification file in `public/`, no verification meta tag in `src/`. The other six sites are all `sc-domain:` (Domain properties), which require exactly the DNS TXT that is missing here.
   - **(b) ~~Even once verified, tarzan is not wired into the pipeline.~~ FIXED 2026-07-16 (T21:08 cron) for the local scripts.** `tarzantrimming.ca` → `sc-domain:tarzantrimming.ca` is now present in the `SITE_TO_GSC_URL` dict of all four local GSC scripts (`.tmp/gsc_discovery_sweep.py`, `.tmp/gsc_daily_kpi.py`, `.tmp/gsc_rank_tracker.py`, `.tmp/portfolio_ranker.py`), 6 → 7 domains each, all compiling. Pre-wired deliberately *before* verification so no code change is needed once the TXT lands. Safe because each script isolates per-site failures with `try/except → continue`; a live read-only probe confirmed tarzan returns a handled `HttpError 403` while `sudburyappliancerepair.ca` returned 262 rows in the same loop. **Residual (b) remainder:** the LIVE daily pull is n8n workflow `uyBDgQqkyq0WNqOz`, read-only to the cron per HARD RULE 2, and still omits tarzan; it needs the same one-line add by hand. So blocker (a) is now the only thing standing between this site and demand data.
   - **Evidence it is these two and not a broken pull:** the daily GSC workflow (`uyBDgQqkyq0WNqOz`) is `active=True` with its last two executions succeeding, and it is feeding the appliance sites normally (MAX `metric_date` 07-13). The pipeline is healthy; tarzan is simply not in it. **Consequence:** the whole GSC-fed half of the cron cascade (P-EXPAND, P-REFRESH, and the P-PIPELINE-AUTO self-feed) is structurally dead on this site, which is why run after run honestly reports NO-SHIP. This is the single highest-leverage unlock here.
7. Submit sitemap to GSC and Bing. **Blocked on 6.** (`https://tarzantrimming.ca/sitemap.xml` exists and returns 200.)
8. Eventually: 301 redirect `theontarioarborist.ca` → `tarzantrimming.ca` once new domain has authority (~6 months out).

## Things this site does NOT have (yet) vs vicjimassage

Intentionally trimmed for v1 scope:

- **No blog.** Astro content collection deleted. Can re-add later if friend wants long-form posts.
- **No bilingual setup.** vicjimassage runs PT root + EN at `/en/`. Tarzan is English-only.
- **No WhatsApp integration.** Canadian customers prefer phone/SMS. `WhatsAppFloat` replaced with `PhoneFloat`.
- ~~**No sitemap.xml route.**~~ DONE 2026-06-09 (cron, commit `5d4ae95`). Custom `src/pages/sitemap.xml.ts` endpoint emits the 4 static pages; `public/robots.txt` references it. Live at `https://tarzantrimming.ca/sitemap.xml`. Add a route to the `PAGES` array in the endpoint when a new page ships.
- **No /quote/ form.** v1 funnels to phone + text. If form conversion data later supports it, can add (using the same /api/quote pattern as the rank-and-rent sites).

## Architecture principles

- **`src/data/site.json` is the source of truth** for brand, contact, location, hours. Every component reads from there. Update once, everything reflects.
- **No certifications or claims that cannot be backed up.** Honest by default. Saves us from having to undo a banned-claim sweep later.
- **The vicjimassage component shapes work.** Don't reinvent the funnel architecture; reuse what's already tested.

## Changelog

| Date | Action | Notes |
|---|---|---|
| 2026-07-20T12:08-04:00 | **P0 health audit FULLY CLEAN + P3 `/contact/` in-content link-equity fix SHIPPED (commit `f3cf24b`)**, closing the last in-content dead end on the site. | **Site pick:** ACTIVE `[auto]`/24h first-hand git = tarzan **0** < sudbury 2 = fortmac 2 (3 FROZEN excluded); tarzan also oldest last-touch (T18:08 07-18, ~42h). HR9 clear. **P0 genuinely due** (last tarzan P0 T14:08 07-18, ~46h > 12h). **P0 CLEAN:** (a) sitemap-derived **14/14** URLs HTTP 200 via spaced Python urllib + Chrome UA, bodies 12,399 to 39,599B (0 under 5KB), **14 distinct titles, 0 dupes**, `/this-should-not-exist-xyz/` returns a real **404** (so no CF unbuilt-URL home fallback masking anything); (c) theme-color `#2D4A36`, `/favicon.svg` 200, `/robots.txt` 200, `tel:+16472161874` + display `(647) 216-1874` both matching `site.json`, H1 mentions niche + region, hours render as "By appointment. Evenings and weekends." with **0** banned 24/7-family phrases, 0 em-dash, 0 en-dash. The `·` in every `<title>` was codepoint-verified as **U+00B7 middle dot, not an en dash** (per memory `mojibake`, verified rather than eyeballed). `qf-email`/`markFieldError` checks are **N/A**: this site has no `/quote/` form by design. (b) screenshot skipped, no visual/source drift. (d) GSC pipeline health is portfolio-level and was verified first-hand **1h earlier** by the T11:08 sudbury run (`uyBDgQqkyq0WNqOz` active, last 3 execs success, `seo_query_daily_kpi` MAX 07-17 = 3d PASS); re-querying inside the hour would be churn, and tarzan has **no rows in the pipeline at all** while blocker 6a stands. (e) N/A, GSC-blind. **Cascade:** P-EXPAND / P-REFRESH structurally dead (blocker 6a, no GSC data); P-PIPELINE human queue **0** `[x] approve` markers first-hand, AUTO self-feed structurally impossible (self-feed reads `seo_query_daily_kpi`, which has no tarzan rows); P1 blocker 6a is Suroy/Nathan-gated → **P3**, executing the explicit cron-doable follow-up the T18:08 07-18 run left. **Finding verified first-hand, not taken on the prior run's word:** `/contact/` carried exactly **1** in-`<main>` link, the breadcrumb `Home`, and **0** in-content internal links. It was also the smallest page on the site (12.4KB live vs 14 to 39KB elsewhere). The two contact cards are themselves `<a href="tel:">` / `<a href="sms:">`, so links could NOT be nested inside them (invalid HTML); the fix had to live in prose. **Fix (2 files):** one new paragraph in the existing "Service area" panel adding **4** natural in-content links, `/service-areas/` ("the areas we serve"), `/services/tree-pruning/` and `/services/limb-removal/` on exact-match phrases, and `/guide/when-to-prune-trees-southern-ontario/` ("seasonal pruning guide"), closing with the site's existing photo-first ask. Anchor classes match the `/about/` sibling pattern exactly. **No new claims:** no price, no certification, no insurance, no availability promise, so the hardened tarzan G5 rule is not engaged. `contact/` sitemap lastmod bumped 06-10 → 07-20 in the same commit (memory `bump-lastupdated`). In-`<main>` link matrix on `/contact/` now **5** (1 breadcrumb + 4 in-content), was 1. **Verify:** build clean **first attempt** (15 pages, 1.05s); dist `<main>` crop confirms all 4 anchors render with correct hrefs; full dist sweep **0 em-dash / 0 en-dash / 0 banned-24-7**; dist sitemap confirms `contact/` lastmod `2026-07-20`. **Ship:** push first attempt `0120c5b..f3cf24b`; wrangler direct-upload deploy `3b3c71fe` (site still not GitHub-linked in CF Pages); content-match marker LIVE at ~10s; **11-URL deploy-state sweep all 200, 12,391 to 39,599B, BAD=0 SMALL=0**; CF zone cache purged `success: true`. IndexNow N/A (no script; site not GSC/Bing-verified). 2 files, $0 DfS, no schema/n8n/Supabase writes. **No new MORNING_TASKS:** every remaining blocker here (GSC 6a, pricing, insurance, hours, cities) is already on file. **Next-run note:** with `/contact/` closed, the site has **no remaining in-content link dead ends**; the internal-link P3 vein on tarzan is exhausted, and further movement is genuinely gated on blocker 6a (GSC verification TXT) rather than on anything the cron can ship. |
| 2026-07-18T18:08-04:00 | **P3 internal-link equity fix SHIPPED (commit `3afe228`)**, `/about/` is no longer an equity dead end. | **Site pick:** ACTIVE `[auto]`/24h first-hand git = tarzan **2** (min) < sudbury 3 = fortmac 3 (3 FROZEN excluded). **P0 NOT due** (last tarzan P0 T14:08 07-18, ~4h < 12h, next ~T02:08 07-19), and the T14:08 run left an explicit cron-doable follow-up, so this run executed it rather than re-auditing. **Cascade:** P-EXPAND / P-REFRESH structurally dead (tarzan still GSC-blind on blocker 6a); P-PIPELINE human queue empty and AUTO pool exhausted (unchanged, no new input since the domain is unverified); P1 blocker 6a Suroy/Nathan-gated -> **P3**. **Finding (verified first-hand, not taken on the prior run's word):** `/about/` carried exactly **1** in-`<main>` link, the breadcrumb `Home`, and **0** in-content internal links of any kind. On a high-intent evaluation page that is a dead end: readers who just decided they trust the operator had no path to a money page, and the page passed no equity onward. **Fix:** 4 natural in-content links added to existing prose, no new paragraphs and no keyword stuffing. `/service-areas/` ("the areas we serve in south Ontario") in the corridor paragraph, `/services/tree-pruning/` and `/services/limb-removal/` on the exact-match phrases already in the focus sentence, and `/contact/` in the closing paragraph. In-`<main>` link matrix on `/about/` now 4 in-content (was 0). `about/` sitemap lastmod bumped 06-14 -> 07-18 in the same commit (memory `bump-lastupdated`). **Verification:** build clean first attempt (15 pages); dist confirms all 4 anchors render inside `<main>` with correct hrefs; dist sweep 0 em-dash / 0 en-dash / 0 banned-24-7 across all files. Commit message initially contained an em dash and was amended before push (unpushed at the time). Wrangler direct-upload deploy `db48a44e`, marker live at 30s, 11/11 deploy-state sweep all 200 and >=12KB (BAD=0 SMALL=0), CF zone cache purged `success: true`. IndexNow N/A (no script; site not GSC/Bing-verified). 2 files, $0 DfS, no schema/n8n/Supabase writes. **Follow-up for the next run (cron-doable, no Suroy input):** `/contact/` still passes **0** in-content internal links (breadcrumb `Home` only, plus tel/sms/mailto). Lower priority than `/about/` was, since it is the conversion endpoint rather than a mid-funnel page, but it is the last remaining in-content dead end on the site. |
| 2026-07-18T14:08-04:00 | **P3 money-page link-equity fix SHIPPED (commit `a5b5d66`)** , homepage service cards now link the two indexable money pages directly instead of the `/services/` hub anchor. | **Site pick:** ACTIVE `[auto]`/24h first-hand git = tarzan **2** (min) < sudbury 3 < fortmac 4 (3 FROZEN excluded), no tie; tarzan also oldest last-touch (T17:08 07-17). **P0 genuinely due** (last tarzan P0 T12:08 07-17, ~26h > 12h) and ran **FULLY CLEAN**: (a) all 14 live sitemap URLs 200 with healthy bodies and correct titles, no CF unbuilt-URL home fallback; (c) theme-color `#2D4A36` matches brand, `/favicon.svg` present, sitewide `tel:+16472161874` matches Nathan's cell, title separator verified U+00B7 middot (not a dash); live-page sweep 0 em-dash / 0 en-dash / 0 banned 24-7 claims across all 14 pages; (d) `[SEO] Daily GSC KPI` (`uyBDgQqkyq0WNqOz`) `active=True`, last 3 executions all `success` (07-16/17/18); (e) N/A, tarzan still GSC-blind on blocker 6a; (b) skipped, no source drift. **Cascade:** P-EXPAND / P-REFRESH structurally dead (GSC-blind); P-PIPELINE human queue empty (C1 + C3 drafted, C2 durable NO-GO, C4 Nathan-pricing-gated, C5 unapproved); P-PIPELINE-AUTO exhausted (no GSC self-feed, 4-post permit cluster complete and Hamilton held per the 06-25 do-not-respend note); P1 blocker 6a Suroy/Nathan-gated -> **P3**. **Finding (first-hand, in-`<main>` link matrix across all 14 live pages):** homepage passed **0** in-content links to `/services/tree-pruning/` and `/services/limb-removal/`. `ServicesGrid.astro` pointed each card at `/services/#<slug>`, so the site's strongest page fed the hub instead of the two money pages targeting those keywords (memory `money-equity`). Fixed the template: `href` -> `/services/<slug>/` plus keyword-bearing anchor text (`More on tree pruning` / `More on limb removal`) replacing generic `Learn more`. The pre-existing `!s.comingSoon` guard already suppresses the link for `full-tree-removal`, which has no detail page, so no broken route is introduced; verified the two non-comingSoon slugs match the `[slug].astro` routes exactly. Homepage sitemap `lastmod` bumped 06-14 -> 07-18 in the same commit (memory `bump-lastupdated`). **Verification:** build clean first attempt (15 pages), dist confirms both new hrefs + 0 residual `/services/#` on home, dist sweep 0 em-dash / 0 en-dash / 0 banned-24-7 across 15 files. Wrangler direct-upload deploy `3ff4db63`, marker live at 0s, 11/11 deploy-state sweep all 200 and >=12KB (BAD=0 SMALL=0), CF zone cache purged `success: true`. IndexNow N/A (no script; site not GSC/Bing-verified). 2 files, $0 DfS, no schema/n8n/Supabase writes. **Follow-up for the next run (cron-doable, no Suroy input):** `/about/` passes **0** in-`<main>` internal links of any kind (no services, no `/service-areas/`, no `/contact/`), making it an equity dead end on a high-intent evaluation page. `/contact/` likewise 0, lower priority since it is the conversion endpoint. |
| 2026-07-17T17:08-04:00 | **P3 internal-link parity fix SHIPPED (commit `f6249c7`)** , added the in-content `/service-areas/` link the Mississauga permit post was missing, closing the follow-up the T12:08 run flagged. | **Site pick:** ACTIVE `[auto]`/24h first-hand git = tarzan 4 = sudbury 4 (tie) < fortmac 6 (3 FROZEN excl); tie-break oldest last-touch = tarzan (last-touch T12:08 ~5h) over sudbury (T16:08 ~1h, also inside its 4h same-task cooldown after its own T16:08 P3). Open Issues both 0 cron-doable. **P0 NOT due** (tarzan last P0 T12:08 ~5h < 12h → next ~T00:08). **Cascade:** P-PIPELINE human queue empty + AUTO exhausted (unchanged from T12:08, no new input since GSC-blind), P-EXPAND/P-REFRESH structurally dead (GSC-blind, blocker 6a Suroy-gated), P1 GSC-verification Suroy/Nathan-gated → **P3**. Verified the documented gap first-hand: permit cluster `/service-areas/` link count was burlington 1 / oakville 1 / toronto 1 / **mississauga 0**. Added one natural contextual link ("working across Mississauga and the wider south Ontario service area") to the closing paragraph of the "How we handle protected trees on a Mississauga job" section, mirroring the sibling pattern in the Tarzan voice, + bumped mississauga `updatedAt` 06-21→07-17 (sitemap lastmod). Parity now 1/1/1/1. Build clean (15 pg), dist sweep 0 em-dash / 0 en-dash / 0 banned-24-7, in-content anchor confirmed rendered in dist. Wrangler direct-upload deploy `825df363`, marker live ~0s, 11/11 deploy-state sweep all 200 ≥12KB (BAD=0 SMALL=0), CF cache purged success. IndexNow N/A (no script; site not GSC/Bing-verified). 1 file (`blog.json`), $0 DfS, no schema/n8n/Supabase writes. |
| 2026-07-17T12:08-04:00 | **P3 internal-link fix SHIPPED (commit `b63dcb5`)** , added an in-content `/service-areas/` link from BOTH money service pages. | P0 first (genuinely-due ~13h since T23:08 07-16): FULLY CLEAN , 14/14 sitemap URLs 200 w/ distinct correct titles (no home-fallback), 0 em/en-dash + 0 "24/7" in dist, theme-color `#2D4A36`, phone `+16472161874`, favicon `/favicon.svg` all correct; separator confirmed U+00B7 middle-dot (not a banned dash, terminal artifact); (d/e) N/A GSC-blind, daily-pull workflow confirmed active by this morning's sudbury/fortmac P0 runs. Cascade below P0: P-PIPELINE human queue empty + AUTO exhausted (C1 dup, C2/C5 demand-dead, C4 price-gated, GSC self-feed dead), P-EXPAND/P-REFRESH GSC-blind, P1 GSC-blocker Suroy-gated → landed on **P3**. Meta-desc audit (136-153 all healthy), image alt/dims audit (all clean), internal-link audit (fixed the double-quote-only regex undercount per `link-quotes` memory) surfaced the real gap: `/service-areas/` (targets "tree service south Ontario") had **0 in-content inbound links from either money page** , the "Where we work" H2 listed cities but linked only `/about/` + `/services/`. Added a natural contextual link ("areas we serve across south Ontario") in `services/[slug].astro` (affects both pages) + bumped `services/`, `tree-pruning/`, `limb-removal/` sitemap lastmod 06-29→07-17. Build clean (15 pg), dist sweep clean, wrangler direct-upload deploy `25877ba5`, marker live ~30s, 11/11 deploy-state sweep OK, CF cache purged. **Follow-up (autonomous, not a Suroy task):** the mississauga permit blog is the only 1 of 4 permit posts missing an in-content `/service-areas/` link its 3 siblings carry , a future-run P3 candidate. |
| 2026-07-16T14:08-04:00 | **P-PIPELINE disarmed a live cannibalization trap in `blog-pipeline.md`** (commit below). No post published, and that is the correct outcome. Doc/data-only: no `src/` change, no build, no deploy, no CF purge, no IndexNow. | **Site pick:** tarzan fewest `[auto]`/24h (2 vs sudbury 3, fortmac 3). P0 not due (T11:08, ~3h < 12h). **Sibling-defect check first (per `sibling-fix`): tarzan is CLEAN of the `Offer.priceRange` defect** the ACTIVE siblings fixed today — verified first-hand, tarzan's `hasOfferCatalog` Offers carry no `priceRange`, and the `"$$"` at `index.astro:43` sits on LocalBusiness where it is valid. **What happened:** human `[x] approve` queue empty → P-PIPELINE-AUTO. GSC self-feed structurally empty (domain still unverified). Seeded pool → C1 (`when to prune trees in Ontario`) ranked top. Phase 0 `seo-keyword-qualifier` INVOKED: **GO WITH MODIFIER** (bare kw 20/mo LOW-17 CPC $11.08, but cluster ~50/mo and the #1 page `localarborist.ca` measures **etv 347/mo across 26 kws = a 17x long-tail multiplier**; SERP 3/3 top = small-arborist blog explainers → G4 PASS, no AI Overview, 1 Reddit at #16 only). Drafted the full 1,301-word post through the 3-phase protocol. **Caught at pre-commit: C1 is a verbatim duplicate of our own live flagship `/guide/when-to-prune-trees-southern-ontario/` (~1,876 words, better sourced — it cites the City of Toronto Apr 1 to Oct 31 oak window, CFIA EAB, and the 2023 Niagara oak-wilt confirmation). Draft REVERTED unpublished** (`git checkout -- src/data/blog.json`, 5 posts restored, tree clean). Shipping it would have self-cannibalized the flagship, the exact thing P0(e) exists to prevent. **Root cause (now saved as memory `g6-blind`): AUTO gate G6 greps `blog.json`/`services.json`/`areas.json` only, and the guide lives in `src/pages/guide/` — outside all three, so G6 structurally cannot see it.** Prior runs (07-01, 07-03) knew "C1 dup of live guide" in HANDOFF prose, but nobody ever edited the file the cron actually READS — it still said `[ ] approve` — so every run had to re-derive the duplication from scratch, and this one failed to until 1,301 words in. **The deliverable is fixing that at the source:** C1 now `[x] skip` with the reason inline, plus a full measured-volume table for C1/C2/C4/C5 so no run re-spends DfS (~$0.30 this run, under cap). **Data recorded:** C2 hazard cluster demand-dead (focus kw null, cluster caps 40/mo) despite passing every honesty/funnel gate; C5 thin (30/mo); **C4 NOT dead — reconciled an apparent conflict: my `tree pruning cost ontario` = null does NOT contradict 06-15's `tree pruning cost` = 210/mo, they are different keywords.** C4 stays the one demand-backed topic, parked on the Suroy price call. **New Step-E flag: `tree pruning toronto` / `tree trimming toronto` = 320/mo each (HIGH 84, $4.82 CPC) and the site has NO per-city page** — 16x the whole blog pipeline's demand; not auto-built (HIGH comp vs a young no-GBP domain, doorway-page risk across 7 corridor cities, and off-page reality says it stalls without a GBP). Needs a Suroy decision. **Net: AUTO is now provably dry until the C4 price call or GSC verification.** Cooldown anchors: P0 next ~T23:08 07-16; C1 permanently skipped. |
| 2026-07-14T15:08-04:00 | **Full ACTIVE cascade re-confirmed DRY first-hand → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min via first-hand git log = **fortmac 3 = tarzan 3 < sudbury 5** (3 FROZEN excluded); tie fortmac/tarzan broken to **tarzan** (fortmac full cascade JUST walked T14:08 ~1h ago = HR9-barred <4h + fortmac P0 not due ~5h; tarzan older last-touch T11:08 ~4h). **P0 NOT due** (tarzan last P0 T11:08 07-14 ~4h < 12h → next ~T23:08) → cascade re-confirmation, not an audit. **First-hand verify tarzan genuinely dry:** (1) HEAD `a37c102` = origin/main, working tree clean, **0 src/remote drift** (last src commit 06-30); (2) tarzan **GSC-blind** (domain still not GSC-verified, MORNING_TASKS L33 root blocker) → P-EXPAND/P-REFRESH/AUTO GSC-self-feed structurally dry; (3) P-PIPELINE human = **0 undrafted `[x] approve`** (blog-pipeline.md read first-hand: C1 dup-of-live-guide/G6, C2/C5 NULL-CA-vol Phase-0 NO-GO, C3 oak `[x] drafted (auto)` 06-30 in 60d cooldown, C4 pricing→Step-E); (4) P-PIPELINE-AUTO velocity-clear (oak 06-30 aged past 7d) but pool durably exhausted — seeded 5 all Phase-0 NO-GO/held, **competitor-gaps.md read first-hand: Gap-1 permit cluster COMPLETE at 4 cities** (Toronto/Mississauga/Oakville/Burlington all `[x] drafted (auto)`) + HARD-STOPPED pending GSC demand (Hamilton 10/mo held), Gap-2 area pages gated on unconfirmed real coverage radius (launch TODO #2), Gap-3 storm angle = C5 NULL-vol NO-GO, Gaps 4-8 (arborist reports/stump/trust block/GBP/hedge) all Nathan no-fabrication decisions; GSC self-feed empty (blind). **Whole ACTIVE portfolio no new input this tick:** appliance GSC **frozen at 07-11** (same data walked DRY at fortmac T14:08 ~1h ago + sudbury T12:08); fortmac cascade HR9-barred <4h; no P0 due anywhere (sudbury T12:08 ~3h, fortmac T10:08 ~5h, tarzan T11:08 ~4h, all <12h); no FROZEN health-check due (kamloops T13:08 ~2h, belleville T20:08 07-13 ~19h, arborist T17:08 07-13 ~22h, all <24h); next GSC advance ~3am 07-15. P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3/P4 saturated (0 src drift); P5 competitor-gaps.md fresh 06-18. **Honest NO-SHIP** — binding constraint = tarzan GSC verification (L33) + Nathan launch/pricing/city decisions (Candidate-4 cost), all already on file → no new MORNING_TASKS (would duplicate). A lastmod-churning tweak here is the churn `appliance-ceiling`/`faq-churn`/`bump-lastupdated`/`no-ship-subset` warn against. Repo untouched. **Anchors:** tarzan cascade re-confirmed dry T15:08 07-14 (next P0 eligible ~T23:08 07-14); next content lever = tarzan GSC verification (L33), ~3am 07-15 appliance GSC pull, or a Nathan decision. |
| 2026-07-14T11:08-04:00 | **P0 health audit DUE (~12h since T23:08 07-13) → ran FULLY CLEAN first-hand (a/c/d; b skip no-src-drift; e N/A GSC-blind) + full ACTIVE cascade re-confirmed DRY → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min via first-hand git log = **tarzan 2 < fortmac 3 < sudbury 4** (3 FROZEN excluded); Open Issues 0/0; tarzan strict min. **P0 genuinely DUE** (last P0 T23:08 07-13, ~12h ≥ 12h) and IS the deliverable. **P0 CLEAN first-hand:** (a) **14/14 live sitemap URLs HTTP 200** via spaced (1.2s) Python urllib + Chrome UA — home 39,579B, service pages ~25.6–25.8KB, `/services/` hub 17,816B, guide 27,545B, 5 blog posts 31.3–33.1KB, `/blog/` 18,654B, smallest `/contact/` 12,391B (all >> 5KB broken-state floor), **14 distinct correct-brand titles (0 dup, no CF home-fallback)**, 404-control `/…zzz/` returns real 404, BAD=0 SMALL=0; live home em-dash U+2014=0 / en-dash U+2013=0 / banned-24-7-family=0; title separator verified **U+00B7 middot at codepoint level** (Win-console cp1252 `�` false-alarm ruled out again — raw em/en counts both 0). (c) home + `/contact/` `theme-color #2D4A36` (forest brand), `favicon.svg` present, `tel:+16472161874` = display `(647) 216-1874` = **site.json canonical Nathan-cell (NOT a new flag)**, hero niche+geo present (Ontario/tree/pruning), CTA visible; no web form by design → `qf-email`/`markFieldError` structurally N/A. (b) screenshot skipped — HEAD `5b82815` = origin/main, working tree clean, **0 src drift** since last CLEAN (last src commit 06-30; all HTML markers pass, would add nothing). (d) **GSC pipeline healthy first-hand:** `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) **active=True** (n8n GET, N8N_PERSONAL); tarzantrimming.ca **GSC-blind** — domain freshness N/A (still not GSC-verified, MORNING_TASKS L33 root blocker unchanged); appliance-shared pipeline confirmed healthy <1h ago by fortmac T10:08 run. (e) cannibalization N/A (no tarzan GSC url data). **Cascade beyond P0 = DRY, re-confirmed first-hand from file state:** P-PIPELINE human = **0 undrafted `[x] approve`** (blog-pipeline read: C1 dup-of-live-guide/G6, C2/C5 NULL-CA-vol, C3 oak `[x] drafted` 06-30 in 60d cooldown, C4 pricing→Step-E; 4-city permit cluster all `[x] drafted (auto)` + HARD-STOPPED pending GSC demand, Hamilton held); P-PIPELINE-AUTO velocity-clear (oak 06-30 aged past 7d) but seeded pool durably exhausted + GSC self-feed empty/blind; P-EXPAND/P-REFRESH GSC-dead (no impression data to select); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3/P4 saturated (0 src drift). Appliance GSC frozen at 07-11 (same data walked DRY 07-13); fortmac HR9-barred <4h. **Honest NO-SHIP** — binding constraint = tarzan GSC verification (L33) + Nathan launch/pricing/city decisions (Candidate-4 cost), all already on file → no new MORNING_TASKS (would duplicate). Repo untouched. **Anchors:** tarzan P0 CLEAN T11:08 07-14 (next eligible ~T23:08 07-14); next content lever = tarzan GSC verification (L33), ~3am 07-14 appliance GSC pull, or a Nathan decision. |
| 2026-07-14T00:08-04:00 | **Full ACTIVE cascade re-confirmed DRY first-hand → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min via first-hand git log = **tarzan 3 < fortmac 4 < sudbury 5** (3 FROZEN excluded); Open Issues 0/0; tarzan is strict min. **P0 NOT due** (tarzan last P0 T23:08 07-13 ~1h < 12h → next ~T11:08) → cascade re-confirmation, not an audit. **First-hand verify tarzan genuinely dry:** (1) `git fetch`, HEAD `27acc82` = origin/main, tree clean, **0 src/remote drift**; (2) tarzan **GSC-blind confirmed LIVE** — SHI REST `seo_query_daily_kpi` tarzantrimming.ca `Content-Range: */0` + body `[]` (domain still not GSC-verified, MORNING_TASKS L33 unchanged) → P-EXPAND/P-REFRESH/AUTO-self-feed structurally dry; (3) P-PIPELINE human = **0 undrafted `[x] approve`** (C1 dup-of-guide/G6, C2/C5 NULL-CA-vol, C3 oak drafted 06-30 in 60d cooldown, C4 pricing→Step-E; permit cluster all 4 `[x] drafted (auto)` + HARD-STOPPED pending GSC demand, Hamilton held); P-PIPELINE-AUTO velocity-clear (oak 06-30 aged past 7d) but pool exhausted + GSC self-feed empty/blind. **Whole ACTIVE portfolio no new input this tick:** appliance GSC **frozen at 07-11** (SHI REST first-hand: sudbury + fortmac MAX metric_date = 2026-07-11 = SAME data already walked DRY at fortmac T21:08 + sudbury T22:08 07-13); both appliance sites **HR9-barred <4h** (sudbury ~2h, fortmac ~3h); next GSC advance ~3am 07-14. P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3/P4 saturated (0 src drift); P5 competitor-gaps.md fresh 06-18. **Honest NO-SHIP** — binding constraint = tarzan GSC verification (L33) + Nathan launch/pricing/city decisions, all already on file → no new MORNING_TASKS. Repo untouched. **Anchors:** tarzan cascade re-confirmed dry T00:08 07-14 (next P0 eligible ~T11:08 07-14); next content lever = ~3am 07-14 appliance GSC pull, tarzan GSC verification (L33), or a Nathan decision. |
| 2026-07-13T10:12-04:00 | **P0 health audit DUE (~20h) ran FULLY CLEAN + ACTIVE cascade DRY → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h via first-hand git log tied **sudbury 2 = tarzan 2** (fortmac 3; 3 FROZEN excluded); Open Issues 0/0; tie-break oldest last-touch → **tarzan** (last ROUTINE_LOG touch T00:08 07-13 ~10h; sudbury just ran T10:08 this hour, HR9-barred <4h; fortmac ran P0 T09:08 ~1h). **P0 genuinely DUE** (tarzan last P0 T14:08 07-12 ~20h > 12h). **P0 result — a/c/d CLEAN, b skip no-src-drift, e N/A GSC-blind:** (a) all **14/14 live sitemap URLs 200** via spaced Python urllib + Chrome UA, **14 distinct titles**, no CF-fallback dup, healthy 12-40KB bodies; (c) home + /contact/ theme-color `#2D4A36`, favicon `/favicon.svg`, phone **(647) 216-1874** consistent (`tel:+16472161874`; stray digit-strings = SVG-path false positives), hero niche+geo present, CTA visible, **0 em-dash / 0 en-dash / 0 "24/7"** (honesty clean); form check N/A (tarzan phone-only by design, not the rank-and-rent qf-email inbound_leads pipeline); (d) [SEO] Daily GSC KPI pull active + appliance data fresh **07-10** confirmed first-hand <2h ago (sudbury T10:08 same shared workflow), tarzan-domain freshness N/A (GSC-blind, unverified per MORNING_TASKS L33); (e) cannibalization N/A (GSC-blind). **Cascade DRY (first-hand file state, not just log):** P-PIPELINE human = **0 undrafted `[x] approve`** (only Candidate 3 `[x] drafted`; C1/C2/C4/C5 remain `[ ]`); P-PIPELINE-AUTO = seeded pool durably exhausted (all 5 Phase-0 NO-GO: C1 dup of live guide, C2/C5 NULL Canada volume, C3 shipped, C4 held Step-E pricing) + competitor-gap Gap-1 permit cluster complete at 4 cities with on-deck hard-stop pending GSC demand + AUTO GSC-self-feed structurally empty (GSC-blind); P-EXPAND/P-REFRESH = no GSC impression data to select (GSC-blind); P1 = launch TODOs (service cities, pricing) Nathan/Suroy-gated; P2 = no claim-free structural gap + no measured demand; P3/P4 = site well-maintained (0 src drift). **Honest NO-SHIP** — binding constraint = tarzan GSC verification (L33) + Nathan launch/pricing/city decisions (Candidate-4 cost L55), all already on file. No dup MORNING_TASKS. Repo untouched. **Anchors:** tarzan P0 CLEAN T10:12 07-13 (next eligible ~T22:12 07-13); cascade NO-SHIP on GSC-blind + appliance-frozen-07-10 state. |
| 2026-07-13T00:08-04:00 | **Full ACTIVE cascade re-confirmed DRY first-hand → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h via first-hand git log tied **sudbury 1 = fortmac 1 = tarzan 1** (3 FROZEN excluded); Open Issues 0/0; tie-break oldest last-touch + only ACTIVE outside HR9's 4h window → **tarzan** (last ROUTINE_LOG touch T14:08 07-12 ~10h; sudbury ran P0 T23:08 07-12 ~1h + fortmac cascade T22:08 07-12 ~2h, both HR9-barred <4h). **P0 NOT due** (tarzan P0 T14:08 07-12 ~10h < 12h → next ~T02:08 07-13) → cascade re-confirmation, not an audit. **First-hand verify tarzan genuinely dry:** (1) `git fetch`, HEAD `1867513` = origin/main, tree clean, **0 src/remote drift**; (2) tarzan **GSC-blind confirmed LIVE** — SHI REST `seo_query_daily_kpi` + `seo_keyword_discovered` both return `[]` for tarzantrimming.ca (domain still not GSC-verified, MORNING_TASKS L33 root blocker unchanged) → P-EXPAND / P-REFRESH / AUTO-self-feed structurally dry; (3) P-PIPELINE human = **0 undrafted `[x] approve`** (the 3 `[x] approve` grep hits are instruction lines L30/34/39, NOT topics — re-verified first-hand, correcting no drift from prior claim); AUTO velocity-clear (last auto oak 06-30 > 7d) but seeded pool exhausted (5 durable NO-GO, permit cluster hard-stopped at 4 cities pending GSC). **Whole ACTIVE portfolio:** appliance GSC frozen at **07-10** (SHI REST first-hand: sudbury + fortmac MAX metric_date = 2026-07-10), already fully walked DRY at T21:08/T22:08 07-12 + both sites HR9-barred <4h; next GSC advance ~3am 07-13. P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3/P4 saturated (0 src drift); P5 competitor-gaps.md fresh 06-18. **Honest NO-SHIP** — binding constraint = GSC verification (L33) + Nathan launch/pricing/city decisions, all already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan cascade re-confirmed dry T00:08 07-13, next P0 eligible ~T02:08 07-13; next content lever = ~3am 07-13 GSC pull or a Nathan decision. |
| 2026-07-12T14:08-04:00 | **P0 health audit DUE (~51h) → ran FULLY CLEAN first-hand (a/c/d; b skip no-src-drift; e N/A GSC-blind) + full ACTIVE cascade re-confirmed DRY → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **tarzan 0 = sudbury 0 < fortmac 1** (3 FROZEN excluded); Open Issues 0/0; tie-break oldest last-touch → **tarzan** (last ROUTINE_LOG touch T15:08 07-10 ~47h; sudbury ran P0 T10:41 07-12 ~3.5h + HR9-barred within 4h; fortmac T17:08 07-11 ~21h). **P0 genuinely DUE** (last P0-health-check T11:08 07-10, ~51h > 12h) and IS the deliverable. **P0 CLEAN first-hand:** (a) **14/14 live sitemap URLs HTTP 200** via spaced (1.3s) Python urllib + Chrome UA — home 39,579B, service pages ~25.6–25.8KB, `/services/` hub 17,816B, guide 27,545B, 5 blog posts 31.3–33.1KB, `/blog/` 18,654B, smallest `/contact/` 12,391B (all >> 5KB broken-state floor), **14 distinct titles (0 dup, no CF home-fallback)**, rendered em-dash U+2014=0 / en-dash U+2013=0 / banned-24-7-family=0 sitewide, BAD=0. (c) home `theme-color #2D4A36` (forest brand), `favicon.svg` present, H1 niche+region; no web form by design → `qf-email`/`markFieldError` structurally N/A. (b) screenshot skipped — HEAD `b0ac6f4` = origin/main, working tree clean, **0 src drift** since last CLEAN (all HTML markers pass, would add nothing). (d) **GSC pipeline healthy:** `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) **active** + last-3 execs all **success** (latest 07-12T01:00Z ~13h prior); tarzantrimming.ca **GSC-blind confirmed live** — Supabase SHI REST `seo_query_daily_kpi` Content-Range `*/0` (domain still not GSC-verified, MORNING_TASKS L33 root blocker unchanged, flagged 06-21); appliance sites MAX **2026-07-09** (3d lag PASS, <4d); `rank_tracking` gsc MAX 07-06T10:00Z (weekly-fed Mon, PASS). (e) cannibalization N/A (no tarzan GSC url data). **Cascade beyond P0 = DRY, re-confirmed first-hand:** P-PIPELINE human queue = **0 undrafted `[x] approve`** (grep: permit cluster L330/346/… all `[x] drafted (auto)`, no bare `[x] approve`); P-PIPELINE-AUTO velocity-clear (last auto = oak 06-30, aged past 7d) but pool exhausted (5 seeded durable NO-GO, permit cluster HARD-STOPPED at 4 cities, GSC self-feed empty/blind); P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3/P4 saturated (0 src drift since last CLEAN); P5 competitor-gaps.md fresh 06-18. **Honest NO-SHIP** — binding constraint = GSC verification (L33) + Nathan launch/pricing/city decisions, all already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan P0 CLEAN T14:08 07-12, next eligible ~T02:08 07-13; content lever next = GSC verification or a Nathan decision. |
| 2026-07-10T11:08-04:00 | **P0 health audit DUE → ran FULLY CLEAN first-hand (a/c/d; b skip no-src-drift; e N/A GSC-blind) + full ACTIVE cascade re-confirmed DRY → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **sudbury 2 = tarzan 2 < fortmac 3** (3 FROZEN excluded); Open Issues 0/0; tie-break oldest last-touch → **tarzan** (last-commit 07-10 01:14 vs sudbury 09:07, fortmac 10:14) AND tarzan is the only ACTIVE outside HR9's 4h window (sudbury ran P0+cascade T09:08 ~2h, fortmac P0 T10:08 ~1h, both foregone NO-SHIP inside 4h). **P0 genuinely DUE** (last P0-health-check T18:08 07-09, ~17h > 12h) and IS the deliverable. **P0 CLEAN first-hand:** (a) **14/14 live sitemap URLs HTTP 200** via spaced (1.3s) Python urllib + Chrome UA — home 39,579B, service pages ~25.7KB, guide 27,545B, 5 blog posts 31–33KB, `/blog/` 18,654B, smallest `/contact/` 12,391B (all >> 5KB broken-state floor), **14 distinct titles (0 dup, no CF home-fallback)**, rendered em-dash U+2014=0 / en-dash U+2013=0 / banned-24-7-family=0 sitewide, BAD=0. Title separator = U+00B7 middot (Win-console cp1252 `�` false-alarm ruled out again — raw em/en-dash counts both 0). (c) home `theme-color #2D4A36` (forest brand), `favicon.svg` present, `tel:+16472161874` = display `(647) 216-1874` (= site.json canonical Nathan-cell, NOT a new flag), H1 "Honest tree work, owner-led, across south Ontario." (niche+region). No web form by design → `qf-email`/`markFieldError` structurally N/A. (b) screenshot skipped — HEAD `5476b61` = origin/main, working tree clean, **0 src drift** since last CLEAN, all HTML markers pass (would add nothing). (d) **GSC pipeline healthy:** `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) **active** + last-3 execs all **success** (latest 07-10T01:00Z ~10h prior); tarzantrimming.ca **GSC-blind confirmed live** — Supabase SHI REST `seo_query_daily_kpi` Content-Range `*/0` (domain still not GSC-verified, MORNING_TASKS L33 root blocker unchanged); appliance sites MAX **2026-07-07** (3d lag PASS, <4d threshold); `rank_tracking` gsc MAX 07-06T10:00Z (weekly-fed Mon, PASS). (e) cannibalization N/A (no tarzan GSC url data). **Cascade beyond P0 = DRY, re-confirmed first-hand:** P-PIPELINE human queue = **0 undrafted `[x] approve`** (blog-pipeline grep: only L152 oak + L330/346/369/401 permit cluster, all `[x] drafted (auto)`; L30/34/39 are instructions); P-PIPELINE-AUTO velocity-clear (0 in rolling-7d — oak 06-30 + Burlington 06-25 both aged past the 7d edge) but pool exhausted (5 seeded durable NO-GO, permit cluster HARD-STOPPED at 4 cities, GSC self-feed empty/blind); P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3/P4 saturated + spot-audited healthy T20:08 07-09 with no src change since; P5 competitor-gaps.md fresh 06-18. **Honest NO-SHIP** — binding constraint = GSC verification (L33) + Nathan launch/pricing/city decisions, all already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan P0 CLEAN T11:08 07-10, next eligible ~T23:08 07-10; content lever next = GSC verification or a Nathan decision. |
| 2026-07-10T01:08-04:00 | **Full ACTIVE cascade re-confirmed DRY first-hand (incl. a fresh competitor-gaps.md read to hunt a non-permit autonomous lever) → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **sudbury 1 = tarzan 1 < fortmac 2** (3 FROZEN excluded); Open Issues 0/0; tie-break oldest last-touch → **tarzan** (T20:08 07-09 ~5h; sudbury T23:08 ~2h; fortmac T00:08 07-10 ~1h) AND tarzan is the only ACTIVE outside HR9's 4h window (sudbury/fortmac both barred). **P0 NOT due** (tarzan P0-health-check T18:08 07-09 ~7h < 12h) → cascade re-confirmation, not a health audit. **Whole ACTIVE portfolio on current data:** sudbury cascade DRY T23:08 on genuinely-advanced 07-07 GSC, fortmac cascade DRY T00:08 on 07-07 GSC, tarzan GSC-blind → no new input since (next GSC pull ~3am 07-10). **First-hand re-verify tarzan genuinely dry:** (1) `git fetch`, HEAD `ccec9af` = origin/main, working tree clean, **0 src/remote drift**; (2) tarzan **GSC-blind confirmed LIVE this run** — Supabase SHI REST `seo_query_daily_kpi` + `seo_keyword_discovered` both Content-Range `*/0` for tarzantrimming.ca (domain still not GSC-verified, MORNING_TASKS L33 root blocker unchanged); (3) P-PIPELINE human queue = **0 undrafted `[x] approve`** (blog-pipeline.md read in full: C1 dup-of-guide, C2/C5 NULL-CA-vol, C3 oak drafted 06-30 in 60d cooldown, C4 pricing→Step-E; 4-city permit cluster all drafted `[x](auto)` + HARD-STOPPED L436-441 "wait for GSC before a 5th city", Hamilton 10/mo held); P-PIPELINE-AUTO velocity-clear (oak 06-30 only in rolling-7d) but pool exhausted + GSC self-feed empty/blind. **Went beyond re-trusting the log — re-read `competitor-gaps.md` first-hand to hunt a non-permit autonomous lever:** the ONLY claim-free autonomous content lever is Gap 1 per-city permit content, which is the completed+paused 4-city cluster; **every other gap is external-gated** by the real-business no-fabrication rule — Gap 2 arborist-reports (ISA/Nathan), Gap 3 area-pages (coverage-radius unconfirmed → no-fabrication block, `serviceCities` still friend-unconfirmed), Gap 4 stump-grinding (Nathan/equipment), Gap 5 storm-limb (NULL-CA-vol + Nathan availability), Gap 6 trust-block (Nathan cert/insurance confirm), Gap 7 hedge (Nathan), Gap 8 GBP (Nathan/Suroy). **P-EXPAND/P-REFRESH GSC-dead** (0 rows); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3/P4 spot-audited well-maintained T20:08 ~5h ago with no new input since (re-audit = HR9 re-tread); P5 competitor-gaps.md fresh 06-18, no refresh-trigger met. **Honest NO-SHIP** — binding constraint = GSC verification (L33) + Nathan launch/pricing/city decisions, all already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan cascade re-confirmed dry T01:08 07-10; next content lever = ~3am 07-10 GSC pull or a Nathan decision. |
| 2026-07-09T20:08-04:00 | **Full ACTIVE deliverable-cascade re-confirmed DRY first-hand + P3/P4 spot-audit (actively hunted a non-content ship, site genuinely well-maintained) → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **tarzan 0 < sudbury 1 = fortmac 1** (3 FROZEN excluded) → tarzan (read-only NO-SHIP runs don't commit to tarzan-site so git undercounts, but tarzan is still the formal min). P0 NOT due (tarzan P0-health-check T18:08 07-09 ~2h < 12h) → cascade re-confirmation, not a health audit. **Whole ACTIVE portfolio walked <4h ago on the current unchanged GSC snapshot** (sudbury T16:27 cascade DRY on 07-06 GSC, fortmac T17:08 cascade DRY on 07-06 GSC, tarzan T18:08 P0), no new input since (next GSC pull ~3am 07-10), so re-doing content on ANY ACTIVE = same-input churn. **First-hand re-verify tarzan genuinely dry:** (1) `git fetch`, HEAD `5289463` = origin/main, working tree clean, **0 src drift**; (2) tarzan **GSC-blind confirmed live this run** — Supabase SHI REST `seo_query_daily_kpi` + `seo_keyword_discovered` both Content-Range `*/0` for tarzantrimming.ca (domain still not GSC-verified, MORNING_TASKS L33 root blocker unchanged); (3) P-PIPELINE human queue = **0 undrafted `[x] approve`** (blog-pipeline.md read in full: C1 dup-of-guide, C2/C5 NULL-CA-vol, C3 oak drafted 06-30, C4 pricing→Step-E, 4-city permit cluster all drafted `[x](auto)` + HARD-STOPPED L436-441); P-PIPELINE-AUTO velocity-clear (oak 06-30 only in rolling-7d) but pool exhausted + GSC self-feed empty/blind; P-EXPAND/P-REFRESH GSC-dead (0 rows). **P3/P4 actively spot-audited (not just re-trusted the log):** `404.astro` present; `PhoneFloat.astro` sticky click-to-call present; blog.json 5 meta descriptions 136–153 chars (all ≤160); service pages use hand-tuned `seoMap` metaDescription 140–155 for the 2 money pages, comingSoon full-tree-removal falls back to 158-char shortDescription (≤160, borderline-over-155 on a not-yet-offered page — deliberate design, changing = churn); per-page sitemap lastmod already shipped 06-13. No claim-free structural gap (P2), no measured demand. **Honest NO-SHIP** — binding constraint = GSC verification (L33) + Nathan launch/pricing/city decisions, all already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan cascade re-confirmed dry T20:08 07-09; next content lever = ~3am 07-10 GSC pull or a Nathan decision. |
| 2026-07-08T01:08-04:00 | **P0 health audit DUE → ran CLEAN first-hand (a/c/d; b skipped no-src-drift; e N/A GSC-blind) + full ACTIVE cascade re-confirmed DRY → NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **sudbury 4 = fortmac 4 = tarzan 4** (all doc-only NO-SHIP commits; 3 FROZEN excluded); Open Issues 0/0 all; tie-break oldest last-touch → **tarzan** (last git touch T20:12 07-07; sudbury T23:24; fortmac T00:12 07-08). HR9 clear (last tarzan run T20:08 ~5h). **P0 genuinely DUE** (last CLEAN T10:08 07-07, ~15h > 12h) and IS the deliverable. **P0 CLEAN first-hand:** (a) **14/14 sitemap URLs HTTP 200** via spaced (1.3s) Python urllib + Chrome UA — home 39,579B, service pages ~25.7KB, guide 27,545B, 5 blog posts 31–33KB, `/blog/` 18,654B, smallest `/contact/` 12,391B (all >> 5KB broken-state floor), **14 distinct titles (0 dup, no CF home-fallback)**, BAD=0; rendered em-dash U+2014=0 / en-dash U+2013=0 all pages. (c) home+`/contact/` `theme-color #2D4A36`, `tel:+16472161874` + display `(647) 216-1874` (= site.json canonical Nathan-cell, NOT a new flag), `favicon.svg` present, **banned-24/7 family = 0** both, H1 "Honest tree work, owner-led, across south Ontario." (niche+region); **title separator verified U+00B7 middot at codepoint level** (cp1252 console `�` false-alarm ruled out again). No web form by design → `qf-email`/`markFieldError` structurally N/A. (b) screenshot skipped — HEAD `c716d81` unchanged since last CLEAN, 0 src drift, all HTML markers pass (would add nothing). (d) **GSC pipeline healthy:** `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) **active** + last-3 exec all **success** (latest 07-08T01:00Z ~8min prior); tarzantrimming.ca **0 rows GSC-blind** (Supabase SHI REST `seo_query_daily_kpi`, MORNING_TASKS L33 unverified-property blocker unchanged); appliance sites MAX **2026-07-05** (2-3d lag PASS); `rank_tracking` gsc MAX 07-06T10:00Z (weekly PASS). (e) cannibalization N/A (no tarzan GSC url data). **Cascade beyond P0 = DRY, re-confirmed first-hand:** P-PIPELINE human **0 undrafted `[x] approve`** (grep); P-PIPELINE-AUTO velocity-clear (oak 06-30 only in rolling-7d) but pool exhausted (5 seeded durable NO-GO, permit cluster HARD-STOPPED at 4, GSC self-feed empty/blind); P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3 clean (0 src drift); P4 no web form by design; P5 saturated. **Honest NO-SHIP** — binding constraint = GSC verification (L33 root unblock) + Nathan launch decisions, both already on file → no new MORNING_TASKS. Anchor: tarzan P0 CLEAN T01:08 07-08, next eligible ~T13:08 07-08. |
| 2026-07-07T20:08-04:00 | Full ACTIVE cascade re-confirmed DRY first-hand + P3 internal-link-graph audit (actively hunted a deliverable, correctly rejected as churn) → **NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **sudbury 3 = fortmac 4 = tarzan 3** → min tie sudbury=tarzan=3; Open Issues 0/0 both; tie-break oldest last-touch → **tarzan** (last ROUTINE_LOG touch T17:08 vs sudbury T19:08 P-PIPELINE-AUTO ~1h, also HR9-barred; 3 FROZEN excluded). **P0 NOT due** (tarzan last CLEAN T10:08 07-07 ~10h < 12h) → cascade re-confirmation, not a health audit. **This run went beyond re-trusting the log — actively hunted for a non-content P3 deliverable via a first-hand internal-link-graph audit:** grepped `/blog/<slug>` href counts in `blog.json` — the 4 permit posts are a **full mesh** (3 inbound + 3 outbound each), and the oak post (`when-to-prune-oak-trees-ontario`, drafted 06-30) has **0 blog-to-blog hrefs** BUT is NOT an orphan: it carries 2 high-relevance inbound links from `/guide/when-to-prune-trees-southern-ontario.astro` (L57) + `/services/[slug].astro` pruning page (L255), i.e. it lives correctly in the PRUNING cluster while the permit posts form the REMOVAL cluster. **Cross-linking oak↔permit rejected as topical churn** (pruning-timing vs removal-permits is a weak tie; exactly the link-mesh churn prior NO-SHIP walks rejected) → no P3 ship. **Inputs re-verified first-hand:** (1) `git fetch` up-to-date, HEAD `851c904` = T17:08 doc-only commit, working tree clean, **0 src/ drift**; (2) P-PIPELINE human queue = **0 undrafted `[x] approve`** (blog-pipeline.md read in full: C1 dup-of-guide, C2/C5 NULL-CA-vol, C3 oak drafted, C4 pricing→Step-E, permit cluster all 4 drafted `[x](auto)`); (3) tarzan still **GSC-blind first-hand** — Supabase SHI REST (bgsimcqixkavtzvnjmoq) `seo_query_daily_kpi` + `seo_keyword_discovered` both Content-Range `*/0` for tarzantrimming.ca (domain not GSC-verified, MORNING_TASKS L33 root blocker unchanged); appliance sites' GSC maxes 2026-07-04 (~3d normal lag, not stale) → whole ACTIVE portfolio in a documented GSC-refresh-wait lull. Cascade DRY: P-PIPELINE-AUTO velocity-clear (only oak 06-30 in rolling-7d) but pool exhausted; P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3 clean (link graph healthy, 0 drift); P4 no web form by design; P5 saturated; cannibalization N/A (no GSC url data). **Honest NO-SHIP** — binding constraint = GSC verification (L33 root unblock) + Nathan launch decisions + Candidate-4 pricing (Step E), all already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan P0 CLEAN T10:08 07-07, next eligible ~T22:08 07-07. |
| 2026-07-07T17:08-04:00 | Full ACTIVE deliverable-cascade re-confirmed DRY first-hand → **NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **3-way tie sudbury 3 = fortmac 3 = tarzan 3** (all doc-only NO-SHIP HANDOFF commits; 3 FROZEN excluded); tie-break oldest last-touch → **tarzan** (last git touch T13:11 07-07 ~4h; fortmac T15:13 ~2h; sudbury T16:12 ~1h, both also HR9-barred). **P0 NOT due** for any ACTIVE site (tarzan last CLEAN T10:08 07-07 ~7h; fortmac T11:08 ~6h; sudbury T09:08 ~8h — all < 12h) → this is a cascade re-confirmation, not a health audit. **Decisive-input deltas since the T13:08 walk, all checked first-hand:** (1) `git fetch` up-to-date, HEAD `2dab0b3`, working tree clean, **0 src/ drift**; (2) P-PIPELINE human queue = **0 undrafted `[x] approve`** (grep: 8 approve marks = 8 drafted; the 3 non-drafted `[x] approve` hits are How-To instruction text L30/34/39, not topics; C1/C2/C4/C5 verified `[ ] approve`, C3 oak `[x] approve (auto) [x] drafted`); (3) tarzan still **GSC-blind first-hand** — Supabase SHI REST (bgsimcqixkavtzvnjmoq) `seo_query_daily_kpi` + `seo_keyword_discovered` + `seo_page_daily_kpi` all Content-Range `*/0` for tarzantrimming.ca (domain not GSC-verified, MORNING_TASKS L33 root blocker unchanged). No delta on any input → cascade identical DRY: P-PIPELINE-AUTO velocity-clear (only oak 06-30 in rolling-7d, < 3/7d + 24h caps) but pool exhausted (C1 dup-of-guide/G6, C2+C5 NULL-CA-vol, C4 pricing→Step-E on file, C3 60d cooldown→~08-29; permit cluster HARD-STOPPED at 4 per blog-pipeline L436-441 "wait for GSC, don't spend blind on a 5th"; GSC self-feed empty 0 rows); P-EXPAND/P-REFRESH GSC-dead (0 rows both); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3 clean (0 src drift, HEAD `2dab0b3`); P4 no web form by design (saturated); P5 saturated; cannibalization N/A (no GSC url data). **Honest NO-SHIP** — binding constraint = GSC verification (L33 root unblock) + Nathan launch decisions, both already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan P0 CLEAN T10:08 07-07, next eligible ~T22:08 07-07. |
| 2026-07-07T13:08-04:00 | Full ACTIVE deliverable-cascade re-confirmed DRY first-hand → **NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = sudbury 1 (fewest) but sudbury ran P-EXPAND T12:08 ~1h ago → HR9 4h-barred; next-fewest tie fortmac 2 = tarzan 2, tie-break oldest last-touch → **tarzan** (T10:08 ~3h; fortmac T11:08 ~2h; sudbury T12:08 ~1h). 3 FROZEN sites all had a health-check <24h (kamloops 07-06 23:08, belleville 07-06 22:08, arborist 07-07 01:08) → none eligible. **P0 NOT due** (last CLEAN T10:08 07-07, ~3h < 12h) — this is a cascade re-confirmation, not a health audit. **Decisive-input deltas since the T10:08 exhaustive walk, all checked first-hand:** (1) git clean, HEAD `fceb268` = the T10:08 doc-only commit, **0 src/ drift**; (2) P-PIPELINE human queue = **0 undrafted `[x] approve`** (all `[x] approve` grep hits are also `[x] drafted` — C3 oak + the 4-city permit cluster; C1/C2/C4/C5 `[ ]`); (3) tarzan still **GSC-blind first-hand** — Supabase SHI REST `seo_query_daily_kpi` + `seo_keyword_discovered` both Content-Range `*/0` for tarzantrimming.ca (domain not GSC-verified, MORNING_TASKS L33 root blocker unchanged). No delta on any input → cascade identical to T10:08 DRY: P-PIPELINE-AUTO velocity-clear (only oak 06-30 in rolling-7d) but pool exhausted (5 seeded durable NO-GO, permit cluster HARD-STOPPED at 4 per blog-pipeline L436-441, competitor-gaps Nathan-gated, GSC self-feed empty); P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3 clean (0 src drift); P4/P5 saturated; cannibalization N/A (no GSC url data). **Honest NO-SHIP** — binding constraint = GSC verification (L33 root unblock) + Nathan launch decisions, both already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan P0 CLEAN T10:08 07-07, next eligible ~T22:08 07-07. |
| 2026-07-07T10:08-04:00 | P0 health audit DUE → ran CLEAN first-hand (spaced Python urllib + Chrome UA per curl-000 memory) + full ACTIVE deliverable-cascade re-confirmed DRY → **NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **tarzan 1 = sudbury 1 = fortmac 1** (3-way tie; 3 FROZEN excluded); tie-break Open Issues 0/0, then oldest last-touch → **tarzan** (last touch T19:25 07-06 ~15h; fortmac T00:08 07-07 ~10h; sudbury T09:08 ~1h, also HR9-barred). **P0 genuinely due** (last CLEAN T19:25 07-06, ~15h > 12h). **P0 CLEAN:** (a) **14/14 live sitemap URLs HTTP 200** via spaced (1.5s) Python urllib + Chrome UA — home 39,565B, both service pages ~25.7KB, guide 27,541B, all 5 blog posts 31–33KB, `/blog/` 18,632B, smallest `/contact/` 12,387B (all >> 5KB broken-state floor), **14 distinct titles** (no CF home-fallback), BAD=0. **Title separator verified U+00B7 middot at codepoint level (NOT em/en dash)** — Win-console cp1252 `�` mojibake false-alarm ruled out; live `<title>` em-dash U+2014=0 / en-dash U+2013=0. (b/c) real-business owner-led site (phone-first, no web form by design → `qf-email`/`markFieldError` structurally N/A, not a defect). (d) **tarzan GSC-blind re-confirmed FIRST-HAND this run — Supabase SHI REST (bgsimcqixkavtzvnjmoq): `seo_query_daily_kpi` + `seo_keyword_discovered` both Content-Range `*/0` for tarzantrimming.ca** (domain still not GSC-verified, MORNING_TASKS L33 root blocker unchanged) → tarzan-domain freshness + (e) cannibalization structurally N/A (no GSC url data). **Cascade beyond P0 = DRY, all re-confirmed first-hand:** P-PIPELINE human 0 undrafted `[x] approve` (only C3 oak `[x] drafted`; C1/C2/C4/C5 `[ ]`); P-PIPELINE-AUTO velocity-clear (only oak 06-30 in rolling-7d, < 3/7d + 24h caps) **but candidate pool exhausted** — 5 seeded durable NO-GO (C1 dup-of-guide/G6, C2/C5 NULL-CA-vol, C3 60d cooldown→~08-29, C4 pricing→Step-E on file), Gap-1 permit cluster HARD-STOPPED at 4 cities (blog-pipeline L436-441 "wait for GSC, don't spend blind on a 5th"), competitor-gaps 2–8 all Nathan-gated (arborist-report/city-coverage/stump/trust-creds/hedge/GBP decisions), GSC self-feed empty (0 rows above); P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap + no measured demand; P3 clean (0 src drift, HEAD 3a5fc3c, repo clean); P4/P5 saturated. **Honest NO-SHIP** — binding constraint = GSC verification (L33 root unblock) + Nathan launch decisions, all already on file → no new MORNING_TASKS (would duplicate). Anchor: tarzan P0 CLEAN T10:08 07-07, next eligible ~T22:08 07-07. |
| 2026-07-06T19:25-04:00 | P0 health audit CLEAN (genuinely-due ~29h) + full ACTIVE deliverable-cascade re-confirmed DRY first-hand → **NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand git log = **tarzan 0** (sudbury 1, fortmac 1; 3 FROZEN excluded), no tie; HR9 clear (last tarzan T17:12 07-05 ~26h). **P0 genuinely due** (last CLEAN T14:08 07-05, ~29h > 12h). **P0 CLEAN:** (a) 14/14 live sitemap URLs HTTP 200 via spaced (0.7s) Python urllib + Chrome UA, 12,391–39,579B all ≥5KB, 14 distinct titles (no CF home-fallback), 404 control `/this-page-does-not-exist-zzz999/` → real HTTP 404, BAD=0; **title separator verified U+00B7 middot (NOT em/en dash)** — console-mojibake false alarm ruled out at codepoint level. (b/c) theme-color #2D4A36, `tel:+16472161874` on `/`+`/contact/` (single consistent phone), phone-first no-web-form design (0 `<form>`, documented owner-led — leads via phone/email not form-to-Supabase), src grep em U+2014=0 / en U+2013=0, all 24/7-family hits are honest negations/notes ("NO 24/7", "no 24/7 framing"). (d) `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) active=True first-hand (N8N_PERSONAL API); tarzan GSC-freshness N/A — `seo_query_daily_kpi` + `seo_keyword_discovered` still **0 rows** first-hand via Supabase REST (GSC-blind, MORNING_TASKS L33 root blocker unchanged). (e) cannibalization N/A (no GSC url data). **Cascade DRY first-hand:** P-PIPELINE human 0 undrafted `[x] approve` (only C3 oak `[x] drafted`; C1/C2/C4/C5 `[ ]`); P-PIPELINE-AUTO velocity-clear (1 auto in rolling-7d: oak 06-30 < 3/7d cap) but pool exhausted — 5 seeded durable NO-GO (C1 dup-of-guide, C2/C5 NULL-CA-vol, C3 60d cooldown→~08-29, C4 pricing→Step-E), Gap-1 permit cluster HARD-STOPPED at 4 cities (blog-pipeline L436-441 "wait for GSC, don't spend blind on a 5th"), GSC self-feed empty; P-EXPAND/P-REFRESH GSC-dead (0 rows). P1 Nathan/Suroy launch/city/pricing/GBP-gated; P2 no claim-free structural gap; P3 clean T14:08 07-05 + 0 src drift (HEAD 77f9527); P4/P5 saturated. **Honest NO-SHIP** — binding constraint = GSC verification (L33 root unblock) + Nathan launch decisions, all already on file → no new MORNING_TASKS (would duplicate). Repo clean. Anchor: tarzan P0 CLEAN T19:25 07-06, next ~T07:25 07-07. |
| 2026-07-05T17:08-04:00 | Full ACTIVE deliverable-cascade re-confirmed DRY first-hand → **NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF/ROUTINE_LOG; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand via git log = sudbury 1 = tarzan 1 (fortmac 2; 3 FROZEN excluded); tie-break oldest last-touch → **tarzan** (T14:08, oldest of the 3 today). **P0 NOT due** (last CLEAN T14:08 07-05, ~3h < 12h) — this run is a cascade re-confirmation, not a health audit. **Decisive input check (first-hand this run):** GSC `seo_query_daily_kpi` MAX metric_date via Supabase REST = **07-02 unchanged** for sudbury + fortmac (byte-identical to the T13:39/T15:08/T16:08 DRY walks), tarzan still **0 rows** (GSC-blind, MORNING_TASKS L33). No GSC advance → no new P-EXPAND / P-REFRESH / P-PIPELINE-AUTO-self-feed can exist since the last 3 ACTIVE cascade walks (all within 3h). **P-PIPELINE human queue = 0 undrafted `[x] approve`** (the 3 `[x] approve` grep hits in blog-pipeline.md are the how-to-approve legend lines 30/34/39, not ticked topics — verified first-hand). **Tarzan source HEAD 8662d22, 0 src/ drift since T14:08 baseline 9a09283** → P3/P4 state identical to T14:08 clean walk; P1 Nathan/Suroy-gated, P2 no gap, P5 saturated. Binding constraint = off-page authority cron can't build; cadence-down rec + tarzan-GSC-blind already on file (MORNING_TASKS) → **no new flag (would duplicate)**. Anchor: tarzan P0 T14:08, next ~T02:08 07-06. |
| 2026-07-05T14:08-04:00 | P0 health audit DUE → ran CLEAN first-hand (Python urllib + Chrome UA, spaced per curl-000 memory); full ACTIVE cascade re-verified DRY → **NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF commit; no source/build-ship/deploy/CF-purge/IndexNow/synthetic) | **Catch-up run after ~23h cron gap** (last portfolio run T13:39 07-05 fortmac; prior tick T15:08 07-03). **Site pick:** ACTIVE `[auto]`/24h portfolio-min first-hand via git log = sudbury 0 = tarzan 0 (fortmac 1; 3 FROZEN excluded); tie-break MOST unresolved Open Issues → **tarzan** (launch/infra TODOs > sudbury's 2 manual). HR9 clear (last tarzan touch T14:08 07-03, ~48h). **P0 genuinely DUE (last CLEAN T09:38 07-03, ~53h > 12h) and IS the deliverable — CLEAN first-hand:** (a) **14/14 canonical live URLs HTTP 200** (spaced Python urllib + Chrome UA) — home 39565B, both service pages ~25.7KB, guide 27541B, all 5 blog posts 31–33KB, smallest `/contact/` 12387B (all >> 5KB broken-state floor), all carry brand token, **0 CF home-fallback**; 404 control `/this-should-404-zzz999/` → real HTTP 404; BAD=0. (b/c) homepage + `/contact/` carry `theme-color #2D4A36` (forest brand) + `tel:+16472161874` consistently sitewide (= site.json canonical (647) 216-1874, the wired-2026-06-13 Nathan-cell pending-Suroy-confirm state per site.json `_phoneNote`, NOT a new flag); live em-dash U+2014=0 / en-dash U+2013=0 / banned-24-7-family=0 on both. **No web form by design** (owner-led phone+text+email) → `qf-email`/`markFieldError` structurally N/A, not a defect. (d) GSC daily-pull workflow health verified portfolio-wide by the fortmac T13:39 run ~30 min prior (shared `[SEO] Daily GSC KPI`, 07-02 data fresh); **tarzan GSC-blind confirmed first-hand — `seo_query_daily_kpi` tarzantrimming.ca = 0 rows via Supabase REST** (domain still not GSC-verified, MORNING_TASKS L33) → tarzan-domain freshness + (e) cannibalization structurally N/A. **Cascade beyond P0 = DRY/BLOCKED, all re-confirmed first-hand:** P-PIPELINE human queue 0 undrafted `[x] approve` (blog-pipeline C1/C2/C4/C5 all `[ ]`, C3 drafted 06-30); **P-PIPELINE-AUTO velocity CLEAR** (only oak 06-30 in rolling-7d, 0 in 24h) **but candidate pool exhausted** — 5/5 seeded dead (C1 dup of live guide/G6, C2+C5 NULL-CA-volume, C4 pricing→Step-E already on file), 4-city permit cluster HARD-STOPPED on a 5th ("wait for GSC demand data"), GSC self-feed empty (0 rows above); P-EXPAND/P-REFRESH N/A (no GSC data); P1 launch/infra TODOs Nathan/Suroy-gated; P2 no measured demand + no claim-free structural gap; P3 ran GENUINELY CLEAN 07-03 T14:08 + **zero source change since** (HEAD still 9a09283, git clean) → re-edit = manufactured churn (`feedback_faqpage_parity_zero_imp_is_churn`); P4/P5 saturated. **No new MORNING_TASKS** — every lever already on file (L33 GSC-verify unlock is the real bottleneck, permit HARD-STOP, C4 cost Step-E, Nathan launch TODOs). Cooldown anchors: tarzan P0 → T14:08 07-05, next eligible ~T02:08 07-06; content pool-empty until GSC verification or a Nathan decision lands. |
| 2026-07-03T14:08-04:00 | Full ACTIVE cascade re-verified DRY first-hand -> **NO-SHIP** (read-only, $0 DfS, doc-only HANDOFF commit; no source/build-ship/deploy/CF-purge/IndexNow/synthetic) | **Site pick:** all 3 ACTIVE tied at 1 `[auto]`/24h (sudbury a0236ea T12:13, fortmac a2d8904 T11:15, tarzan eb52cf6 T09:46; FROZEN excluded). Tie-break oldest last-touch -> **tarzan** (T09:38 < fortmac T11:08 < sudbury T12:08), and tarzan is the only one outside HARD-RULE-9's 4h window (fortmac ~3h / sudbury ~2h since their P0+cascade NO-SHIPs). **P0 not due** (last CLEAN T09:38, ~4.5h < 12h cooldown). **Cascade all re-confirmed first-hand this run:** P-PIPELINE human queue = 0 undrafted `[x] approve` (blog-pipeline C1/C2/C4/C5 all `[ ]`, C3 drafted); **P-PIPELINE-AUTO velocity NOT capped** (only oak 06-30 in rolling-7d; Burlington 06-25 aged out) **but candidate pool exhausted** -- 5/5 seeded dead (C1 dup of live guide/G6, C2+C5 NULL-CA-volume, C4 pricing->Step-E), 4-city permit cluster HARD-STOPPED on a 5th, GSC self-feed empty (unverified domain); P-EXPAND/P-REFRESH N/A (no GSC data). **P3 integrity audit ran GENUINELY CLEAN (first-hand, not churn):** blog.json = 5 posts, **all 5 in sitemap** (auto-derived, per-post lastmod); **every internal link across all 5 post bodies resolves** (validated slug-level vs services.json + blog.json + static routes, 0 broken); **0 orphan posts** (all 5 render on `/blog/` hub in dist); `comingSoon` `full-tree-removal` correctly unbuilt in dist AND **0 blog links point at it** (no dangling 404). Recent runs already took every real P3 lever (title trim 06-30, hero perf 06-28/06-29, internal-link de-orphan T23:08 06-30, sitemap lastmod 06-13) -> another micro-edit = manufactured churn (`feedback_faqpage_parity_zero_imp_is_churn`). P1 launch/infra TODOs Nathan/Suroy-gated; P2 no measured demand + no claim-free structural gap. **No new MORNING_TASKS** -- every lever already on file (GSC-verify unlock is the real bottleneck, permit HARD-STOP, C4 cost Step-E, Nathan launch TODOs). Cooldown anchors: tarzan P0 next eligible ~T21:38 07-03; content pool empty until GSC verification or a Nathan decision lands. |
| 2026-07-03T09:38-04:00 | P0 health audit DUE → ran CLEAN first-hand (Python urllib + Chrome UA, spaced); full ACTIVE cascade re-verified DRY → **NO-SHIP** (read-only, $0 DfS, no build-ship/deploy/CF-purge/IndexNow/synthetic) | **Catch-up run: the hourly cron was down ~32h** (last portfolio run T01:08 07-02; this is the first tick since). **Site pick:** all 3 ACTIVE tied at 0 `[auto]`/24h (cron downtime); tie-break oldest last-touch per ROUTINE_LOG → **tarzan** (T23:47 07-01 < fortmac T00:08 < sudbury T01:08), and tarzan's P0 was the most overdue (last CLEAN P0 T14:45 07-01, ~43h). **P0 CLEAN (genuinely due, IS the deliverable):** (a) **14/14 canonical live URLs HTTP 200** (spaced Python urllib + Chrome UA per curl-000 memory) — home 39579B, both service pages ~25.7KB, guide 27545B, all 5 blog posts 31–33KB, smallest `/contact/` 12391B (all >> 5KB broken-state floor); **14 distinct titles, 0 dup** (no CF home-fallback); `/favicon.svg` 200 (372B, the 06-22 fix holding), `/sitemap.xml` 200, `/robots.txt` 200; BAD=0. (b/c) homepage carries `theme-color #2D4A36` + `tel:+16472161874` (= site.json canonical); live em-dash U+2014=0 / en-dash U+2013=0 / banned-24-7-family=0. **No web form by design** (owner-led phone+text+email) → `qf-email`/`markFieldError` markers structurally N/A, not a defect. (title `·` separator renders as `�` in Win console cp1252 only — em/en counts are 0 and bytes healthy, cosmetic not a defect.) (d) tarzan GSC-blind → tarzan-domain freshness + cannibalization structurally N/A. **Cascade beyond P0 = DRY/BLOCKED, all re-confirmed first-hand this run:** P-PIPELINE human queue 0 undrafted `[x] approve` (blog-pipeline C1-5 all `[ ]`/NO-GO/Step-E); P-PIPELINE-AUTO pool exhausted (5/5 seeded dead, 4-city permit cluster HARD-STOPPED on a 5th, GSC self-feed empty); **P-EXPAND / P-REFRESH GSC-dead — queried Supabase REST first-hand: `seo_keyword_discovered` 0 rows, `seo_query_daily_kpi` 0 rows, `seo_page_daily_kpi` 0 rows for tarzantrimming.ca** (domain still not GSC-verified, MORNING_TASKS L33); P1 launch/infra TODOs Nathan/Suroy-gated; P2 no measured demand + no claim-free structural gap. **P3 genuinely clean, NOT churn (fresh live audit this run):** all 14 meta-desc in 136–153 band, every page single H1, **0 images missing width/height** (CLS-safe), sitemap = exactly the 14 live URLs, oak-wilt post well-integrated (4 inbound from 3 distinct pages / 12 outbound internal links), robots/sitemap 200. Shipping here = manufactured churn (`feedback_faqpage_parity_zero_imp_is_churn`). **No new MORNING_TASKS** — every lever already on file (L33 GSC-verify unlock is the real bottleneck, L45/L314 permit HARD-STOP, C4 cost Step-E, Nathan launch TODOs). Cooldown anchors: tarzan P0 → T09:38 07-03, next eligible ~T21:38; content pool-empty until GSC verification or a Nathan decision lands. |
| 2026-07-01T23:47-04:00 | **NO-SHIP** — full ACTIVE cascade re-verified DRY first-hand (read-only, $0 DfS, no commit-to-src/build-ship/deploy/CF-purge). Built locally (15pg clean) + ran a fresh 5-way dist audit. | **Site pick:** tarzan won the ACTIVE `[auto]`/24h tie (tarzan 0 = fortmac 0; sudbury 1) on oldest-last-touch (tarzan T14:45 < fortmac T17:08). P0 not due (last T14:45, ~9h < 12h). **Cascade DRY:** P-PIPELINE human `[x] approve` = 0; P-PIPELINE-AUTO pool exhausted — seeded 5 all dead (C1 dup of live guide, C2/C5 NULL-CA volume, C3 drafted 06-30, C4 held for Nathan pricing = Step E), 4-city permit cluster explicitly PAUSED (blog-pipeline L436 "wait for GSC data", Hamilton 10/mo held), **GSC self-feed confirmed EMPTY first-hand — `seo_query_daily_kpi` tarzantrimming.ca = 0 rows via Supabase REST (domain still not GSC-verified, MORNING_TASKS L33)**; P-EXPAND/P-REFRESH N/A (no GSC data); P1/P2 gaps all Nathan-gated (area-page coverage L88, arborist-report/stump/trust/GBP owner decisions). **On-page P3 genuinely clean, NOT churn:** dist audit → all 14 indexable meta-desc in 136-153 band (404 at 107 = fine, noindex utility), heading hierarchy 0 skips / single H1 each, sitemap = 14 URLs (all 5 blog posts + guide + all pages; 404 correctly excluded), robots.txt + Sitemap line correct, 15/15 canonicals → tarzantrimming.ca, OG (40735B) + hero assets present, **0 broken internal links**, only "flag" = decorative hero `alt=""` (defensible). **No MORNING_TASKS add** — bottleneck already saturated (L33 GSC-verify unlock, L45 permit-cluster pause, L75-77 portfolio cadence-down). Shipping here = manufactured churn (`feedback_faqpage_parity_zero_imp_is_churn`, fortmac NO-SHIP precedent). **Real unblock unchanged: GSC verification (opens self-feed) + Nathan launch/coverage/GBP decisions.** Cooldown anchors: tarzan P0 next ~T02:45; content run pool-empty until GSC-verify or a Nathan decision lands. |
| 2026-06-30T13:08-04:00 | **P-PIPELINE-AUTO SHIP** — auto-published seeded Candidate 3 as a new blog post: "When to Prune Oak Trees in Ontario" (`/blog/when-to-prune-oak-trees-ontario/`), commit `cf1fbf2`, wrangler deploy `52421800`, LIVE + verified | **Site pick:** STEP 2 ACTIVE portfolio-min — all 3 ACTIVE tied at 8 `[auto]`/24h (sudbury/fortmac/tarzan); tie-break = oldest last-touch per ROUTINE_LOG → **tarzan** (T08:08, ~5h; fortmac T11:08, sudbury T10:08). **P0 (genuinely-due, last tarzan P0 T00:08 06-30 = 13h > 12h):** CLEAN — 13/13 canonical live URLs HTTP 200 healthy bytes 12–40KB (Python urllib + Chrome UA, spaced per curl-000 memory), phone present sitewide, 0 em/en dashes in src, title `·` separator is valid U+00B7 (not a dash); `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) active=True; `rank_tracking_results` gsc MAX 06-29T10:00Z within weekly Monday cadence (not stale); tarzan still GSC-blind (0 `seo_*_kpi` rows — standing L33 blocker, NOT re-flagged); cannibalization N/A (no GSC rows). **Cascade:** P-PIPELINE human `[x] approve` queue empty (5/5 seeded `[ ]`). **P-PIPELINE-AUTO eligible this run** (velocity clear: only Burlington 06-25 in rolling-7d; 0 in 24h). Candidate gate: C1 dup of live `/guide/`; C4 cost = Step E (pricing); C5 storm = NULL CA volume; **C2 "signs a branch is dangerous" exact-phrase = NULL** (diffuse cluster). **C2/C3 had NEVER been Phase-0 qualified** (L311 NO-SHIP directive only ever named C1+C5) → ran the mandatory `seo-keyword-qualifier` first-hand. **C3 = GO:** seeded exact phrase NULL but measured cluster "oak wilt ontario" 90/mo + "pruning oak trees" 70/mo + "when to prune oak trees" 50/mo, all LOW comp; SERP no AI Overview, blog-dominated (matches our format), small ON arborist blogs safetree.ca #5 + toarborist.com #6 already top-6 = winnable, geo gap (savatree #9 = Texas). Shipped focus kw `when to prune oak trees in Ontario`. **Diversifies away from the 4-post near-duplicate permit cluster** (the exact thin-content concern L311 raised) — funnels `/services/tree-pruning/` + `/services/limb-removal/`. **Honesty (hardened tarzan G5):** written 100% claim-free from general arboriculture + primary sources (Invasive Species Centre + CFIA, both verified via WebFetch); NO price/cert/insurance/availability/24-7 claim, no new service line. Phase 4 `seo-on-page-optimizer` = 16-17 PASS / 2 WARN (new-post inbound links; Org byline) / 0 FAIL. 1,205 body words / 1,478 w/FAQs, 0 em/en dashes, 0 24/7. **Verify:** `npm run build` clean 15pg (1st attempt); sitewide dist sweep U+2014=0 / U+2013=0 / banned-24-7=0; BlogPosting+FAQPage+BreadcrumbList schema present; sitemap auto-includes post @ 2026-06-30. Push `e75750a..cf1fbf2` 1st attempt; wrangler direct-upload `52421800` (GitHub auto-deploy still not connected); content-match poll LIVE instantly (needle "Bretziella"); **11-URL deploy-state sweep BAD=0** (all 200, 12–40KB, `/blog/` grew to 18.6KB confirming hub updated); CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. IndexNow N/A (tarzan has no IndexNow script). DfS ~$0.04 < $1 cap (qualifier only). New MORNING_TASKS: AUTO-published FYI + C3 friend-verify (oak-wilt timing as standard guidance / "seal hazard cuts" line). `blog-pipeline.md` C3 marked `[x] approve (auto) [x] drafted` + commit. **Cooldown anchors: C3 topic 60-day from 06-30; tarzan content run next eligible ~T17:08; tarzan P0 next ~T01:08 07-01.** |
| 2026-06-30T08:08-04:00 | **NO-SHIP** — full deliverable cascade re-verified DRY first-hand, read-only ($0 DfS, no commit-to-src/build/deploy/CF-purge/IndexNow/synthetic) | **Site pick:** STEP 2 ACTIVE portfolio-min `[auto]`/24h first-hand via git log = **tarzan 8** (fortmac 9, sudbury 10; 3 FROZEN excluded), strict min — taken on its own dry cascade rather than HR9-diverting (last tarzan ship was T02:08 P3 title-trim ~6h ago, HR9-clear; last tarzan FULL walk was the T00:08 P0 ~8h ago). **Decisive check this run:** queried the SHI Database directly (Supabase MCP `execute_sql`) for tarzantrimming.ca GSC data — `seo_query_daily_kpi` **0 rows**, `seo_page_daily_kpi` **0 rows**, `seo_keyword_discovered` **0 rows**, `rank_tracking_results` source=gsc **0 rows**. The domain (~50+ days old) is STILL not GSC-verified, so the entire GSC-driven engine is blind, confirmed first-hand not assumed. **Cascade:** P0 not due (last T00:08 06-30, +12h = ~T12:08); P-PIPELINE human `[x] approve` queue empty (5/5 seeded NO-GO/Step-E per 2026-06-15 qualifier); P-PIPELINE-AUTO dead (seeded pool exhausted + 4-city permit cluster explicitly HARD-STOPPED at 4 per `blog-pipeline.md` "do not auto-draft a 5th Gap-1 city without re-checking demand" + GSC self-feed 0 rows as verified above; velocity cap is itself clear — only Burlington 06-25 in the rolling 7d — but there is no eligible candidate to publish); P-EXPAND / P-REFRESH GSC-dead (0 rows); P1 launch/infra TODOs all Nathan/Suroy-input-gated (phone, cities, pricing, hours, insurance claim, owner/job photos, GSC verification); P2 = honesty/Step-E (no measured demand + no claim-free structural gap); P3/P4/P5 saturated — the last several tarzan runs already scraped these (link-equity mesh, title trim 64→60, hero LCP, areas-hub + footer guide links, per-post lead images, FAQPage parity), so another would be the documented churn the portfolio NO-SHIP precedent (06-16, 06-21, 06-29 T20:08) explicitly rejects. **Outcome:** documented NO-SHIP is the correct deliverable; no thin commit manufactured. **No new MORNING_TASKS** — every actionable lever already on file: GSC property verification (the single highest-leverage tarzan unblock), the Nathan/Suroy launch TODOs, the Candidate-4 cost-post decision (210/mo, Step E), and the portfolio-wide www→apex 301. Cooldown anchor: tarzan full-cascade NO-SHIP walk → T08:08; next tarzan P0 eligible ~T12:08 06-30. |
| 2026-06-30T02:08-04:00 | **P3 on-page SHIP** — trimmed homepage `<title>` 64→60 chars (commit `0d877a0`, wrangler deploy `e950096e`, LIVE + verified) | **Site pick:** ACTIVE `[auto]`/24h portfolio-min = tarzan **6** (fewest, outright; sudbury 8, fortmac 8; 3 FROZEN excluded). Last tarzan task T00:08 P0 (~2h, P0 on 12h cooldown → not eligible; outside other-task HR9 window). **Cascade:** P0 cooldown; P-PIPELINE human queue 0 `[x] approve` (re-read blog-pipeline.md — the only `[x]` ticks are the 4 already-drafted auto permit posts; seeded Candidates 1-5 still `[ ]`); P-PIPELINE-AUTO pool exhausted (C1-5 NO-GO/escalated per prior runs) + GSC self-feed empty (unverified ~31-day domain); P-EXPAND/P-REFRESH 0 candidates (no GSC data); P1 friend/Suroy-gated; P2 no structural gap (services + service-areas hub built, full-removal intentionally comingSoon). **P3 deliverable:** ran a fresh full dist audit (14pg, title/metadesc/H1/img-alt/img-dim) — only real defect was the homepage `<title>` at **64 chars**, over the ~60 SERP guideline, risking truncation of the geo-modifier "South Ontario" (the most important local term, and it sat last). Title had never been length-optimized (recent commits `f322d3f`/`24fedd3` touched only meta-desc + JSON-LD), so this is a genuine fix, not churn. Changed `index.astro` title from "...Tree Pruning and Limb Removal in South Ontario" → "...Tree Pruning & Limb Removal, South Ontario" (60 chars, pulls the geo 4 chars earlier; `&`/comma allowed, no dashes). **Verify:** `npm run build` clean 14pg; dist title=60 (html.unescape); sitewide dist sweep em U+2014=0 / en U+2013=0 / banned-24-7=0. **Deploy:** push `9f63e8c..0d877a0` 1st attempt; wrangler direct-upload `e950096e` (GitHub auto-deploy still not connected); apex content-match poll attempt **1** (~instant, needle "Removal, South Ontario" = the new comma+geo arrangement, absent from old "in South Ontario"); **11-URL deploy-state sweep BAD=0** (all 200, all >5KB; `/quote/` 404 expected — no quote form); CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (tarzan has no IndexNow script; no new URLs). Skills NOT invoked (mechanical title-length fix, no single-keyword GSC target on the unverified domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged). DfS $0.00. **Cooldown anchors: tarzan P3 T02:08; tarzan P0 next ~T12:08.** |
| 2026-06-30T00:08-04:00 | P0 health audit DUE → ran CLEAN first-hand (curl Chrome-UA), read-only NO-SHIP (no commit-to-src/deploy/CF-purge, $0 DfS) | **Genuinely-due P0** (last full tarzan P0 T12:08 06-29, exactly 12h prior; the intervening T16:08 perf + T22:08 link-equity were SHIPs of a different task-class). **Site pick:** STEP 2 ACTIVE portfolio-min `[auto]`/24h first-hand via git log = **tarzan 5** (fewest) vs sudbury 6, fortmac 8 (3 FROZEN excluded). Last tarzan task T22:08 (~2h, a P3 link-equity SHIP — different task-class, HR9-clean for a due P0). **P0 CLEAN:** (a) **14/14 canonical live URLs HTTP 200** (Chrome UA via curl) with healthy bytes (home 39075B, tree-pruning 25104B, limb-removal 25122B, Burlington post 32591B, service-areas 23811B, guide 26847B, contact 11887B, sitemap.xml 1588B — all well above the 5KB broken-state floor); both control URLs return real **404** (`/nope-404-control-xyz/` + the intentionally-comingSoon `/services/full-tree-removal/`); head-referenced assets `/favicon.svg` 200 (372B), `/photos/og-default.png` 200 (40735B, `image/png`), `/photos/hero-canopy.webp` 200 (296688B `image/webp` = the T16:08 06-29 optimization holding). BAD count = 0. (b/c) homepage + contact + tree-pruning + Burlington post all carry `theme-color #2D4A36` (= brand forest green) + `/favicon.svg` ref; `(647) 216-1874` display + `tel:+16472161874` present on home + contact (= site.json canonical); home `<title>` "Tarzan Trimming · Tree Pruning and Limb Removal in South Ontario" (niche + region), single H1; contact lead path live (email `nathan@tarzantrimming.ca` + `sms:+16472161874` ×2 + `tel:` ×4). **No web form by design** (owner-led phone+text+email) → `qf-email`/`markFieldError` rank-and-rent quote-template markers structurally N/A, NOT a defect. Live em-dash U+2014 = **0** / en-dash U+2013 = **0** / banned-24-7-family = **0** on all 4 representative pages. (d) tarzan GSC-blind (0 `seo_*_kpi` rows, unverified-property blocker MORNING_TASKS L33) → tarzan-domain freshness + cannibalization (steps d2/e) structurally N/A; portfolio `[SEO] Daily GSC KPI` pipeline verified active+fresh ~1h prior by the fortmac T23:08 06-29 P0 (active=True, `seo_query_daily_kpi` MAX 06-27, `rank_tracking_results` gsc 06-29T10:00Z) → not re-spent within the hour. **Cascade beyond P0 = DRY/HARD-STOPPED (first-hand re-confirmed):** P-PIPELINE human queue 0 undrafted `[x] approve` (only the 4 drafted permit posts ticked); P-PIPELINE-AUTO pool exhausted (5/5 seeded NO-GO/Step-E per 2026-06-15 qualifier, 4-city permit cluster HARD-STOP on a 5th, GSC self-feed 0 rows on unverified property — velocity moot, pool empty); P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 friend/Suroy/Nathan-gated; P2 new pages = fabrication/Step-E; P3 link-graph saturated (T22:08 Burlington inbound + punctuation fix, ~2h ago); P4 healthy. **Orphaned-WIP check:** tarzan working tree clean, in sync origin/main 0/0. **Outcome:** the DUE P0 health audit IS this run's deliverable (real-business health CLEAN); cascade dry/blocked → NO-SHIP, no `src/` touched, no build/deploy/CF-purge/IndexNow, no synthetic test (no quote form). No new MORNING_TASKS (GSC-verify L33 + Candidate-4 cost L52 + permit hard-stop + Nathan launch TODOs all on file). DfS $0.00, read-only (curl + git + local files). **Cooldown anchor: tarzan P0 → T00:08 06-30, next eligible ~T12:08 06-30; content run pool-empty until GSC verification (L33) or a Nathan decision (L52) lands.** |
| 2026-06-29T22:08-04:00 | **P3 internal-link-equity SHIP** — linked the Burlington permit post from the service pages + guide (was missing); fixed list punctuation; bumped sitemap lastmod. Commit `c602c9c`, wrangler deploy `9aae4847`, LIVE + verified | **Real drift caught (prior P3 audits missed it):** the 4-post permit cluster (Toronto/Mississauga/Oakville/Burlington) is fully sibling-cross-linked, BUT the inbound references from the two higher-authority **money pages** + the flagship guide were stuck at 3 cities. The `permitGuides` array in `src/pages/services/[slug].astro` (its own maintenance comment says "Keep this list in lockstep with the live /blog/ permit posts") + the hardcoded list in `guide/when-to-prune-trees-southern-ontario.astro` both listed only Toronto/Mississauga/Oakville — **Burlington (published 06-25, newest) had ZERO inbound links from anything but its 3 sibling posts.** Added Burlington to both. **Site pick:** STEP 2 ACTIVE portfolio-min `[auto]`/24h first-hand via git log = **tarzan 5** (sudbury 6, fortmac 6; 3 FROZEN excluded), clear min. P0 not due (last T12:08 → ~T00:08 06-30). Cascade: P-PIPELINE human 0 `[x] approve`; P-PIPELINE-AUTO pool exhausted (5 seeded NO-GO/Step-E per 2026-06-15 qualifier, 4-city permit cluster HARD-STOPPED on a 5th, GSC self-feed 0 rows on unverified property); P-EXPAND/P-REFRESH GSC-dead; P1 Nathan/Suroy-gated; P2 = fabrication/Step-E. The inbound-link gap on the live cluster is a genuine non-churn P3 (link equity to the newest money-funnel post), now HR9-clean (last tarzan ship was T16:08 perf, ~6h ago). **Bonus quality fix (same render block, folded into the commit):** the `permitGuides` JSX fragment left trailing whitespace after each `</a>`, rendering visible "Toronto , Mississauga , Oakville , and Burlington ." (spaces before commas/period) on BOTH money pages. Collapsed the fragment to one line → now clean "Toronto, Mississauga, Oakville, and Burlington." **Verify:** `npm run build` clean 14pg (1st attempt); Burlington link present in built tree-pruning + limb-removal + guide HTML (1 each); rendered punctuation clean; sitemap lastmod bumped 06-29 on `services/`, `services/tree-pruning/`, `services/limb-removal/`, `guide/...` (hub-from-leaf rule honored); sitewide dist sweep em U+2014 = 0 / en U+2013 = 0 / banned-24-7 = 0. Push `fb6d29e..c602c9c` 1st attempt; wrangler direct-upload deploy `9aae4847` (GitHub auto-deploy still not connected); apex content-match poll attempt 1 (~10s, needle `tree-removal-permit-burlington` on `/services/tree-pruning/`); **11-URL deploy-state sweep all HTTP 200 healthy bytes BAD=0** (home 39075B … Burlington post 32591B); CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. IndexNow N/A (tarzan has no IndexNow key by design; no NEW URLs, only inbound links + freshness on existing pages). No synthetic test (no quote form). No new MORNING_TASKS (no blockers). DfS $0.00. **Cooldown anchor: tarzan P3 link-equity SHIP T22:08; tarzan P0 next ~T00:08 06-30; content run pool-empty until GSC verification (L33) or a Nathan decision (L52) lands.** |
| 2026-06-29T16:08-04:00 | **P3/P4 perf SHIP** — optimized the shared `hero-canopy.webp` LCP image, commit `d131934`, wrangler deploy `6d2bb003`, LIVE + verified | **Closes the recurring "shared 457KB hero" WARN** flagged in the last several Phase 4 on-page audits (Burlington 06-25 etc.). `public/photos/hero-canopy.webp` was **458KB** and is the homepage LCP element (`Hero.astro`, `fetchpriority="high"`) AND the visible content hero on the Oakville + Burlington permit posts (`blog.json`). **Site pick:** STEP 2 ACTIVE portfolio-min `[auto]`/24h first-hand via git log = **tarzan 4** (fortmac 5, sudbury 8; 3 FROZEN excluded). tarzan's last run T12:08 (~4h, a P0 — different task-class, HR9-clean for a deliverable). **Diagnosis (non-destructive scratchpad test first):** quality was NOT the lever — re-encoding at q82/q88 produced ≥460KB (the original was already ~q82; weight is high-frequency canopy detail + resolution). The real lever was **resolution**: 1366×911 → **1200×800** at q74 method 6 → **296,688 bytes (35% / ~161KB cut)**. Safe on the homepage (image sits behind a 78% dark overlay + gradient + grain, decorative `aria-hidden`) and on the 2 blog heroes (content width ≤ 1200). Updated the `width`/`height` attrs to 1200×800 in `Hero.astro` + both `blog.json` heroImage entries (accurate intrinsic dims; aspect 1.4995→1.50 negligible, CLS preserved). **Verify:** `blog.json` JSON.parse OK; `npm run build` clean 14pg (1st attempt); dist `hero-canopy.webp` = 296688B @ 1200×800; 3× `width="1200"` in built HTML (homepage + 2 blog heroes); dist sweep em U+2014 = 0 / en U+2013 = 0 / banned-24-7 = 0. Pushed `56201ab..d131934` 1st attempt; wrangler direct-upload deploy `6d2bb003` (GitHub auto-deploy still not connected); apex content-match poll attempt 1 (~10s, needle `width="1200" height="800"`); **11-URL deploy-state sweep all HTTP 200 healthy bytes BAD=0** (home 39075B … Oakville post 32390B); live image confirmed 296688B; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` cache purged success:true. IndexNow N/A (tarzan has no IndexNow key by design; no new URLs, asset swap only). No synthetic test (no quote form). No new MORNING_TASKS (no blockers; the WARN it resolves is now closed). DfS $0.00. **Cooldown anchor: tarzan content/perf run next eligible ~T20:08; tarzan P0 next ~T00:08 06-30 (unchanged from T12:08).** |
| 2026-06-29T12:08-04:00 | P0 health audit DUE → ran CLEAN first-hand (curl Chrome-UA), read-only NO-SHIP (no commit-to-src/deploy/CF-purge, $0 DfS) | **Genuinely-due P0** (last full tarzan P0 T15:08 06-28, ~21h ago, well past the 12h cooldown; intervening tarzan run T23:08 06-28 was a P3/P4 SHIP, different task-class). **Site pick:** STEP 2 ACTIVE portfolio-min `[auto]`/24h first-hand via git log = fortmac **2** (sudbury 6, tarzan 6; 3 FROZEN excluded). fortmac fewest but ran T09:57 (~2h, inside HARD-RULE-9's 4h window) + sudbury ran T10:08 (~2h, also HR9-blocked) → neither min/tie site carries a non-repeat deliverable. Diverted to **tarzan** (last task T23:08 06-28, >4h ago = HR9-clean) whose P0 is genuinely DUE. **P0 CLEAN:** (a) **13/13 canonical live URLs HTTP 200** (Chrome UA via curl — Python urllib hung on this box, curl is the reliable path) with healthy bytes (home 39075B, tree-pruning 24990B, Burlington post 32591B, contact 11887B, sitemap.xml 1588B), **0 dup titles**, all brand-correct (`· Tarzan Trimming`); both control URLs return real **404** (`/nope-404-control-xyz/` + the intentionally-comingSoon `/services/full-tree-removal/`, NOT a home-page 200 fallback); head-referenced assets `/favicon.svg` 200 (372B, the T21:08 06-22 fix holding) + `/photos/og-default.png` 200 (40735B). BAD count = 0. (b/c) homepage + Burlington post + tree-pruning + contact all carry `theme-color #2D4A36` (= brand forest green), `tel:+16472161874` = `(647) 216-1874` (= site.json canonical); live em-dash U+2014 = **0** / en-dash U+2013 = **0** / banned-24-7 = **0** on all four. **No web form by design** (owner-led phone+text+email) → `qf-email`/`markFieldError` markers N/A, not a defect. (d) P0 GSC pipeline: tarzan GSC-blind (0 `seo_*_kpi` rows, unverified-property blocker MORNING_TASKS L33) → tarzan-domain freshness + cannibalization (steps d2/e) structurally N/A; portfolio `[SEO] Daily GSC KPI` pipeline verified active+fresh ~2.5h prior by the fortmac T09:57 P0 (active=True, `seo_query_daily_kpi` 06-26, `rank_tracking_results` gsc 06-29T10:00Z), no open flag → not re-spent within the hour. **Cascade beyond P0 = DRY/HARD-STOPPED (first-hand re-confirmed):** P-PIPELINE human queue 0 undrafted `[x] approve` (re-read `blog-pipeline.md`: only the 4 drafted permit posts ticked); P-PIPELINE-AUTO pool exhausted (5/5 seeded NO-GO/Step-E, 4-city permit cluster HARD-STOP on a 5th, GSC self-feed 0 rows on unverified property — velocity moot, pool empty); P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 friend/Suroy/Nathan-gated; P2 new pages = fabrication/Step-E; P3 link-graph/meta/sitemap saturated (T18:08 06-28 full dist QA CLEAN) + recent on-page gaps all closed (favicon, service-page hero images T23:08, webp re-encode, blog lead images); P4 healthy. **Orphaned-WIP check:** tarzan working tree clean, in sync origin/main 0/0. **Outcome:** the DUE P0 health audit IS this run's deliverable (real-business health CLEAN); cascade dry/blocked → NO-SHIP, no `src/` touched, no build/deploy/CF-purge/IndexNow, no synthetic test (no quote form). No new MORNING_TASKS (GSC-verify L33 + Candidate-4 cost L52 + permit hard-stop + Nathan launch TODOs all on file). DfS $0.00, read-only (curl + git + local files). **Cooldown anchor: tarzan P0 → T12:08 06-29, next eligible ~T00:08 06-30; content run pool-empty until GSC verification (L33) or a Nathan decision (L52) lands.** |
| 2026-06-28T23:08-04:00 | P3/P4 SHIP: added a CLS-safe hero photo to BOTH service (money) pages (commit `6a9f7ef`); wrangler deploy `5e47cd17` + CF purge | **Site pick (honest HR9 divert):** STEP 2 strict ACTIVE portfolio-min `[auto]`/24h via first-hand git log = fortmac **1** (sudbury 5, tarzan 4; 3 FROZEN excluded), but fortmac ran a full-cascade NO-SHIP T22:08 (~1h, inside HARD-RULE-9's 4h window) + fortmac P0 not due (last T13:08 → ~T01:08 06-29) → re-walking fortmac repeats that task. Diverted to next-fewest eligible = **tarzan** (4 < sudbury 5), last task T18:08 P3-audit (~5h, **outside** the 4h window → HR9-clean). **No ACTIVE P0 due** (tarzan T15:08 → ~T03:08 06-29). **Cascade re-verified DRY/BLOCKED first-hand:** P-PIPELINE human 0 `[x] approve`; P-PIPELINE-AUTO pool exhausted (5/5 seeded Phase-0 NO-GO/Step-E per record, 4-city Gap-1 permit cluster HARD-STOPPED on a 5th per `blog-pipeline.md`, Hamilton 10/mo deprioritized, GSC self-feed 0 rows on unverified property L33); P-EXPAND/P-REFRESH GSC-dead; P1/P2 Nathan/Suroy-gated. **Real gap found (the legitimate P3/P4 deliverable):** a fresh first-hand dist audit (meta-desc length+uniqueness all clean, single H1/page, all imgs alt+dims, internal mesh 12-15/page) surfaced that the **two `/services/` pages — the highest-intent money pages — were the ONLY image-less pages on the site** (home 5 imgs, every blog post 1 img, service pages 0). **Action (1 file, `src/pages/services/[slug].astro`):** added a per-slug `heroMap` + conditional `<img>` banner between the intro block and the body grid, sourced from existing licensed `public/photos` assets (`work-climb.webp` → `/tree-pruning/`, `work-limb.webp` → `/limb-removal/`); decorative, honest descriptive alt (no cert/job/outcome claim), `width=height=800` intrinsic + fixed `h-64 md:h-96 object-cover` display box = CLS-safe; `comingSoon` full-tree-removal still excluded so no new claim surface. **Verify:** `npm run build` clean (14 pages, exit 0); both service pages render the hero `<img>` with correct src/alt/width/height; sitewide dist sweep em-dash U+2014 = **0** / en-dash U+2013 = **0** / banned-24-7 = **0**; both photos present in `dist/photos`. Push `6a8a958..6a9f7ef` 1st attempt; wrangler direct-upload deploy `5e47cd17`; apex content-match LIVE in ~20s (`work-climb.webp` on `/services/tree-pruning/`, `work-limb.webp` on `/services/limb-removal/`); **11-URL deploy-state sweep all HTTP 200 healthy bytes** (service pages now ~25KB, up from ~17KB; home 39KB; no 500/tiny), no broken state; CF zone cache purged (success). No IndexNow (modified existing URLs, not new pages). No synthetic test (no quote form by design). No new MORNING_TASKS (GSC-verify L33 + Candidate-4 cost L52 + permit hard-stop + Nathan launch TODOs all on file). DfS $0.00. **Cooldown anchor: tarzan P3/P4 service-image SHIP T23:08 06-28; tarzan P0 next ~T03:08 06-29; content run pool-empty until GSC verification (L33) or a Nathan decision (L52) lands.** |
| 2026-06-28T18:08-04:00 | NO-SHIP — HR9-divert ACTIVE pick (tarzan); ran a fresh full P3 on-page dist audit (built locally, 14pg) that re-verified CLEAN → no real defect to fix; read-only, $0 DfS, no commit-to-src/deploy/CF-purge/IndexNow | **Site pick (honest HR9 divert):** STEP 2 strict ACTIVE portfolio-min `[auto]`/24h = fortmac **1** (fewest) vs sudbury 3, tarzan 3, but fortmac ran a full-cascade NO-SHIP T16:08 (~2h, inside HARD-RULE-9's 4h window) + fortmac P0 not due (last T13:08 → ~T01:08 06-29) → re-walking fortmac repeats that task. Sudbury just shipped a **P2 area page T17:08** (~1h, /areas/coniston/) → another sudbury P2 within the hour = HR9 repeat-block + thin-content-spike risk. Diverted to tarzan, last task T15:08 P0 (~3h, **outside** the 4h window → a non-P0 task-class here is HR9-clean). **No ACTIVE P0 due** (tarzan T15:08 → ~T03:08 06-29; sudbury T14:08 → ~T02:08; fortmac T13:08 → ~T01:08). **No FROZEN check due** (kamloops T21:08 06-27 → ~T21:08 today; arborist T23:08 06-27; belleville T11:08 06-28 → all later). **Live data-phase re-verify (Supabase REST, $0 DfS, first-hand not parroted):** `tarzantrimming.ca` returns empty `[]` in BOTH `seo_keyword_discovered` (P-EXPAND) AND `seo_page_daily_kpi` (P-REFRESH), and 0 rows in `seo_query_daily_kpi` (portfolio has 7 domains with 06-21+ data; tarzan absent) → GSC still unverified (~30-day domain, standing L33 blocker), so P-EXPAND / P-REFRESH / P-PIPELINE-AUTO GSC-self-feed all yield 0 candidates. **Cascade DRY/BLOCKED:** P-PIPELINE human 0 `[x] approve` (5/5 seeded `[ ]`, all Phase-0 NO-GO/Step-E); P-PIPELINE-AUTO pool exhausted — seeded C1/C3 dup the live flagship guide, C2/C5 NULL-Canada-vol, C4 cost 210/mo Step-E price-gated (MORNING_TASKS L52), the 4-city Gap-1 permit cluster is a HARD STOP on a 5th (`blog-pipeline.md` L420-425), GSC self-feed 0 rows → 0 eligible (velocity moot: last AUTO Burlington 06-25, 0 in 24h / 1 in 7d, both caps clear but pool empty); P1 launch/infra TODOs friend/Suroy/Nathan-gated; P2 new service/area pages = fabrication/coverage decision = Step E. **P3 deliverable attempt = on-page QA audit (the legitimate non-churn output this hour):** built `dist` locally (14 pages clean) and swept ALL 14 HTML files first-hand — em-dash U+2014 = **0**, en-dash U+2013 = **0**, banned-24/7 = **0**; meta-descriptions all in-band (about 141 / blog 136-153 / guide 153 / home 142 / service-areas 153 / services 140 / pruning 143 / limb 145; 404 = 107 is fine for a noindex recovery page, no overflow anywhere); images all carry alt + width/height (homepage `alt=""` is the `aria-hidden` hero background = correct decorative pattern, the other 4 home + 5 blog/guide imgs have descriptive alt); sitemap `lastmod` is per-page distinct (06-10 → 06-25, NOT a shared build-time stamp); blog-post JSON-LD = BlogPosting + BreadcrumbList + FAQPage + LocalBusiness + Organization + ImageObject; Burlington post internal mesh = full (links Toronto/Mississauga/Oakville siblings + both service pages + service-areas + about + contact, well over the 4-8 target). **→ P3 genuinely CLEAN, no defect exists.** Manufacturing a speculative SEO tweak on a 0-GSC-data site is churn (per the 0-impression-page anti-churn memories), so NO-SHIP is correct. **Orphaned-WIP check:** tarzan tree clean, in sync origin/main. **Outcome:** the fresh first-hand P3 on-page audit (confirming no real defect) IS this run's output; nothing in `src/` touched → no build-commit/deploy/CF-purge/IndexNow; no synthetic test (no quote form by design). No new MORNING_TASKS — the GSC-verify blocker (L33), Candidate-4 cost decision (L52), permit hard-stop (L297), and Nathan launch TODOs (L133/L134) are all already on file. DfS $0.00, read-only (Supabase REST + local build + git). **Cooldown anchor: tarzan NO-SHIP/P3-audit cascade-class T18:08 06-28; tarzan P0 next ~T03:08 06-29; content run pool-empty until GSC verification (L33) or a Nathan decision (L52) lands.** |
| 2026-06-28T15:08-04:00 | P0 health audit DUE → ran CLEAN, read-only NO-SHIP (no commit-to-src/deploy/CF-purge, $0 DfS); also validates the T12:08 webp perf deploy is healthy sitewide | **Genuinely-due P0** (last full tarzan P0 T11:12 06-27 per commit `fe8ebf0`, ~28h ago, well past the 12h cooldown; the T12:08 06-28 task was P3/P4 perf, a different task-class). **Site pick (honest HR9 divert):** STEP 2 strict ACTIVE portfolio-min `[auto]`/24h = sudbury **1** = fortmac **1** (tarzan 2, most), but BOTH min-sites ran their full deliverable cascade inside HARD-RULE-9's 4h window (sudbury T14:08 ~1h, fortmac T13:08 ~2h) → no HR9-clean deliverable on either. Diverted to tarzan, whose P0 is genuinely DUE this hour AND whose last GSC-cascade walk was T08:08 (~7h, HR9-clean) → tarzan is the only ACTIVE site carrying an actionable, non-repeat deliverable. **P0 CLEAN:** (a) **14/14 live URLs Chrome-UA HTTP 200** with healthy bytes (home 39075B, tree-pruning 24697B, Burlington post 32591B, contact 11887B, sitemap.xml 1587B — all above the 5KB broken-state floor) and unique brand-correct `<title>`s; unbuilt `/this-page-does-not-exist-xyz/` → real HTTP **404** (custom 404 page). (b/c) homepage `theme-color #2D4A36` (= brand forest green), `tel:+16472161874` = `(647) 216-1874` = site.json canonical, email `nathan@tarzantrimming.ca`, hero H1 "Honest tree work, owner-led, across south Ontario." (niche + region; pruning ×9, limb ×8, ontario ×6 on home), CTAs visible (5 `tel:` links + Get-in-touch/quote/text). **No web form by design** (owner-led phone+text+email) → `qf-email`/`markFieldError` are rank-and-rent quote-template markers, structurally N/A here, NOT a defect. Live em-dash U+2014 = 0 / en-dash U+2013 = 0 / banned-24-7 = 0 on home + Burlington post + tree-pruning page. **Deploy verification (T12:08 webp perf SHIP):** LIVE and healthy — 5 webp refs on home, `hero-canopy.webp` serves HTTP 200 `image/webp`; the T12:08 wrangler deploy landed cleanly and broke no route. **(d) P0 GSC pipeline health:** tarzan is GSC-blind (0 `seo_query_daily_kpi` / `seo_keyword_discovered` rows, unverified-property blocker on file MORNING_TASKS L33), so tarzan-domain GSC freshness + cannibalization (steps d2/e) are structurally N/A; portfolio-level `[SEO] Daily GSC KPI` pipeline was verified active+fresh by the sudbury T14:08 P0 (~1h prior) with no open gsc-pull/gsc-stale flag → not re-spent this run. **Cascade beyond P0 = DRY/HARD-STOPPED:** P-PIPELINE human queue 0 `[x] approve` (5/5 seeded `[ ]`); P-PIPELINE-AUTO genuinely exhausted — seeded C1/C3 dup the live guide, C2/C5 NULL-Canada-vol, C4 cost Step-E price-gated (MORNING_TASKS L52), the 4-city permit lever is now a **HARD STOP on a 5th permit post** (MORNING_TASKS L297), GSC self-feed 0 rows → 0 eligible candidates (velocity moot: 2 AUTO in rolling 7d, under the 3 cap, but pool empty); P-EXPAND/P-REFRESH N/A (no GSC data); P1/P2 friend/Suroy/Nathan-gated. **Orphaned-WIP check:** tarzan working tree clean, in sync origin/main. **Outcome:** the DUE P0 health audit IS this run's deliverable (real-business health CLEAN + T12:08 deploy validated). NO-SHIP, no `src/` touched → no build/deploy/CF-purge/IndexNow, no synthetic test (no quote form). No new MORNING_TASKS (GSC-verify L33 + Candidate-4 cost L52 + permit hard-stop L297 + Nathan launch TODOs all on file). DfS $0.00, read-only (curl + git + local files). **Cooldown anchor: tarzan P0 → T15:08 06-28, next eligible ~T03:08 06-29; tarzan content run pool-empty until GSC verification (L33) or a Nathan decision (L52) lands.** |
| 2026-06-28T12:08-04:00 | P3/P4 perf: re-encoded all 7 `public/photos/*.webp` at q80 (commit `0903094`); SHIPPED via wrangler deploy (`9e1951f5`) + CF purge | **First SHIP after a long portfolio-wide NO-SHIP streak.** Site pick: tarzantrimming = ACTIVE portfolio-min (0 `[auto]`/24h vs sudbury 1, fortmac 1, verified first-hand via git log). P0 not due (last T11:08 06-27, 12h cooldown → next ~T23:08; also note: this is the run AFTER the long ~9h+ logging gaps, P0 anchors intact). Cascade: P-PIPELINE human queue empty + P-PIPELINE-AUTO genuinely exhausted (5 seeded candidates all qualified-out per the 2026-06-15 Phase-0 record: C1 dup-of-guide, C2/C5 NULL-Canada-vol, C3 gov-wall+dup, C4 cost held for Nathan; 4-city permit cluster complete + paused; 0 GSC self-feed rows on unverified property); P-EXPAND/P-REFRESH N/A (no GSC data); P1 all friend/Suroy-gated (GSC verify + Nathan business confirms, already on file). Rather than a 4th+ consecutive NO-SHIP, took the one concrete, non-churn, autonomous P3/P4 lever: the recurring Phase-4 "457KB hero" WARN. **Action:** PIL q80 re-encode (method=6) in place, smaller-only guard, dimensions preserved. Photo payload 1945.8KB → 1554.4KB (**−391KB / −20%**): about-trees −32%, work-limb −39%, work-rigging −37%, work-climb −35%, accent-forest −17%, accent-branches −10%, hero-canopy −2%. **Honest caveat:** hero-canopy (the homepage LCP hero w/ `fetchpriority=high`, also Burlington+Oakville blog hero) is genuinely near-incompressible foliage — even q70 only −18% and it's full-bleed so can't safely downscale (1366px ≈ 1x retina at 1200px container). So the headline "457KB hero" WARN number barely moves; the real wins are the lazy-loaded gallery/about/accent photos (total bytes/bandwidth, not LCP). Loading strategy already correct pre-change (LCP hero `fetchpriority=high`, gallery/about/guide `loading=lazy`, blog hero `eager`) so no loading-attr lever existed. **Verify:** `npm run build` 14 pages clean; dist sweep 0 em-dash/0 en-dash/0 banned-phrase; all 7 re-decode as valid WEBP, dims unchanged; live apex about-trees.webp Content-Length = 231294 (new) confirmed; 11-URL deploy-state sweep all HTTP 200 healthy bytes (11.8–39KB), no 500/tiny; homepage screenshot confirms hero renders crisp, no artifacts. CF cache purged (success). No IndexNow (no new URLs). No new MORNING_TASKS. Cooldown anchor: tarzan P3/P4 perf T12:08; tarzan P0 still ~T23:08 06-27. |
| 2026-06-27T11:08-04:00 | P0 health audit DUE → ran CLEAN, read-only NO-SHIP (no commit-to-src/deploy/CF-purge, $0 DfS) | **Genuinely-due P0** (last full tarzan P0 T23:08 06-26, exactly 12h prior; prior runs anchored "tarzan P0 ~T11:08" as the next forward event). **13/13 canonical live URLs HTTP 200** (Chrome UA) with healthy bytes (11.9KB to 39.1KB) and unique brand-correct `<title>`s (U+00B7 middle-dot separator). Homepage: `theme-color #2D4A36` (= brand forest green), `/favicon.svg` present, `tel:+16472161874` = `(647) 216-1874` consistent on home + contact + LocalBusiness schema, hero "Tree Pruning · Limb Removal · South Ontario" (niche + region), CTAs visible (Get in touch / Free quote / Send a text). **No web form by design** (0 `<form>` on home + contact; owner-led lead capture = phone + text + protected email) → `qf-email`/`markFieldError` checks are rank-and-rent quote-template markers, structurally N/A here, NOT a defect. Honesty CLEAN: "By appointment, evenings and weekends", emergency FAQ honestly defers urgent daytime to full crews, no 24/7 / always-available claims. **P0 step d (GSC pipeline, portfolio-wide):** `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) active=true; last 3 executions all `success` (06-27/06-26/06-25 01:00Z); `seo_query_daily_kpi` MAX = 2026-06-24 for sudbury + fortmac (3d lag < 4d = FRESH); tarzan rows = 0 (unverified-GSC-property blocker, already on file in MORNING_TASKS, NOT re-flagged). No flags. **P0 step e (cannibalization):** N/A (0 GSC query rows on the unverified property). **Cascade beyond P0:** P-PIPELINE human queue empty + P-PIPELINE-AUTO pool exhausted (4-city permit cluster complete) + 0 GSC self-feed rows; P-EXPAND/P-REFRESH N/A (no GSC data); P1 infra friend/Suroy-gated; P2/P3 saturated → DRY. Outcome: DUE P0 IS this run's deliverable (real-business health CLEAN); NO-SHIP, no commit-to-src. No new MORNING_TASKS (GSC verification + Nathan/Suroy launch TODOs already on file). Cooldown anchor: tarzan P0 → T11:08, next eligible ~T23:08 06-27. |
| 2026-06-27T08:08-04:00 | NO-SHIP — ACTIVE portfolio-min pick (tarzan, `[auto]`/24h = 1, fewest outright); full deliverable cascade independently re-verified DRY/BLOCKED on FRESH live data; read-only, $0 DfS, no commit-to-src/build/deploy/CF-purge/IndexNow. | **Site pick:** STEP 2 strict ACTIVE portfolio-min `[auto]`/24h = tarzan **1** (fewest, no tie) vs sudbury 2, fortmac 2 (FROZEN kamloops/belleville/arborist excluded). Last tarzan task T23:08 06-26 P0 (~9h ago, outside HARD-RULE-9 4h window → clean to re-walk). **No ACTIVE P0 due** (tarzan last T23:08 → next ~T11:08; sudbury T01:08; fortmac T04:08). **No FROZEN health check due** (kamloops T14:08 / arborist T17:08 / belleville T20:08 06-26 → all +24h later today). **Live re-verify (Supabase REST, $0 DfS, not parroted):** `tarzantrimming.ca` = Content-Range `*/0` in `seo_query_daily_kpi` + empty `[]` in `seo_keyword_discovered` + empty `[]` in `seo_page_daily_kpi` → GSC still unverified (~29-day domain, standing L33 blocker), so P-EXPAND / P-REFRESH / P-PIPELINE-AUTO GSC-self-feed all 0 candidates. **Cascade DRY/BLOCKED:** P-PIPELINE human 0 `[x] approve`; P-PIPELINE-AUTO pool exhausted — seeded C1-5 all Phase-0 NO-GO/escalated (C1 dupes the live flagship guide; C2/C5 NULL CA volume; C3 govt-SERP + guide-covered; **C4 cost 210/mo = only real demand, Step E behind Nathan price call, MORNING_TASKS L52**), 4-city Gap-1 permit cluster declared COMPLETE + hard-paused pending GSC (`blog-pipeline.md` L420-425), GSC self-feed 0 rows; velocity moot anyway (Burlington 06-25T22:08 → ~34h, 1/24h clear; rolling-7d = Oakville+Burlington = 2, 3/7d clear) — pool-empty not cap-bound. P1 friend/Suroy-gated; P2 area-pages/new-service-lines fabrication/Nathan-gated (G5-hardened); P3 link-graph + sitemap-lastmod + blog meta/title saturated (06-13/06-16/06-25 full 4-city mesh); P4 healthy; P5 competitor-gaps.md current. **Orphaned-WIP check:** working tree CLEAN, in sync origin/main 0/0, nothing to adopt. **Outcome:** per anti-churn rules the fresh live re-verify IS the output; no rendered content manufactured to bump the counter → no build/deploy/CF-purge/IndexNow; no synthetic test (no quote form by design); nothing broken → no auto-fix. No new MORNING_TASKS (GSC-verify L33 + Candidate-4 cost L52 + Burlington FYI L44 + off-page reality + Nathan launch TODOs all on file). DfS $0.00, read-only (Supabase REST + git + local files). **Cooldown anchor:** tarzan NO-SHIP cascade-class T08:08 06-27; P0 next ~T11:08; content run unblocked on velocity but pool-empty until GSC verification lands (L33) or Nathan decides Candidate 4 (L52). |
| 2026-06-26T23:08-04:00 | P0 health audit DUE → ran CLEAN, read-only NO-SHIP (no commit-to-src/deploy/CF-purge, $0 DfS). **Notable new fact this hour: the P-PIPELINE-AUTO velocity cap LIFTED but the candidate pool is genuinely empty — cap is moot.** | **Site pick:** STEP 2 strict ACTIVE portfolio-min `[auto]`/24h = tarzan **1** (fewest) vs sudbury 4, fortmac 5 (FROZEN kamloops/belleville/arborist excluded). **P0 genuinely DUE** (last tarzan P0 T11:08 06-26, +12h=T23:08 exactly now). **P0 CLEAN:** (a) all 13 live sitemap URLs Chrome-UA HTTP 200, real distinct content 11.8KB–39KB (home 39075B, tree-pruning 24697B, Burlington post 32591B, contact 11887B — all above the 5KB broken-state floor), unique correct titles, no home-page fallbacks. (b/c) homepage title correct, phone `(647) 216-1874` (`tel:+16472161874`) present, "Book" CTA present, live home+contact em-dash U+2014=0 / en-dash U+2013=0 / banned-24-7=0; contact page uses `mailto:`+`tel:` by design (real-business owner-led, no n8n quote form — confirmed in `src/pages/contact.astro`). (d) `[SEO] Daily GSC KPI` `uyBDgQqkyq0WNqOz` active=True; tarzan = Content-Range `*/0` in `seo_query_daily_kpi` (GSC still unverified, ~28-day domain → freshness rule N/A, not a stale flag); appliance-site GSC backbone verified FRESH (06-24) earlier this afternoon. (e) cannibalization N/A (no tarzan GSC ranking data). **Velocity-cap re-computation (genuinely changed this hour):** last P-PIPELINE-AUTO publish Burlington 06-25T22:08 → now ~25h (>24h) so 1-per-24h CLEARED; rolling-7d window back-edge 06-19T23:08 leaves only Oakville 06-21 + Burlington 06-25 = 2 in window so 3/7d CLEARED. **But the cap is moot because the candidate pool is exhausted:** seeded Candidates 1-5 all Phase-0 NO-GO/escalated (C1 dupes the live flagship guide, C2/C5 NULL Canadian volume, C3 gov-wall + guide-covered, **C4 cost = the only real-demand topic, gated behind Nathan's pricing decision → Step E, already on MORNING_TASKS**); the 4-city Gap-1 permit cluster is explicitly declared COMPLETE (Hamilton 10/mo deprioritized, hard "do not auto-draft a 5th without re-checking demand"); GSC self-feed empty (0 rows). So no eligible P-PIPELINE-AUTO topic. P-EXPAND / P-REFRESH N/A (no GSC data). P1 friend/Suroy-gated; P2 fabrication-gated (G5 hardened); P3 4-city permit mesh saturated; P4 healthy; P5 current. **Due-P0-CLEAN = the deliverable.** Nothing broken → no auto-fix; no `src/` touched → no build/deploy/CF-purge/IndexNow; no synthetic test (no quote form). No new MORNING_TASKS (GSC-verify L33 + Candidate-4 cost decision + Nathan launch TODOs all already on file). DfS $0.00, read-only (curl + Supabase REST + n8n GET + git + local files). **Cooldown anchor:** tarzan P0 next eligible ~T11:08 06-27; tarzan content run unblocked on velocity but pool-empty until GSC verification lands or Nathan decides Candidate 4. |
| 2026-06-26T05:08-04:00 | NO-SHIP — ACTIVE portfolio-min pick (tarzan) cascade independently re-verified DRY/BLOCKED on FRESH live data; adopted the orphaned T23:08 P0 changelog row (written 06-25T23:14, never committed) (doc-only, no build/deploy/CF-purge, $0 DfS) | **Site pick:** STEP 2 strict ACTIVE portfolio-min `[auto]`/24h = tarzan **2** (fewest) vs sudbury 3, fortmac 3 (FROZEN kamloops/belleville/arborist excluded). Tarzan's last full cascade walk was the T23:08 06-25 P0 (~6h, **outside** HARD-RULE-9's 4h window) → HR9-clean to re-walk and it is the genuine min-pick. **No ACTIVE P0 due** (tarzan last T23:08 → next ~T11:08; sudbury T01:08 → ~T13:08; fortmac T04:08 → ~T16:08) and **no FROZEN health check due** (kamloops T14:08 / arborist T17:08 / belleville T18:08 06-25, all next due this afternoon). **Live re-verification this hour (Supabase REST, DfS $0.00, not parroted):** `tarzantrimming.ca` = Content-Range `*/0` in BOTH `seo_query_daily_kpi` AND `seo_keyword_discovered` (still GSC-unverified, ~28-day domain) → P-EXPAND / P-REFRESH / P-PIPELINE-AUTO GSC-self-feed all N/A; GSC backbone FRESH (portfolio `seo_query_daily_kpi` MAX `2026-06-23`, 3d lag <4d). **Cascade DRY/BLOCKED:** P-PIPELINE human queue 0 `[x] approve`; **P-PIPELINE-AUTO velocity-capped — Burlington auto-published 06-25T22:22 is inside the rolling 24h → 1-per-24h HARD-capped until ~T22:08 06-26** (the 5 seeded candidates 1-5 remain `[ ] approve` but are blocked by the cap, and there is no GSC self-feed source); P1 friend/Suroy-gated launch TODOs (10 friend + 8 infra, none autonomous-doable); P2 service/area beyond pruning/limb-removal = fabrication-escalate (G5 hardened per `feedback_arborist_real_business_no_fabrication.md`); P3 link-graph saturated (4-city permit mesh + footer); P4 healthy; P5 current. Per anti-churn rules (`feedback_p0_no_recurring_synthetic_lead_test`) the fresh live re-verification IS the output; no `src/` rendered content manufactured to bump the commit counter → no build/deploy/CF-purge/IndexNow, no synthetic test (no quote form). Nothing broken → no auto-fix. No new MORNING_TASKS (GSC-verify + Nathan launch TODOs + AUTO-unblock ~06-26T22:08 all already on file). DfS $0.00, read-only (Supabase REST + git + local files). **Cooldown anchor:** tarzan NO-SHIP cascade-class T05:08 06-26; tarzan P0 next ~T11:08; tarzan content run next eligible ~T22:08 06-26 post-cap. |
| 2026-06-25T23:08-04:00 | P0 health audit DUE → ran CLEAN, read-only NO-SHIP (no commit/deploy/CF-purge, $0 DfS); validated the fresh T22:08 Burlington deploy is healthy sitewide | **Genuinely-due P0** (last full P0 T11:08 06-25, +12h=T23:08). **Site pick:** STEP 2 strict ACTIVE portfolio-min `[auto]`/24h = fortmac **1** (fewest) vs sudbury 2, tarzan 3 (FROZEN excluded). But fortmac's full cascade was independently re-verified DRY T20:08 (~3h, inside HARD-RULE-9's 4h window) with no new data since (GSC still MAX 06-23), and fortmac P0 not due (last T15:08, next ~T03:08) → re-walking fortmac repeats that task. Diverted to tarzan, whose P0 is genuinely DUE this hour AND which just deployed the Burlington post T22:08 (~1h) — a P0 here actually validates that fresh deploy rather than manufacturing a redundant NO-SHIP. **P0 CLEAN:** (a) all 13 live URLs Chrome-UA HTTP 200 with real distinct content 11.8KB–39KB (home 39075B, tree-pruning 24697B, Burlington post 32591B, contact 11887B — all well above the 5KB broken-state floor), unique correct titles; `/services/full-tree-removal/` → HTTP 404 is INTENTIONAL (`comingSoon:true` in services.json, `getStaticPaths` filters `!s.comingSoon`, zero internal links to it, verified). (c) theme-color #2D4A36 = brand, phone (647) 216-1874 present sitewide (home/tree-pruning/Burlington/contact all carry it). (d) GSC backbone: `[SEO] Daily GSC KPI` `uyBDgQqkyq0WNqOz` active=True; `seo_query_daily_kpi` portfolio MAX 2026-06-23 (2d lag, FRESH <4d); `rank_tracking_results` (gsc) MAX 2026-06-22T10:00Z (3d, FRESH <4d) — no false stale-flag. (e) cannibalization + tarzan-domain GSC freshness + qf-email/markFieldError N/A by design (tarzan still GSC-unverified, 0 `seo_*_kpi` rows on file MORNING_TASKS L33; no quote form). Cascade beyond P0 unchanged DRY/BLOCKED: P-PIPELINE-AUTO just shipped T22:08 (inside 4h cooldown + at the ≤3/7d cap); P-EXPAND/P-REFRESH GSC-N/A; P2 fabrication-gated; P3 link-graph saturated. **Due P0 + fresh-deploy validation = the deliverable.** Nothing broken → no auto-fix, no new MORNING_TASKS, no build/deploy/purge/IndexNow, no synthetic test. DfS $0.00, read-only (Supabase REST + n8n GET + curl + local files). Cooldown anchor: tarzan P0 next eligible ~T11:08 06-26; tarzan content run next eligible ~T02:08 06-26. |
| 2026-06-25T22:08-04:00 | **P-PIPELINE-AUTO SHIP** — 4th Gap-1 permit post auto-published: Burlington tree removal permit explainer (`/blog/tree-removal-permit-burlington/`), commit `5586cd3`, wrangler deploy `030eba71`, LIVE + verified | **Site pick:** ACTIVE `[auto]`/24h tie fortmac 1 = tarzan 1 (sudbury 2; FROZEN excluded); tie-break MOST Open Issues = fortmac, but fortmac was full-cascade NO-SHIP-walked T20:08 (~2h, inside HARD-RULE-9 4h window) → HR9-divert to tarzan (last full walk T01:08 ~21h, only P0 T11:08 since). **The velocity cap CLEARED this run:** the 3 prior P-PIPELINE-AUTO publishes are Toronto `8cbd5b3` 06-18T21:17:35, Mississauga `2cf6fdc` 06-19T22:28, Oakville `5c0db4d` 06-21T04:20; at 22:08 the rolling-7d back edge (06-18T22:08) is now PAST the Toronto publish → Toronto aged out → 2 in window → publishing the 4th = 3 = at the ≤3 cap (1-per-24h also clear, last publish 06-21). **Drafted the pre-validated Burlington brief (GO WITH MODIFIER, 50/mo, validated 06-24, $0 DfS this run) through the full protocol:** Phase 0 = pre-validation reused; **DRAFT-TIME VERIFY gate pulled live `burlington.ca` facts and CAUGHT a brief error — the pre-val brief assumed "By-law 02-2020 / 15 cm" but the real bylaw is By-law 040-2022: 20 cm DBH at 1.37 m, urban-boundary-only, $54+HST fee, replacement ratios 2:1/3:1/4:1.** The urban-boundary-only nuance became the post's unique angle vs the other 3. 3-phase draft, 1,365 body words, focus kw ×4 (H1 + first sentence + body + close), 7 internal links + reciprocal Burlington links added back into Toronto/Mississauga/Oakville (4-city cluster now a full mesh). Phase 4 `seo-on-page-optimizer` = **17 PASS / 1 WARN (shared 457KB hero, pre-existing across all 4 posts) / 0 FAIL** (meta-desc trimmed 163→136 to clear to PASS). **Verify:** `npm run build` clean 14pg; dist sweep em U+2014=0 / en U+2013=0 / banned-24-7=0; FAQPage+BlogPosting+BreadcrumbList schema present; per-post sitemap lastmod 2026-06-25; pushed GitHub `4d3bcae..5586cd3`; wrangler direct-upload `030eba71` (GitHub auto-deploy still not connected); content-match poll LIVE ~10s; **11+1-URL deploy-state sweep = 0 broken (all 200, all >5KB)**; CF zone cache purged (success). IndexNow N/A (tarzan has no IndexNow script/key, same as the 3 prior posts; sitemap lastmod drives discovery). blog-pipeline.md updated (Burlington marked drafted + moved to auto-approved; on-deck Hamilton held). New MORNING_TASKS: AUTO-published FYI + friend-verify Burlington fee/ratios + brief-drift note + optional IndexNow setup. DfS $0.00. **Cooldown anchors: Burlington 60-day topic cooldown from 06-25; tarzan content run next eligible +4h ~T02:08; tarzan P0 next ~T23:08.** |
| 2026-06-25T01:08-04:00 | NO-SHIP — full ACTIVE-portfolio cascade independently re-verified DRY/BLOCKED on fresh live data (doc-only, no build/deploy/CF-purge, $0 DfS) | **Site pick (honest HR9 divert):** STEP 2 strict portfolio-min `[auto]`/24h among ACTIVE-only = fortmac **3** (fewest) vs sudbury 4, tarzan 7 (kamloops/belleville/arborist FROZEN-excluded). But fortmac was just cascade-NO-SHIP-verified T20:08 + P0+cascade NO-SHIP T23:08 (~2h, **inside HARD RULE 9's 4h window**) with no new data since (GSC still MAX 06-22) → re-running fortmac's cascade NO-SHIP repeats that task. Sudbury was P0+cascade NO-SHIP T00:08 (~1h, also HR9-blocked). **Diverted to tarzantrimming, last task T19:08 P-PIPELINE pre-validation (~6h, HR9-clean) and the real-business growth site most likely to carry genuine work.** **Live re-verification this hour (Supabase REST + Python aggregation + git, DfS $0.00):** **(fortmac, GSC fresh MAX `2026-06-22`, 3d)** P-PIPELINE-AUTO self-feed 90d ≥15imp = `appliance repair fort mcmurray` 122/pos? + `fort appliance repair fort mcmurray` 59 + `fort mcmurray appliance repair` 40 + `fort appliance repair` 39 (all commercial geo/brand head terms **G2-excluded**, homepage-owned) + `fridge repair fort mcmurray` 19 (**G6-owned** by `/services/fridge-repair/`) → no eligible AUTO candidate; seeded queue = 5 topics all **validated NO-GO 06-18** (null CA volume, no rescuable umbrella, file says do-not-re-qualify); P-EXPAND pos8-20 imp≥20 = only `fort appliance repair` 41/pos18.2 (GSC-truncated competitor/homepage head-fragment; separate page = cannibalize, not buildable); P-REFRESH 60d imp≥10 avg_pos≥25 = `/services/microwave-repair/` 101/pos42.2 + `/services/stove-oven-repair/` 73/pos50.2 (per-appliance SERP-ceiling **DO-NOT-FIX** per `feedback_appliance_per_service_page_serp_ceiling.md`), `/contact/` 87/pos83.8 + `/areas/` 28/pos44.6 + home 21/pos26.7 + `/areas/gregoire/` 11/pos26.3 (utility/hub/authority-bound/thin-demand); P1 Suroy-gated; P2 corpus complete. **(tarzan)** P-PIPELINE human queue 0 `[x] approve`; **P-PIPELINE-AUTO velocity-capped — counted 3 P-PIPELINE-AUTO publishes in rolling 7d (Toronto 06-18T21:24 / Mississauga 06-19 / Oakville 06-21), frees ~06-25T21:24 when Toronto ages out; Burlington brief already pre-validated T19:08 awaits the non-capped run**; **P-EXPAND/P-REFRESH/self-feed re-queried Supabase REST this hour: `tarzantrimming.ca` = Content-Range `*/0` in both `seo_query_daily_kpi` and `seo_keyword_discovered` (GSC still unverified, ~21-day domain)**; P1 friend/Suroy-gated launch TODOs; P2 service/area beyond pruning/limb-removal = fabrication-escalate (G5 hardened per `feedback_arborist_real_business_no_fabrication.md`); P3 link-graph saturated (reciprocal permit + footer linking shipped through 06-22/06-24); P4 healthy; P5 current. **(sudbury)** independently confirmed content-dry at T00:08 (1h prior, within its own window). **Git-state sweep (orphaned-WIP check per `feedback_adopt_orphaned_wip_on_clean_p0.md`):** all 3 ACTIVE sites at origin/main 0/0. fortmac has an uncommitted HANDOFF.md row (its own T23:08 P0 entry, never committed) + 4 stale 06-10 untracked audit scripts — left for the next fortmac run to adopt (not touched here; committing fortmac now would itself be inside fortmac's 4h HR9 window). tarzan + sudbury clean. **Outcome:** entire ACTIVE portfolio confirmed on-page-saturated (shells) / velocity-capped + fabrication/friend-gated (tarzan) this cycle; the binding constraint remains off-page authority + GBP the cron cannot build (on file). Per anti-churn rules the fresh live re-verification IS the output; no rendered content manufactured to bump the commit counter. No `src/` touched → no build/deploy/CF-purge/IndexNow; no synthetic test (no quote form). No new MORNING_TASKS (GSC-verify + Nathan launch TODOs + AUTO-unblock ~06-25 + portfolio off-page reality all already on file). DfS $0.00. **Cooldown anchor: tarzan NO-SHIP cascade-class T01:08 06-25; tarzan P0 next eligible ~T22:08 (last T22:08 06-24).** |
| 2026-06-24T19:08-04:00 | P-PIPELINE pipeline pre-validation (doc-only, commit `e2367e2`): resolved the on-deck "Hamilton or Burlington" 4th Gap-1 permit post and recorded the validated next pick + full draft brief in `blog-pipeline.md` | **Tarzan = ACTIVE portfolio-min `[auto]`/24h (5; fortmac 6, sudbury 7; FROZEN excluded).** No ACTIVE P0 due (tarzan last T10:08 ~9h <12h; fortmac T09:08; sudbury T12:08); all 3 FROZEN health-checks already ran today (kamloops T13:08, arborist T17:08, belleville T18:08). **Cascade re-verified live, not parroted:** P-PIPELINE human queue 0 `[x] approve`; **P-PIPELINE-AUTO velocity-capped (Toronto 06-18 / Mississauga 06-19 / Oakville 06-21 = 3 in rolling 7d; frees ~06-25 when Toronto ages out)**; **P-EXPAND + P-REFRESH N/A, re-queried Supabase REST this hour: `tarzantrimming.ca` = Content-Range `*/0` in both `seo_query_daily_kpi` and `seo_keyword_discovered` (GSC still unverified)**; P1 friend/Suroy-gated; P2 fabrication-gated; P3 link graph saturated; mechanical audit clean (blog meta-desc 149/151/153, 404 present, Footer h3 no skip, 3 permit posts full mesh). **Deliverable = advance the content engine within the cap:** ran mandatory Phase 0 `seo-keyword-qualifier` on BOTH on-deck cities ahead of time. **Burlington `Burlington tree removal permit` = 50/mo LOW (idx 5), SERP City owns #1-2 + winnable small-arborist band #5/#7/#9/#11, no AIO, 1 Reddit #6 sub-threshold, competitor mcfarlantree etv 274/65kw, GO WITH MODIFIER** (same pattern as Mississauga/Oakville). **Hamilton only 10/mo, deprioritized** (the a-priori "bigger city wins" assumption falsified; Hamilton bylaw applies to a sub-area so searches fragment). Recorded both verdicts plus a verbatim draft brief (slug, internal-link mesh, draft-time `burlington.ca` bylaw verification) so the next non-capped run drafts immediately without re-spending DfS. Also fixed a stray pre-existing em-dash in the Mississauga pipeline entry. DfS spend ~$0.09 (well under $1 cap). Doc-only: not in `src/`, no build/deploy/CF-purge/IndexNow. No new MORNING_TASKS (GSC-verify + Nathan launch TODOs already on file; cap self-resolves). Cooldown anchor: tarzan P-PIPELINE pre-validation, next eligible content run ~06-25 post-cap. |
| 2026-06-24T15:08-04:00 | NO-SHIP, full ACTIVE-portfolio cascade independently re-verified DRY on fresh live data; ran one genuine fresh build + comprehensive dist quality sweep (clean); doc-only commit also adopts the orphaned T10:08 P0 changelog entry | **Tarzan = ACTIVE portfolio-min tie (fortmac/tarzan 5 `[auto]`/24h, sudbury 7), tie-break oldest last-touch (tarzan T06:08 ~9h vs fortmac T14:08 ~1h).** No ACTIVE P0 due (tarzan last T10:08, next ~T22:08; fortmac T09:08; sudbury T12:08) and no FROZEN health check due (kamloops ran T13:08; belleville/arborist not yet 24h). **Cascade independently re-verified, not parroted:** P-PIPELINE human queue = 0 `[x] approve`; P-PIPELINE-AUTO velocity-capped (counted 3 P-PIPELINE-AUTO publishes in rolling 7d: Toronto 06-18T21:24 / Mississauga 06-19 / Oakville 06-21; the 06-18 ages out ~06-25T21:24, so still 3/7d HARD-capped); **P-EXPAND + P-REFRESH N/A — re-queried Supabase REST live this hour: `tarzantrimming.ca` returns Content-Range */0 in BOTH `seo_query_daily_kpi` AND `seo_keyword_discovered` (GSC still unverified, MORNING_TASKS L33)**; P1 friend/Suroy-gated infra TODOs; P2 service/area pages fabrication-gated (G5 hardened + competitor-gaps "confirm coverage with Nathan"); P3 link-graph saturated (reciprocal permit links shipped T01:08, image-CLS clean T06:08); P4 healthy; P5 current. **Genuine fresh check (not rubber-stamp):** `npm run build` clean 13pg; `src/` em/en sweep 0; Python UTF-8 dist sweep over all 13 HTML — em-dash U+2014 = 0, en-dash U+2013 = 0, banned 24/7/always-available = 0; meta-description lengths all healthy (140–153 chars, none >160, 404 intentionally 107). No defect found → no fix to ship; per anti-churn rules manufactured NO busywork edit. Read-only, $0 DfS, no build artifact committed, no deploy/CF-purge/IndexNow (no `src/` touched). No new MORNING_TASKS (GSC-verify L33 + off-page-unlock L20 already on file; AUTO unblocks ~06-25 naturally). Cooldown anchor: tarzan NO-SHIP cascade-class T15:08 06-24; P0 next eligible ~T22:08. |
| 2026-06-24T10:08-04:00 | P0 health audit DUE → ran CLEAN, read-only NO-SHIP (no commit/deploy/CF-purge, $0 DfS) | **Genuinely-due P0 (last full P0 T22:08 06-23, +12h=T10:08).** All 12 live content URLs Chrome-UA HTTP 200, healthy bytes 11.8KB–39KB, unique correct titles; /nope control → real 404. Homepage: theme-color #2D4A36 = brand, favicon.svg present, phone (647) 216-1874 + tel:+16472161874 = site.json, hero niche/city markers present, canonical = tarzantrimming.ca. Sitewide dist UTF-8 sweep: em=0 / en=0 / banned-24-7=0. GSC backbone (global) confirmed active+fresh 1h prior by fortmac T09:08 P0 (uyBDgQqkyq0WNqOz active, KPI MAX 06-21, rank-gsc MAX 06-22); tarzan-domain GSC freshness N/A (still GSC-unverified, 0 seo_*_kpi rows, on file in MORNING_TASKS); cannibalization + qf-email/markFieldError checks N/A by design (no GSC data, no quote form). Cascade beyond P0 unchanged DRY/BLOCKED (P-PIPELINE-AUTO velocity-capped to ~06-25; P-EXPAND/P-REFRESH GSC-N/A; P2 fabrication-gated; P3 link-graph saturated). DUE P0 = the deliverable. No new MORNING_TASKS. P0 next eligible ~T22:08 06-24. |
| 2026-06-24T06:08-04:00 | NO-SHIP, fully-cooled portfolio; ran one fresh image-CLS audit (clean) | **Tarzan = ACTIVE portfolio-min tie (fortmac/tarzan 5 `[auto]`/24h, sudbury 6), tie-break oldest last-touch.** Nothing HR9-clean + actionable: all 3 ACTIVE cascades NO-SHIP'd within last 3h (tarzan T03:08/fortmac T04:08/sudbury T05:08); no ACTIVE P0 due (tarzan next ~T10:08); all FROZEN health checks inside 24h cooldown. AUTO velocity-capped (3 GTA permit posts rolling 7d, unblocks ~06-25); P-EXPAND/P-REFRESH N/A (GSC unverified, 0 `seo_*_kpi` rows, MORNING_TASKS L33); P1 friend/Suroy-gated; P2 fabrication-gated. **Genuine fresh check (not rubber-stamp):** image-CLS audit: all 5 `<img>` (Hero/MiniAbout/PhotoGallery/blog hero/prune-guide accent) have `alt` + explicit `width`/`height`, CLEAN, no fix. No churn shipped (anti-churn rule 3). $0 DfS, read-only, no build/deploy/CF-purge. Cooldown anchor: tarzan NO-SHIP cascade-class T06:08 06-24; P0 next ~T10:08. |
| 2026-06-24T03:08-04:00 | Full ACTIVE-portfolio cascade independently re-verified DRY/BLOCKED → NO-SHIP (doc-only, $0 DfS, no build/deploy/CF-purge) | **Tarzan was the portfolio-min pick (4 `[auto]`/24h vs fortmac 5, sudbury 6; kamloops/belleville/arborist FROZEN-excluded).** Cascade: P0 not due (T22:08 06-23 CLEAN, next ~T10:08); P-PIPELINE human queue = 0 `[x] approve`; P-PIPELINE-AUTO velocity-capped (3 GTA permit posts rolling 7d, 06-18 Toronto ages out ~06-25); **P-EXPAND/P-REFRESH live-confirmed N/A — `tarzantrimming.ca` has 0 rows in BOTH `seo_query_daily_kpi` and `seo_keyword_discovered` (GSC still unverified, MORNING_TASKS L33)**; P1 friend/Suroy-gated; P2 area/service pages fabrication-gated (competitor-gaps Gap 3 "confirm coverage with Nathan" + G5 hardened gate); P3 HR9-blocked (just ran T01:08); P4 verified healthy (`site.json` contact CTAs live since 06-13/06-14, 404 + sitemap.xml + PhoneFloat all present); P5 docs current. Shells re-verified dry (sudbury P-EXPAND `[]`; fortmac only the unbuildable `fort appliance repair` head-fragment pos18.2; both GSC fresh 06-21). No content shipped (anti-churn: a 4th near-identical permit post = thin-content risk on a <30-day domain; re-enriching maxed pages is the warned-against churn). Logged to ROUTINE_LOG + added 1 new cron note re: the ~06-25 AUTO unblock should diversify / weigh GSC-verification + Nathan unlocks, NOT mechanically stack a 4th GTA permit post. Cooldown anchor: tarzan NO-SHIP cascade-class T03:08 06-24; P0 next eligible ~T10:08. |
| 2026-06-24T01:08-04:00 | Reciprocal permit-cluster links: both service pages now link the 3 city permit guides (cron P3, commit `0259420`, wrangler deploy `c2b28e48`) | **P3 internal-link deliverable — closes the reverse edge of the permit content cluster.** Site pick: ACTIVE-only portfolio-min `[auto]`/24h = **tarzantrimming 2** (sudbury 6, fortmac 4; kamloops/belleville/arborist FROZEN-excluded), HR9-clean (last tarzan task T09:08 06-23 P0, ~16h, different task-class). **Cascade:** P0 eligible (>12h) but recent ones CLEAN NO-SHIP, deprioritized for a real ship; P-PIPELINE human queue = **0 `[x] approve`**; P-PIPELINE-AUTO **velocity-capped** (3 auto-posts in rolling 7d window 06-17→06-24 = Toronto 06-18 / Mississauga 06-19 / Oakville 06-21, all live; unblocks ~06-25 when the 06-18 post ages out of the window); P-EXPAND/P-REFRESH **N/A** (domain still GSC-unverified at ~24 days, no `seo_*_daily_kpi` rows); P1 launch/infra TODOs friend/Suroy-gated; P2 new service/area pages = **fabrication-escalate** (Gap 3 area pages explicitly "confirm coverage with Nathan first", G5 hardened gate per `feedback_arborist_real_business_no_fabrication.md`) → **P3.** **Real gap found:** the 3 permit blog posts link INTO `/services/tree-pruning/` + `/services/limb-removal/`, but the service pages only linked the pruning-timing `/guide/`, never back out to the permit posts — the permit cluster linked one way only. **Fix (1 file, `src/pages/services/[slug].astro`):** added a `permitGuides` frontmatter array (Toronto/Mississauga/Oakville → their `/blog/tree-removal-permit-*/` URLs) + a new "Removing a protected tree? Check the permit rules first" section (between "Where we work" and "What it costs", renders on BOTH real service pages; comingSoon full-removal excluded by getStaticPaths). **Honesty:** copy states most corridor cities require a permit + arborist report to REMOVE a protected tree, explicitly says "We focus on pruning and limb removal rather than full removals," routes removal-intent visitors to the bylaw explainers without claiming Tarzan does removals — zero capability/credential/pricing claim. **Verify:** `npm run build` clean 13pg; dist both service pages render the heading + all 3 permit links (`tree-removal-permit-{toronto,mississauga,oakville}`); rendered prose reads "guides for Toronto, Mississauga, and Oakville."; sitewide dist sweep em U+2014 = 0 / en U+2013 = 0 / banned-24-7 = 0. Pushed `db8c439..0259420` 1st attempt; wrangler direct-upload deploy `c2b28e48` (GitHub auto-deploy still not connected); apex content-match poll attempt 1 (needle "Removing a protected tree"); 12-URL deploy-state sweep all HTTP 200 healthy bytes (11.8–39KB) BAD=0 + /nope → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only change to existing URLs, no new pages). No synthetic test (no quote form). No sitemap lastmod bump (navigational/in-content link addition, not a content rewrite of the service copy; avoids inflated-freshness across the 2 pages). Skills not invoked (mechanical reciprocal-link fix, no single-keyword GSC target on the unverified 24-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged; AUTO-unblock ~06-25). |
| 2026-06-23T15:08-04:00 | Full ACTIVE-portfolio cascade independently re-verified DRY/BLOCKED → NO-SHIP (doc-only, no build/deploy/CF-purge, $0 DfS) | **Site pick (honest divert):** STEP 2 strict portfolio-min `[auto]`/24h among ACTIVE-only = fortmac **2** (fewest) vs tarzan 5, sudbury 6 (kamloops/belleville/arborist FROZEN-excluded). But fortmac's only available task-class (cascade NO-SHIP) was done T12:08 (3h, **inside HARD RULE 9's 4h window**) and its P0 is not due (last T08:58, ~6h <12h) → fortmac has no HR9-clean deliverable. Sudbury's cascade NO-SHIP was T14:08 (1h, also HR9-blocked) + P0 not due (T10:08). **Diverted to tarzantrimming, last task T09:08 P0 (~6h, different task-class, HR9-clean) and the real-business growth site most likely to carry genuine work.** **Live re-verification (Supabase REST + Python aggregation + git + local files, DfS $0.00):** **(fortmac, GSC fresh MAX `2026-06-20`, 3d)** P-PIPELINE-AUTO self-feed 90d ≥15 imp = `appliance repair fort mcmurray` 112/pos13.1, `fort appliance repair fort mcmurray` 55, `fort appliance repair` 39, `fort mcmurray appliance repair` 37 (all commercial geo head terms **G2-excluded**, homepage-owned) + `fridge repair fort mcmurray` 19 (**G6-owned** by `/services/fridge-repair/`) → no eligible AUTO candidate; P-EXPAND pos8-20 imp≥20 = only `fort appliance repair` 41/pos18.2 (malformed head-fragment the homepage owns; separate page = cannibalize, not buildable); P-REFRESH 60d imp≥10 avg_pos≥25 = `/services/microwave-repair/` 98/pos43.9 + `/services/stove-oven-repair/` 73/pos50.2 (per-appliance SERP-ceiling **DO-NOT-FIX** per `feedback_appliance_per_service_page_serp_ceiling.md`), `/contact/` 84/pos86.7 + `/areas/` + homepage (utility/authority-bound), `/areas/gregoire/` 10/pos28.1 (already dispositioned SERP-complete + authority-bound T22:08 06-22, do not re-investigate). **(tarzan)** P-PIPELINE human queue = **0 `[x] approve`**; P-PIPELINE-AUTO **velocity-capped** (3 auto-posts in rolling 7d = `/blog/tree-removal-permit-{toronto 06-18, mississauga 06-19, oakville 06-21}/`, all confirmed live in blog.json; unblocks ~06-25 when 06-18 ages out); P-EXPAND/P-REFRESH **N/A** (domain not GSC-verified, no `seo_*_daily_kpi` rows); P1 launch/infra TODOs all friend/Suroy-gated; P2 new service page beyond tree-pruning/limb-removal = **fabrication-escalate** (G5 hardened gate per `feedback_arborist_real_business_no_fabrication.md`), not autonomously buildable; P3/P4 link graph + "Helpful guides" footer + per-page sitemap lastmod + favicon all closed by 06-22/06-23 runs; P5 competitor-gaps.md exists + actively feeding the (capped) AUTO queue, seeded `[ ]` queue has runway. **(sudbury)** independently confirmed content-dry T14:08 (1h prior). **Outcome:** entire ACTIVE portfolio confirmed on-page-saturated (shells) / velocity-capped + fabrication-gated (tarzan) this cycle. Per MORNING_TASKS L13-20 (2026-06-14 directive) GBP/citations is NOT a valid pre-tenant Suroy lever for the shells, organic long-tail content is the whole lever, which the (this-cycle-capped/dry) AUTO engine targets. Deliberately shipped no rendered content, manufacturing churn to bump the commit counter is explicitly warned against (AUTONOMY anti-churn + `feedback_p0_no_recurring_synthetic_lead_test.md`); the live re-verification IS the output. No site-content commit, no deploy, no CF purge, no synthetic test (no quote-form file touched). No new MORNING_TASKS, saturation + off-page reality (L13-20) + tarzan friend/Suroy blockers + AUTO-unblock ~06-25 all already on file. **Cooldown anchor: tarzantrimming NO-SHIP cascade-class T15:08 06-23; tarzan P0 next eligible ~T21:08 06-23.** |
| 2026-06-22 | Footer "Helpful guides" column (cron P3/P4, commit `a29f20b`, wrangler deploy `73369a2c`) | Footer previously had brand + contact + service-area columns but ZERO links to any content/money page. Added a 4th column linking the 4 cornerstone guides (pruning-timing guide + Toronto/Mississauga/Oakville permit explainers) — durable sitewide internal links into the indexed blog cluster from every page, distributing authority from the highest-authority pages. Navigational chrome change, so sitemap PAGE_LASTMOD intentionally NOT bumped (avoids inflated-freshness across all 13 pages for a non-content edit). Grid `md:grid-cols-3` to `sm:grid-cols-2 lg:grid-cols-4`; new heading `<h3>` per footer-heading convention. Build clean 13pg; footer renders sitewide (home/about/blog verified); 4 targets all live 200; 11-URL deploy-state sweep 11/11 200 [11.8-39KB] BAD=0; em/en/24-7 sweep 0; zone purged. |
| 2026-05-29 | Initial scaffold | Cloned vicjimassage architecture, stripped bilingual + blog scope, rebranded palette (forest/bark/sky/moss/cream), wrote new English-only components, built 4 pages (home, services, about, contact), 88K dist, zero em/en dashes. Awaiting friend's input on the 9 TODOs above before launch. |
| 2026-05-29 | Removed ReviewsWall + reviews.json | Friend has no real customer reviews yet. Section dropped from homepage rather than left dormant. Will re-add only when there are at least 3 real reviews to show. |
| 2026-05-29 | Soften day-job copy, no named employer | Suroy could not verify if friend works at Davey Tree or that was second-hand info. Customer-visible copy now says "commercial tree crews" generically. Easy to swap to a named employer once confirmed. |
| 2026-05-29 | Hide all contact CTAs | Phone fields in site.json blanked; Header/Hero/FinalCTA/Footer/PhoneFloat/contact.astro all conditional-render based on presence. Once friend confirms phone + email, populate src/data/site.json contact block and the whole site lights back up. |
| 2026-05-29 | First deploy to Cloudflare Pages | Project `tarzantrimming` created via API, dist/ direct-uploaded via wrangler. Apex + www CNAME records created and attached to Pages project. Repo is on GitHub but NOT yet wired to auto-deploy. Live at https://tarzantrimming.pages.dev. Custom domain SSL provisioning. |
| 2026-06-04 | Competitor name conflict noted + proactive partnership plan documented | Discovered Tarzan Tree Removal (Barrie/Simcoe County, ON, Mark Mederak, 5+ years operating, Landscape Ontario Young Entrepreneur 2019, Barrie Advance Readers Choice 2020-2022, ISA Certified Arborist). Friend Nathan reviewed the conflict and chose to proceed with Tarzan Trimming. Strategy: proactive partnership outreach BEFORE any cease-and-desist letter, framed as new-entrant introduction with referral exchange offer (we focus pruning + limb removal west-GTA / Kitchener corridor; they focus full removal + Barrie/Simcoe). Outreach deferred until tarzantrimming.ca has 60-90 days of authority + a few demonstrated leads to back the partnership credibility. Risk profile (with proactive outreach folded in): cease-and-desist ~10%, forced rebrand ~3-5%, real referral partnership ~25-35%. Sudbury, Fort Mac, etc rank-and-rent sites' lead-handoff playbook (e.g. Moira / Valley / Fort Repairs) provides the operational template. |
| 2026-06-04 | nathan@tarzantrimming.ca via Migadu planned, awaiting DNS records from Suroy's Migadu dashboard | Friend's email will be `nathan@tarzantrimming.ca` (not `info@` -- explicitly requested). Plan: Migadu mailbox (uses existing Micro plan, tarzantrimming would be 7th domain), DNS records added to Cloudflare zone, Nathan hooks up via Gmail Settings -> "Check mail from other accounts" (IMAP) + "Send mail as" (SMTP) so he never leaves his Gmail inbox. Blocked on Suroy adding the domain in Migadu dashboard and pasting the 3 DNS records (MX + SPF TXT + DKIM TXT) for the Cloudflare API to set up. Cloudflare Email Routing alternative considered and rejected because Nathan's replies would come from his personal Gmail address rather than nathan@tarzantrimming.ca, breaking the brand. |
| 2026-06-09 | SEO launch foundation: canonical URL fix + sitemap + robots.txt (cron, commit `5d4ae95`) | First autonomous-routine deliverable after tarzantrimming entered cron scope. (1) **Critical fix:** `astro.config.mjs` `site` was still the vicjimassage clone value `https://vicjimassage.com.br`, so every page emitted `<link rel="canonical" href="https://vicjimassage.com.br/...">` and the homepage JSON-LD `url` pointed at the Brazilian massage site. Changed to `https://tarzantrimming.ca`. Verified dist: all 4 canonicals + JSON-LD url now correct, zero vicjimassage refs remain. (2) Added custom `src/pages/sitemap.xml.ts` (4 static pages, fixed honest lastmod) + `public/robots.txt` with Sitemap directive. (3) Shipped via wrangler direct-upload (deploy `b3e3e075`, GitHub auto-deploy still not connected), apex poll OK attempt 1, 6-URL deploy-state sweep 6/6 HTTP 200 BAD=0, CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged. **Unblocks** infra TODO #6/#7 (GSC verification + sitemap submission now possible). Note: CF Managed robots.txt prepends AI-bot blocks above our content, but the `Sitemap:` line is present (Google reads it anywhere in the file). |
| 2026-06-09 | Dedicated service detail pages: tree-pruning + limb-removal (cron, commit `7b80ac9`) | **P2 structural-gap deliverable.** The two launch services had rich, friend-approved copy in `services.json` but only rendered inside the single combined `/services/` menu, so neither had a dedicated indexable URL to rank. Added `src/pages/services/[slug].astro` (getStaticPaths over non-`comingSoon` services) producing `/services/tree-pruning/` and `/services/limb-removal/`. Each page: focus-keyword H1, 143/145-char meta description, reused description/process/goodFor copy, "Where we work" + "What it costs" + "Related service" + closing sections, sticky CTA aside, 5 unique internal-link targets (home, /services/, /about/, /contact/, sibling service), and `Service` + `BreadcrumbList` JSON-LD. Linked the `/services/` menu cards to the new pages ("Full details" + name link) for crawl. Added both URLs to `sitemap.xml.ts` PAGES (6 pages now). Build clean 6pg, dist sweep em=0/en=0/24-7=0, meta lengths 143/145 in range. Shipped via wrangler direct-upload (deploy `640dcb7e`, GitHub auto-deploy still not connected), apex content-match poll attempt 1 (~10s), 6-URL deploy-state sweep 6/6 HTTP 200 all ≥5KB BAD=0, CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged. No IndexNow (vicjimassage-derived site has no indexnow script, by design). Site is 11 days old + not yet GSC-verified, so no GSC demand data drove keyword choice; structural-gap rule applied. |
| 2026-06-09 | P0 health audit + head-meta completeness fix (cron, commit `58727fe`) | First P0 audit on tarzantrimming since entering cron scope. **Audit result: clean** — all 4 live pages 200, correct titles, canonical fix from `5d4ae95` confirmed live (every canonical = `tarzantrimming.ca`, 0 vicjimassage leftovers), 0 em/en dashes, 0 banned 24/7 claims, sitemap + robots live. **One gap found + fixed:** `theme-color` meta was absent site-wide (not in src or dist), and `og:url` was missing from the OG block. Added both to `src/layouts/BaseLayout.astro` head: `<meta name="theme-color" content="#2D4A36">` (brand forest green) + `<meta property="og:url" content={canonical}>`. Build clean 4pg; dist sweep theme-color 4/4 + og:url 4/4 (correct per-page URLs) + em=0/en=0/24-7=0. Shipped via wrangler direct-upload (deploy `0835bcbe`, GitHub auto-deploy still not connected), apex content-match poll 1 (~10s), 4-URL deploy-state sweep 4/4 HTTP 200 ≥4KB theme-color present BAD=0, CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged. P0 GSC pipeline/cannibalization sub-checks intentionally skipped: domain is 11 days old, not yet GSC-verified, so NULL `seo_*_daily_kpi` = no-data-yet (expected), not a stale-pipeline failure — no false flag raised. No quote form exists (phone/text funnel) so qf-email/markFieldError P0 checks are N/A by design. |
| 2026-06-09 | Custom 404 page (cron, commit `f4ea7f4`, wrangler deploy `34390b03`) | **P4/UX deliverable.** No 404 page existed, so any unmatched URL returned the homepage at HTTP 200 (CF Pages soft-404 fallback per `feedback_cloudflare_pages_unknown_url_serves_home.md`) — bad for SEO (soft 404s) and gives lost visitors no recovery path. Added `src/pages/404.astro` (BaseLayout, brand-coherent: cream bg, forest H1 "This branch leads nowhere.", 6 recovery cards linking home + both service detail pages + services menu + about + contact, "Back to home" button). Astro built `dist/404.html` (7.2KB); CF Pages now serves it with a proper 404 status for unmatched routes. Build clean 7pg; dist sweep em=0/en=0/24-7=0; all 6 links resolve to real built pages. Shipped via wrangler direct-upload (GitHub auto-deploy still not connected). Also folded in the T04:14 P2 run's uncommitted HANDOFF changelog edits (the `7b80ac9` service-detail-pages doc rows that prior run left dirty in the working tree). |
| 2026-06-09 | Homepage FAQPage schema + fix site-wide broken og:image (cron, commit `0b994de`, wrangler deploy `d4576a2d`) | **P3 structured-data + meta-completeness deliverable.** Two real gaps found while auditing head/schema: (1) the homepage renders 8 substantive FAQ Q&As (`faq.json` via `FAQ.astro`) but emitted only `LocalBusiness` JSON-LD, so no `FAQPage` rich-result eligibility. Added a second JSON-LD block in `index.astro` that maps `faqData.items` → `FAQPage`/`Question`/`acceptedAnswer` (schema matches visible accordion content, Google-compliant). (2) `BaseLayout.astro` `og:image` pointed at `/photos/hero.jpg`, but `public/photos/` did not exist, so every page's OG image 404'd. Generated a brand-coherent `public/photos/og-default.png` (1200x630, forest-green card, brand name + tagline + 7 cities, Arial via PIL) and repointed `ogImage` to it; also added `og:image:width`/`height` + `twitter:image` for completeness. Build clean 7pg; dist JSON-LD parse 2/2 blocks valid (`LocalBusiness`+`FAQPage` with 8 questions), og:image+twitter:image resolve to `og-default.png` (present in `dist/photos/`, 40KB), em=0/en=0/24-7=0. Shipped via wrangler direct-upload (GitHub auto-deploy still not connected). When the friend sends a real hero photo, replace `og-default.png` or repoint `ogImage`. |
| 2026-06-09 | Service Areas hub page (cron, commit `606da92`, wrangler deploy `1e59f0aa`) | **P2 structural-gap deliverable.** Site served 7 cities but had zero local-targeting content beyond the homepage `areaServed` JSON-LD, and no area page existed. Built `src/pages/service-areas.astro`: H1 "Tree Pruning and Limb Removal Across South Ontario", 153-char meta naming all 7 cities, one H2 + honest blurb per city (Toronto/Mississauga/Oakville/Burlington/Hamilton/Kitchener/Whitby) grounded in real tree species + terrain (silver/Norway maples on tight Toronto ravine lots, Lorne Park maturity in Mississauga, Niagara Escarpment edge at Burlington, Carolinian zone at Kitchener, etc.), plus "Not on the list?" + "How the work goes" sections. `Service` JSON-LD (serviceType "Tree Care", 7-city `areaServed`, LocalBusiness provider) + `BreadcrumbList`. Wired into Header nav (site-wide via BaseLayout, desktop + mobile) and `sitemap.xml.ts` PAGES (7 pages now). **Honesty discipline:** deliberately ONE editable hub, not 7 thin per-city doorway pages, because the city list is still friend-unconfirmed (launch TODO #2) and doorway pages are SEO-risky for a new owner-led domain. The Oakville blurb references its stricter private-tree bylaw in general terms only (no specific size/permit threshold asserted) and carries an inline `<!-- FRIEND-VERIFY -->` comment; flagged to MORNING_TASKS for owner confirmation. Build clean 8pg; dist em=0/en=0/24-7=0; both JSON-LD blocks parse (Service 7-city + Breadcrumb); pushed `0d1663a..606da92` first attempt; wrangler direct-upload deploy `1e59f0aa`; apex content-match poll 2 (~20s); 9-route deploy-state sweep all 200 + healthy bytes BAD=0, /nope route correctly 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success. No IndexNow (site has no indexnow script by design). Skills not invoked (structural-gap page build, no single-keyword GSC target on an unverified 12-day-old domain). |
| 2026-06-04 | GitHub + Cloudflare Pages deploy live with hidden contact info | Repo `Notsuroy/tarzantrimming-site` created and pushed (5 commits as of session end). Cloudflare Pages project `tarzantrimming` connected via direct upload (not yet linked to GitHub for auto-deploy -- user can flip that on later via dashboard). Custom domains `tarzantrimming.ca` + `www.tarzantrimming.ca` attached, CF auto-created the CNAME records, SSL provisioned. All contact CTAs (Header phone chip, Hero buttons, FinalCTA, PhoneFloat, Footer column, contact page) conditional-render based on `site.contact.phoneE164`/`email` being non-empty; right now they're blank so the site shows "Launching soon" framing instead of bad/wrong contact info. Once Suroy populates `src/data/site.json` contact block, every CTA lights up. Davey Tree reference softened from explicit "trained at Davey" to generic "commercial tree crews" since Suroy couldn't confirm whether Nathan actually works at Davey or it was friend-of-friend info. ReviewsWall component + reviews.json deleted entirely (rather than left dormant) since Nathan has no real customer reviews yet. |
| 2026-06-10 | P3 meta-description audit: unique in-range descriptions for 4 pages (cron, commit `f322d3f`, wrangler deploy `8ce14103`) | **P3 SEO deliverable.** Audit found 4 pages (`/`, `/services/`, `/about/`, `/contact/`) all emitting the SAME 185-char `site.seo.defaultDescription` (duplicate + over the ~155-char Google truncation limit) because none passed a `description` prop to BaseLayout, falling back to the default. The 3 newer pages (tree-pruning 143, limb-removal 145, service-areas 153) already had unique in-range descriptions. Added a unique, page-specific `description` prop (140-142 chars each) to the 4 stale pages: home (brand + free-estimate CTA), services (two-services framing), about (owner-led arborist story), contact (get-in-touch + free quote). og:description follows automatically (BaseLayout uses the same `desc` var). Build clean 8pg; dist meta-desc 4/4 unique 140-142 + service/area pages unchanged (143/145/153); dist sweep em=0/en=0/24-7=0. Pushed `5411131..f322d3f` first attempt; wrangler direct-upload deploy `8ce14103`; apex content-match poll attempt 1 (~35s); 7-page deploy-state sweep all 200 + healthy bytes BAD=0, sitemap 200, /nope route correctly 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success. No IndexNow (site has no indexnow script by design; meta-only change, no new URLs). P0 health audit run first this cascade = CLEAN (7/7 pages 200 unique titles, 404 works, theme-color/og/favicon present, H1 niche+region, 0 em/en/24-7); GSC/cannibalization sub-checks skipped (domain ~12 days old, not GSC-verified, no-data expected). |
| 2026-06-10 | Breadcrumb schema parity on /about/, /contact/, /services/ (cron P3, commit `58a32cd`, wrangler deploy `24e7ba5a`) | **P3 structured-data deliverable.** Audit found the three 1-level pages (`/about/`, `/contact/`, the `/services/` menu) emitted NO JSON-LD and NO breadcrumb, while the deep pages (`/services/[slug]/`, `/service-areas/`) already had both a visible breadcrumb `<nav aria-label="Breadcrumb">` and matching `BreadcrumbList` JSON-LD. Ported the exact deep-page pattern to all three: a visible `Home / <Page>` breadcrumb nav at the top of each first section + a 2-item `BreadcrumbList` script (`new URL('/', Astro.site)` absolute item URLs) before `</BaseLayout>`. Now every non-home page on the site carries breadcrumb structured data + a visible trail (home keeps LocalBusiness+FAQPage). Build clean 8pg; dist JSON-LD parse 3/3 BreadcrumbList valid (about→[Home,About], contact→[Home,Contact], services→[Home,Services]) + visible nav present 3/3; sitewide sweep em=0/en=0/24-7=0. Pushed `bb3ca0f..58a32cd` first attempt; wrangler direct-upload deploy `24e7ba5a`; apex content-match poll attempt 1 (~10s, `aria-label="Breadcrumb"` live on `/about/`); 8-route deploy-state sweep all 200 + healthy bytes BAD=0, /nope route correctly 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success. No IndexNow (site has no indexnow script by design; no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical template-schema parity fix, no single-keyword GSC target on the unverified 12-day-old domain). |
| 2026-06-10 | Created `blog-pipeline.md` (cron P5, commit pending) | **P5 content-pipeline seed + drift fix (doc-only, no deploy).** Cascade for this run dead-ended: P0/P3 on cooldown (both ran 09:11 same day), P-EXPAND/P-REFRESH need GSC data (12-day unverified domain has none), P1 all friend/Suroy-blocked, P2 no structural gap (services intentionally limited to the 2 offered, areas are one editable hub by design). Real finding: MORNING_TASKS L19 (2026-06-09 T20:22) claimed "all 4 rank-and-rent sites + tarzantrimming have [blog-pipeline] files now" but `tarzantrimming-site/blog-pipeline.md` did NOT exist on disk. Created it, matching the kamloops/fortmac pipeline format: 5 candidate posts (1 when-to-prune, 2 dangerous-limb signs, 3 oak-wilt summer-pruning timing, 4 pruning cost factors, 5 storm-limb cleanup), all angled informational/seasonal/cost (not commercial head terms) and mapped to the 2 offered services. All `[ ] approve` (awaiting Nathan, owner-led honesty discipline). Each carries a Friend-verifies line (seasonal availability, hydro-line boundary, oak-wilt practice, quote-only pricing language, no-emergency-response framing). File documents that the blog SYSTEM still does not exist (`src/pages/blog/` absent), so the first approved topic triggers a 2-run P-PIPELINE sequence (infra build, then draft). 0 em/en dashes verified. No `npm run build` / deploy / cache purge (repo doc, not under src or public). Flagged to MORNING_TASKS: drift correction + Nathan should review/approve topics + per-topic friend-verify items. Also folded in the prior 09:11 run's uncommitted HANDOFF meta-description changelog row. |
| 2026-06-10 | Fix `/about/` heading-hierarchy skip (cron P3, commit `ae58bd7`, wrangler deploy `0d89ef6a`) | **P3 accessibility/SEO deliverable.** Heading-sequence audit across all 7 content pages found exactly one violation: `/about/` emitted `h1 -> h3 -> h3 -> h2(FinalCTA) -> h3 -> h3`, an H1->H3 skip because the two info cards ("What we will tell you upfront" / "What we will not do") used `<h3>` directly under the H1 with no intervening H2. Promoted both card headings from `<h3>` to `<h2>` (styling is class-based `text-xl`, so zero visual change). Sequence now `h1 h2 h2 h2 h3 h3`, no skips. All other pages (home/contact/services/service-areas/both service detail) already had clean hierarchies and no `<h4>` anywhere (no Footer-h4 defect on this vicjimassage-derived template, unlike the rank-and-rent Astro template). Build clean 8pg; dist sweep em=0/en=0/24-7=0. Pushed `0d0cbef..ae58bd7` first attempt; wrangler direct-upload deploy `0d89ef6a`; apex content-match poll attempt 1 (~10s, `What we will tell you upfront</h2>` live on `/about/`); 8-route deploy-state sweep all 200 + healthy bytes BAD=0, /nope route correctly 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success. No IndexNow (site has no indexnow script by design; no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical heading-tag fix, no single-keyword GSC target on the unverified 12-day-old domain). P0 not run this cascade (last P0 09:11 same day, inside 12h cooldown). |
| 2026-06-13 | Service-page FAQ accordion + FAQPage schema on both service pages (cron P3, commit `82d95ac`, wrangler deploy `0945e9f6`) | **P3 structured-data + content parity deliverable.** P0 on 12h cooldown (last 03:15 same day, skipped). Cascade below: P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (15-day domain, no GSC data); P1 friend/Suroy-gated; P2 no structural gap. **Real gap found:** both service detail pages (`/services/tree-pruning/`, `/services/limb-removal/`) carried only `Service` + `BreadcrumbList` JSON-LD with no FAQ content or `FAQPage` schema, the same service-page parity gap closed earlier on the Sudbury/Fort Mac rank-and-rent service pages. Added a slug-keyed `faqMap` in `[slug].astro` driving BOTH a visible `<details>` accordion (matching the homepage `FAQ.astro` styling) AND `FAQPage` JSON-LD from one array (guaranteed schema/visible parity). 5 honest, owner-led FAQs per service: pruning = best season + oak-wilt timing, cost (quote-only), does-pruning-hurt-tree, how-much-canopy-at-once, cleanup; limb-removal = limb-vs-full-removal, turnaround (mirrors services.json "same-day or next-evening", no 24/7), over-roof/driveway rigging, near-power-lines (defers to hydro, honesty), cost. No certs claimed, no 24/7, pricing stays quote-only. Since both pages share the route file, one edit brings both to parity. **Verify:** `npm run build` clean 8pg; dist sweep em=0/en=0/24-7=0 sitewide; FAQPage parse 5 Qs each page + 0 missing-visible-answers (full schema/visible parity). Pushed `f6cfd25..82d95ac` first attempt; wrangler direct-upload deploy `0945e9f6`; apex content-match poll attempt 1 (~10s, needle "When is the best time to prune trees"); 8-route deploy-state sweep all HTTP 200 healthy bytes BAD=0 (sitemap.xml 842B is a small XML file, false-positive on byte threshold) + /nope 404; service pages grew ~13KB→~18KB with FAQ content; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true; desktop+mobile screenshot clean render. No IndexNow (site has no indexnow script by design; content-only, no new URLs). No synthetic test (no quote form). Skills not invoked (structured-data/content parity port mirroring the proven sister-site service-page pattern, no single-keyword GSC target on the unverified 15-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged). |
| 2026-06-13 | Enrich homepage LocalBusiness JSON-LD (cron P3, commit `24fedd3`, wrangler deploy `7f5fcc1e`) | **P3 structured-data deliverable.** P0 health audit run first = CLEAN (8/8 live URLs HTTP 200 Chrome-UA, unique titles, /nope→404, 0 em/en/24-7; GSC/cannibalization sub-checks skipped — domain 15 days old, not GSC-verified, no-data expected; contact block still fully blank in `site.json` so site converts zero leads = known launch-TODO #1 + MORNING_TASKS L64, not re-flagged). Cascade below P0: P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (no GSC data on unverified 15-day domain); P1 all friend/Suroy-gated; P2 no structural gap (2 services intentionally limited, area hub by design); image-alt P3 N/A (site has **0 `<img>` tags**, SVG/CSS only). **Real gap found:** homepage `LocalBusiness` JSON-LD was thin — only name/description/url/areaServed/address/priceRange. Added Google-recommended `image` + `logo` (both → existing `/photos/og-default.png`), a stable `@id` (`https://tarzantrimming.ca/#business`, valuable given the Tarzan Tree Removal Barrie name conflict — strengthens entity identity), and a `hasOfferCatalog` of the **2 real offered services only** (tree-pruning + limb-removal with absolute URLs + serviceType + 7-city areaServed; `full-tree-removal` `comingSoon:true` deliberately EXCLUDED for honesty — built by filtering `services.services.filter(s=>!s.comingSoon)` so it stays honest automatically). `telephone` stays conditionally absent (blank contact). **Verify:** 1 build-fail self-corrected (`services.filter` → `services.services.filter`, JSON shape is `{services:[...]}`); `npm run build` clean 8pg; dist JSON-LD parse: LocalBusiness 12 keys incl @id+image+logo+hasOfferCatalog(2 svc, telephone absent) + FAQPage 8Q both valid; sitewide dist sweep em=0/en=0/24-7=0. Pushed `2532134..24fedd3` first attempt; wrangler direct-upload deploy `7f5fcc1e`; apex content-match poll 1 (~10s, needle `hasOfferCatalog`); 8-route deploy-state sweep all 200 healthy bytes BAD=0 + /nope 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only schema change, no new URLs). Skills not invoked (mechanical entity-schema enrichment, no single-keyword GSC target on the unverified 15-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged L60/L64/L68). |
| 2026-06-13 | FAQ accordion + FAQPage schema on `/service-areas/` (cron P3, commit `7a54e03`, wrangler deploy `0e01fd80`) | **P3 structured-data + content-parity deliverable — closes the last FAQ-less indexable page on the site.** P0 health audit run first this cascade = CLEAN (8 live URLs HTTP 200 Chrome-UA, unique titles, sitemap 200, /nope → 404; GSC/cannibalization sub-checks skipped, domain 15 days old + not GSC-verified = no-data expected; contact block still blank = known launch-TODO #1, not re-flagged). Cascade below P0: P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (no GSC data on unverified 15-day domain); P1 friend/Suroy-gated; P2 no structural gap. **Real gap found:** the homepage and both service detail pages all carry an FAQ accordion + `FAQPage` JSON-LD, but the `/service-areas/` hub carried only `Service` + `BreadcrumbList` JSON-LD with no FAQ. This was the tarzan analog of the portfolio-wide blog-FAQPage-parity rollout running across the 4 rank-and-rent sites today (tarzan has no blog, so the area hub is the right target). Added 5 honest, owner-led FAQs deliberately about WHERE we work (coverage/cities, travel-fee, just-outside-the-area, timing, permits), distinct from the service-technique FAQs on the two service pages. One `faqs` array drives BOTH a visible `<details>` accordion (matching the service-page styling) AND `FAQPage` JSON-LD (guaranteed schema/visible parity). No certs, no 24/7 ("not a middle-of-the-night emergency service" + defers active-on-a-house/power-line cases to hydro/emergency services), quote-only pricing, and the permit answer stays general (no specific bylaw threshold, mirrors the published Oakville blurb). Bumped `/service-areas/` sitemap lastmod 06-09 → 06-13 per the freshness-on-edit rule. **Verify:** `npm run build` clean 8pg 861ms; dist `/service-areas/` 12.7KB → 19.5KB; FAQPage parse = 5 Questions, all 5 answer texts present on page (full parity, 0 schema-only answers); 6 `<details>` = 5 FAQ + 1 mobile-nav (expected); sitewide dist sweep em U+2014 = 0 / en U+2013 = 0; banned-24/7 visible-copy = 0 (the 3 regex hits are all `rgb(250 247 240)` = brand cream `#FAF7F0` in `about.css`, a numeric false-positive, not page copy). Pushed `36c4b55..7a54e03` 1st attempt; wrangler direct-upload deploy `0e01fd80` (GitHub auto-deploy still not connected); apex content-match poll attempt 1 (~10s, needle "Questions about where we work"); 8-route deploy-state sweep all HTTP 200 healthy bytes BAD=0 (sitemap 842B small XML expected) + /nope → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only change, no new URL). No synthetic test (no quote form). Skills not invoked (structured-data/content-parity port mirroring the proven sister-site FAQ-parity pattern, no single-keyword GSC target on the unverified 15-day domain, per `feedback_seo_skill_invocation_required.md` exemption). Every indexable tarzan page now carries an FAQ + FAQPage schema. No new MORNING_TASKS (all tarzan blockers already flagged L60/L64/L68). |
| 2026-06-13 | Per-page sitemap lastmod (cron P3, commit `48c6606`, wrangler deploy `3936a162`) | **P3 freshness-signal deliverable — closes the shared-lastmod defect class on tarzan.** `src/pages/sitemap.xml.ts` emitted a single `LASTMOD = '2026-06-09'` for all 7 URLs, so Google read a flat freshness signal that understated the pages genuinely edited since (home + both service pages changed 2026-06-13, about/contact 2026-06-10) and overstated the rest. Exact defect class hardened across the rank-and-rent sites (`feedback_sitemap_static_paths_anchor_dates.md` + `feedback_p_refresh_must_bump_lastupdated.md`) but never ported to this vicjimassage-derived site. **Site pick:** tarzan won the 3-way co-min `[auto]`/24h tie (4, tied kamloops+fortmac; sudbury 5, belleville 6) — all three tied sites have 0 actionable open issues (kamloops has no Open Issues section; fortmac items 3/4/5 = GSC/citations/competition all Suroy-blocked; tarzan TODOs friend/Suroy-blocked) → tie-break #2 oldest last-touch: tarzan 08:14 < fortmac 09:15 < kamloops 10:25, and tarzan was the only co-min site touched >4h ago (clean HARD-RULE-9 headroom). **Cascade:** P0 skip (last 08:18 ~5h, <12h cooldown); P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (15-day unverified domain, no GSC data); P1 friend/Suroy-gated; P2 no structural gap → **P3.** **Fix (1 file):** replaced `PAGES[]` + scalar `LASTMOD` with a `PAGE_LASTMOD` map of honest git-last-commit dates per page source (home/services/tree-pruning/limb-removal 06-13, service-areas 06-09, about/contact 06-10). Applied the hub-from-leaf rule per `feedback_sitemap_static_paths_anchor_dates.md`: `/services/` hub date = max of its two service leaves (06-13) even though services.astro itself last changed 06-10, so the hub never reports staler than content it links to. Added a MAINTENANCE comment: bump a page's date here in the same commit as any future content change. **Verify:** `npm run build` clean 8pg; dist `/sitemap.xml` shows per-page dates (4× 06-13, 1× 06-09, 2× 06-10), no shared stamp; sitewide dist sweep em U+2014 = 0 / en U+2013 = 0 / banned-24-7 = 0. Pushed `0bd90b8..48c6606` 1st attempt; wrangler direct-upload deploy `3936a162` (GitHub auto-deploy still not connected); apex content-match poll attempt 1 (~10s, needles `2026-06-10` + `2026-06-13` — neither existed in the old uniform sitemap); 8-route deploy-state sweep all HTTP 200 healthy bytes BAD=0 (sitemap 842B = small XML, expected) + /nope → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only change, no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical sitemap-endpoint fix, no single-keyword GSC target on the unverified 15-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged L60/L64/L68). |
| 2026-06-15 | **Build the blog system (P-PIPELINE infra, commit `015a805`, wrangler deploy `ddecf18e`)** | **P-PIPELINE infrastructure deliverable — the blog system did not exist (`src/pages/blog/` absent, blog.json absent), which structurally blocked every future P-PIPELINE / P-PIPELINE-AUTO content run on this site.** Per the SKILL P-PIPELINE rule + this site's `blog-pipeline.md` "Blog system status" note, the first eligible run BUILDS the system and the NEXT run drafts post 1; this is that build run. **Site pick (divert):** strict portfolio-min was fortmac (0 `[auto]`/24h) but fortmac's full GSC cascade fresh-verified DRY (P-EXPAND 0 rows; AUTO-feed only commercial head terms, gate-G2 excluded; P-REFRESH only utility pages + microwave/stove per-appliance ceiling pages flagged stop-refreshing per `feedback_appliance_per_service_page_serp_ceiling.md`) = its 4th consecutive NO-SHIP; sudbury (2) also GSC-dry (P-EXPAND 0, AUTO 0, fridge-repair within 60-day refresh cooldown from 2026-05-16, microwave ceiling). Diverted to tarzantrimming (3 `[auto]`/24h, last touched ~4h prior, most genuine open build work as the actively-growing owner-led site). **Built (3 new files + 2 edits):** `src/pages/blog/index.astro` (hub: forest/cream brand-coherent, empty-state aware — features the existing `/guide/when-to-prune-trees-southern-ontario/` as flagship + funnels to both services + service-areas, renders a post grid automatically once posts land; `Blog` + `BreadcrumbList` JSON-LD), `src/pages/blog/[slug].astro` (post template: intro + sections + optional FAQ accordion + inline CTA + related-services + sibling "read more"; `BlogPosting` + `BreadcrumbList` + optional `FAQPage` JSON-LD; LocalBusiness publisher; guards for tiny/empty corpus), `src/data/blog.json` (`[]`, next run drafts post 1). `sitemap.xml.ts`: imports blog.json, appends per-post URLs with honest per-post `lastmod` (updatedAt||publishedAt) + `/blog/` hub date = max(2026-06-15, newest post) per the hub-from-leaf rule (`feedback_sitemap_lastmod_per_post.md`). `Header.astro`: replaced the single-purpose "Guide" nav link with "Blog" → `/blog/` (the guide is now the featured article on the hub, so it stays reachable; IA decision, reversible). **Verify:** build clean 10pg (`/blog/` built; `[slug].astro` correctly produced 0 post pages from empty blog.json); dist sweep em U+2014 = 0 / en U+2013 = 0 / banned-24-7 = 0; hub JSON-LD 2/2 parse (Blog + BreadcrumbList); sitemap shows `/blog/` lastmod 2026-06-15. Pushed `4833705..015a805` 1st attempt; wrangler direct-upload deploy `ddecf18e` (GitHub auto-deploy still not connected); apex content-match poll attempt 2 (~20s, needle "Practical tree care guides for south Ontario"); 9-route deploy-state sweep all HTTP 200 healthy (11.2KB–38.4KB) BAD=0 + /this-should-not-exist → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (no script by design). No synthetic test (no quote form). Skills not invoked (infra scaffold, no copy claims, no single-keyword GSC target on the unverified domain). **Next run:** P-PIPELINE-AUTO can now draft post 1 (Candidate 1 "when to prune trees in Ontario" or Candidate 3 "oak wilt timing" — both pure-educational, AUTO-eligible under the hardened arborist G5 gate). |
| 2026-06-16 | **Blog hub P3 on-page fix (commit `697b97f`, wrangler deploy `e62e84b9`)** | **P0 health audit CLEAN + P3 deliverable on the newly-built (06-15) blog hub, which had never been on-page-audited.** Site pick: tarzantrimming won the co-min `[auto]`/24h tie (6, tied sudbury 6; kamloops 8, belleville/fortmac 7; arborist FROZEN) on tie-break #1 most-open-issues (~9 launch TODOs vs sudbury 2). **P0 (due, last T19:15 ~31h > 12h):** 11/11 live URLs healthy (8 static + `/blog/` 200 13.2KB confirming the 06-15 wrangler deploy landed + sitemap 200 + /404-control → 404), unique titles, theme-color `#2D4A36` + favicon.svg + og:url/og:image present, H1 "Honest tree work, owner-led, across south Ontario" (niche+region), phone `tel:+16472161874` consistent, em=0/en=0/24-7=0 across all 9 pages. GSC/cannibalization sub-checks skipped (unverified ~18-day domain, NULL `seo_keyword_discovered`/`seo_page_daily_kpi` confirmed live = no false flag); qf-email/synthetic-test N/A (no quote form). **Cascade:** P-PIPELINE human queue empty (5 candidates all `[ ] approve`); P-PIPELINE-AUTO pool unchanged since the 06-15 Phase 0 validation (all 5 seeded NO-GO: C1 dup-of-guide / C2+C5 NULL CA volume / C3 unwinnable govt SERP+dup / C4 price-gated Step E already in MORNING_TASKS; GSC self-feed empty on unverified domain) → anti-churn skip (no DfS re-spend); P-EXPAND 0 rows + P-REFRESH 0 candidates (confirmed NULL GSC live); P1 friend/Suroy-gated; P2 no autonomous structural gap (no-fabrication bars adding service pages Nathan does not offer) → **P3.** **Two genuine defects on the fresh blog hub `src/pages/blog/index.astro`:** (1) meta description was 159 chars (over the 140-155 band + past SERP truncation) → trimmed to 149 ("...When to prune, dangerous limbs, storm cleanup, and what tree work costs."); (2) `<title>` used a `|` separator while all 8 other pages + 404 use `·` → fixed to `Tree Care Guides · Tarzan Trimming` for sitewide brand/title consistency. **Verify:** build clean 10pg 1.05s; dist title=middot + desc=149c; sitewide dist sweep em=0/en=0/24-7=0. Pushed `7a7d4b8..697b97f` 1st attempt; wrangler direct-upload deploy `e62e84b9` (GitHub auto-deploy still not connected); apex content-match poll attempt 1 (~10s, needle "Tree Care Guides ·"); 11-URL deploy-state sweep all HTTP 200 healthy BAD=0 + /nope-404-control → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (no script by design, no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical meta/title audit, no single-keyword GSC target on unverified domain, per `feedback_seo_skill_invocation_required.md` exemption). DfS $0.00. No new MORNING_TASKS (all tarzan blockers already flagged: phone/cities/pricing/hours/insurance/photos/GSC/Migadu launch TODOs + Candidate 4 cost Step E). |
| 2026-06-16 | **P0 health audit CLEAN, read-only NO-SHIP (0 commits)** | **Genuinely-due P0 on the actively-growing owner-led site; no defect found, no site change.** Site pick: tarzantrimming strict portfolio-min `[auto]`/24h (2; sudbury 6, belleville 7, kamloops 8, fortmac 12; arborist FROZEN), last touch T07:21 NO-SHIP (~7.7h, clears HARD RULE 9). **P0 due** (last full P0 T02:14 ~13h > 12h). **Result CLEAN:** 9/9 sitemap URLs HTTP 200 Chrome-UA, unique titles, healthy bytes [11.7KB-38.9KB], 0 CF home-fallback; `/sitemap.xml` 200 (1076B); `/nope-404-control/` → 404. Brand markers: theme-color `#2D4A36`, favicon.svg, canonical `tarzantrimming.ca` (0 vicjimassage), og:url present, H1 "Honest tree work, owner-led, across south Ontario." (niche+region), phone `tel:+16472161874` consistent. Honesty sweep em U+2014=0 / en U+2013=0 / banned-24-7=0 across all 9 live pages. Deploy-drift check (site-specific, no GitHub auto-deploy): live `/blog/` title separator `·` confirms the 06-16 `e62e84b9` deploy is live; local `dist` (built T07:13) matches HEAD `520be89` source; working tree clean. GSC/cannibalization sub-checks skipped (unverified ~18-day domain, NULL `seo_*_daily_kpi` = no-data expected); qf-email/synthetic-test N/A (no quote form by design). **CF Email-Obfuscation false-alarm dispositioned (worth recording):** the homepage curl initially showed 0 `nathan@`/`mailto:`, which looked like the live email CTA was missing vs the source (Footer renders a `mailto:` since `763a8be`). Root cause = **Cloudflare Email Address Obfuscation** rewrote the footer `mailto:nathan@tarzantrimming.ca` into `/cdn-cgi/l/email-protection#6e000f1a...` (text decoded client-side by CF JS); decoding the hash with key byte `0x6e` confirms it = `nathan@tarzantrimming.ca`. The email IS present + works for real visitors (footer obfuscated + `/contact/` plaintext). The FinalCTA email button is intentionally gated `hasEmail && !hasPhone`, so with the phone wired it correctly shows Call+Text instead of an email button. NOT a defect, no change. **Cascade below P0** all exhausted (documented, no DfS re-spend): P-PIPELINE human queue empty + P-PIPELINE-AUTO pool unchanged since 06-15 Phase 0 (5 seeded all NO-GO/Step-E), GSC self-feed empty on unverified domain; P-EXPAND/P-REFRESH N/A (no GSC); P1 friend/Suroy-gated; P2 no autonomous gap (no-fabrication); P3 saturated (every indexable page has FAQ+FAQPage, blog hub audited 06-16, per-page sitemap lastmod, breadcrumbs site-wide). NO-SHIP per the documented anti-churn precedent (T04:14 + T19:15 clean-audit pattern). No commit, no deploy, no CF purge, no synthetic test. DfS $0.00. No new MORNING_TASKS (all tarzan blockers already flagged). Saved memory `feedback_cf_email_obfuscation_breaks_curl_check`. P0 anchor: tarzan 12h → T15:08. |
| 2026-06-17 | **P0 health audit CLEAN, read-only NO-SHIP (doc-only row, no site change)** | **Genuinely-due P0 on the owner-led growing site; no defect, no site/content change.** Site pick: tarzantrimming won the 3-way strict portfolio-min `[auto]`/24h tie (1 each; tied belleville + sudbury; kamloops 6, fortmac 7; arborist FROZEN). Sudbury excluded (just ran T09:11 P0, inside HARD-RULE-9 4h window); tie-break #2 oldest last-touch tarzan T15:08 06-16 < belleville T19:14 06-16. **P0 due** (last full P0 T15:08 06-16 ~19h > 12h cooldown). **Result CLEAN:** 9/9 live URLs (8 static + `/blog/` hub) HTTP 200 Chrome-UA, unique correct titles, healthy bytes [11.2KB-38.4KB], 0 CF home-fallback; `/nonexistent-xyz/` → 404 (branded). Brand markers: theme-color `#2D4A36`, favicon.svg, H1 "Honest tree work, owner-led, across south Ontario." (niche+region), phone `tel:+16472161874` = site.json `(647) 216-1874` consistent, email nathan@tarzantrimming.ca live. Live homepage honesty sweep em U+2014=0 / en U+2013=0 / banned-24-7=0. **GSC pipeline health (portfolio-wide, P0 step d):** `[SEO] Daily GSC KPI` workflow `uyBDgQqkyq0WNqOz` active=true; `seo_query_daily_kpi` MAX(metric_date)=2026-06-14 (3 days, under the 4-day stale rule); GSC `rank_tracking_results` MAX(checked_at)=2026-06-15T10:00Z (2 days), both FRESH, no flag. GSC/cannibalization SITE sub-checks skipped (unverified ~19-day domain, NULL `seo_*_daily_kpi` for tarzan = no-data expected); qf-email/synthetic-test N/A (no quote form by design). Working tree clean (no orphaned WIP to adopt). **Cascade below P0** all exhausted (no DfS re-spend): P-PIPELINE human queue empty + P-PIPELINE-AUTO pool unchanged since 06-15 Phase 0 (5 seeded all NO-GO/Step-E) + GSC self-feed empty on unverified domain; P-EXPAND/P-REFRESH N/A (no GSC); P1 friend/Suroy-gated; P2 no autonomous gap (no-fabrication); P3 saturated (every indexable page has FAQ+FAQPage, blog hub audited 06-16, per-page sitemap lastmod, breadcrumbs site-wide). NO-SHIP per documented anti-churn precedent. No site/content commit, no deploy, no CF purge, no synthetic test, DfS $0.00. No new MORNING_TASKS (all tarzan blockers already flagged). P0 anchor: tarzan 12h → T10:08 (2026-06-17). |
| 2026-06-18 | **P5 competitor-gap analysis: created `competitor-gaps.md` (commit `4df5c12`)** | **First competitor-gap analysis for tarzantrimming, brings it to parity with the 4 rank-and-rent sites which all already had this file (sudbury got its own 06-17, fortmac 06-18).** Site pick: tarzantrimming strict portfolio-min `[auto]`/24h (0; belleville 1, sudbury 2, kamloops 4, fortmac 4; arborist FROZEN). Content cascade verified exhausted portfolio-wide first (free, Supabase REST + n8n read-only): P0 NOT due on ANY site (all 5 last-P0 within 12h: kamloops T11:24, belleville T12:08, sudbury T15:08, fortmac T09:34, tarzan T10:18); P-EXPAND dry (kamloops only banned/owned head terms, other 3 sites `[]`); P-REFRESH all winnable pages cooldowned or per-appliance-ceiling (sudbury fridge lastUpdated 06-04 + prior refresh did not move pos, microwave refreshed 06-09); P-PIPELINE human queue drained (kamloops C4/C5 `[x] drafted`, C1/C3/C6 human-gated incl pre-authored poly-B) + AUTO self-feed empty (sudbury 0 queries >=15imp/90d, kamloops 66 all commercial head terms, tarzan 5 seeded all NO-GO since 06-15 + no GSC on ~24-day domain). GSC pipeline health (P0 step d, incidental): `[SEO] Daily GSC KPI` `uyBDgQqkyq0WNqOz` active=true, 3 daily execs success, `seo_query_daily_kpi` + `rank_tracking_results` MAX 2026-06-15 (3d, fresh). With no P0 due + content saturated (5th portfolio-wide confirmation this cycle, see ROUTINE_LOG), diverted to the one available non-churn deliverable: the missing P5 competitor-gaps.md on the strict-min site. **Method (free, $0.00 DfS):** WebSearch SERP discovery + WebFetch on communitytreecare.ca (full-service + blog), dochollidaytreeservice.com (arborist reports + storm), codyscontractingandtreeservice.com (corridor match), + toronto.ca tree-permit pages (Ch.813, 30cm DBH, $500-$100k fines). **Findings:** GTA corridor is a MATURE full-service field (unlike our thin appliance/plumber incumbents), so tarzan is the challenger; the clearest opening is a credential-thin field (most competitors do not state ISA cert / insurance / years). 8 gaps surfaced, split AUTONOMOUS (per-city tree-permit/bylaw content = highest claim-free lever, area pages, honest storm angle) vs NATHAN-DECISION (arborist reports/ISA cert = highest revenue + most gated, stump grinding, stated trust block, GBP claim, hedge trimming) per the no-fabrication rule for this real business. Doc-only deliverable (not in src/, no site change). **Verify:** em U+2014=0 / en U+2013=0 (the 3 "24/7" + 2 "always available" hits are all negation/instruction context forbidding those claims on-site, never enters dist). Pushed `932703b..4df5c12` 1st attempt. No build/wrangler-deploy/CF-purge/IndexNow (repo doc, not deployed, mirrors sudbury commit `d79ac21` doc-only handling). Skills not invoked (strategic analysis, no single-keyword GSC target on unverified domain). One consolidated MORNING_TASKS line added pointing Nathan to the surfaced service-line/credential decisions (distinct from the existing blog-approval/launch-TODO flags). |
| 2026-06-18 | **FIRST blog post published: Toronto tree removal permit explainer (P-PIPELINE-AUTO, commit `8cbd5b3`, wrangler deploy `2995e289`)** | **The blog system built 06-15 had sat empty (`blog.json` = `[]`); this is its first real post + the first P-PIPELINE-AUTO content shipped on tarzantrimming.** Site pick: tarzantrimming strict portfolio-min `[auto]`/24h (2; belleville 3, kamloops/sudbury/fortmac 4; arborist FROZEN). Cascade: P0 not due (last T10:18, ~11h < 12h); **P-PIPELINE human `[x] approve` queue empty → P-PIPELINE-AUTO ran.** The 5 seeded candidates were all confirmed dead 06-15 (C1 dup-of-guide, C2/C5 NULL CA vol, C3 govt-wall, C4 price-gated Step E) + GSC self-feed empty (unverified ~20-day domain), so the topic was pulled from the **06-18 `competitor-gaps.md` Gap 1** (per-city tree-permit/bylaw content, flagged there as the #1 autonomous claim-free lead-funneling lever). **Phase 0 `seo-keyword-qualifier` = GO:** focus kw `Toronto tree removal permit` 260/mo LOW comp ($6.16 CPC); SERP positions 3 to 12 are small arborist blog explainers (toarborist, bigleaftree, torontotreeservices `/guides/` cluster) we can match, gov owns only #1-2, no AI Overview, Reddit only #14; competitor pages etv 44/164/176 ranking 19 to 50 kw each. **Drafted via the 3-phase protocol** (1,221 words, operator voice, short paras + lists + bolded callouts). **Honesty discipline (real business, no-fabrication):** funnels the PRUNING + LIMB-REMOVAL subset only, states plainly that full tree removal "is not something we offer yet" + routes genuine removals to the city process, NO pricing/credential/insurance claim, every bylaw fact attributed to City of Toronto Municipal Code Chapter 813 (30 cm DBH threshold, arborist report, ~30-business-day review, $500 to $100,000/tree fines), threshold-under-review hedge. **Phase 4 `seo-on-page-optimizer` = 16 PASS / 2 WARN (inbound links limited as first post; org byline on borderline-regulatory topic) / 0 FAIL.** 3 JSON-LD blocks (BlogPosting + BreadcrumbList + FAQPage 5 Q). **Verify:** build clean 11pg (was 10, +1 post); `/blog/` hub now renders the post card (13.7KB); sitemap includes the post w/ per-post lastmod 2026-06-18; sitewide dist em=0/en=0/24-7=0. Pushed `2cf90fa..8cbd5b3` 1st attempt; wrangler direct-upload deploy `2995e289`; apex content-match poll attempt 1 (~10s, needle "Toronto Tree Removal Permit: The Rules"); 10-URL deploy-state sweep all HTTP 200 healthy [11.2KB to 38.4KB, post 28.0KB] BAD=0 + /nope-control → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design). No synthetic test (no quote form). Skills INVOKED: seo-keyword-qualifier (Phase 0, GO) + seo-on-page-optimizer (Phase 4, 16/18). DfS spend ~$0.05 (cached SERP + volume + traffic-est), well under $1/run cap. Recorded in `blog-pipeline.md` under "Auto-approved + drafted (competitor-gap self-feed)" + MORNING_TASKS FYI for Nathan review. **Next permit-content candidate per Gap 1: Mississauga or Oakville, staggered to avoid a thin-content spike on a young domain.** |
| 2026-06-19 | **2nd blog post published: Mississauga tree removal permit explainer (P-PIPELINE-AUTO, commit `2cf6fdc`, wrangler deploy `4ed48c1f`)** | **The staggered Gap-1 follow-on to the 06-18 Toronto post; 2nd P-PIPELINE-AUTO content on tarzantrimming, within the 1-per-24h + 3-per-7d velocity caps (last auto-post 06-18 21:17, ~25h prior; 2nd in 7d).** Site pick: tarzantrimming strict portfolio-min `[auto]`/24h (0; sudbury 1, belleville 2, fortmac 4, kamloops 6; arborist FROZEN). **P0 health audit run first = CLEAN** (last full P0 06-18 T10:18 >12h, due): 11/11 live URLs HTTP 200 Chrome-UA healthy bytes [11.2KB to 38.4KB] + /nope-404-control → 404; unique titles; theme-color `#2D4A36`, favicon.svg, phone `tel:+16472161874` (= site.json), H1 niche+region; live homepage em=0/en=0/24-7=0. GSC pipeline (P0 step d, portfolio-wide): `[SEO] Daily GSC KPI` `uyBDgQqkyq0WNqOz` active=true; `seo_query_daily_kpi` MAX 2026-06-17 (2d, FRESH < 4d); `rank_tracking_results` (gsc) MAX 2026-06-15T10:00Z = weekly tracker's normal mid-week state (runs Mondays, next 06-22), daily-KPI freshness proves GSC auth healthy, NO false stale-flag. GSC/cannibalization SITE sub-checks skipped (unverified ~21-day domain, NULL `seo_*_daily_kpi` = no-data expected); qf/synthetic N/A (no quote form). **Cascade:** P-PIPELINE human `[x] approve` queue empty → **P-PIPELINE-AUTO** (5 seeded all NO-GO since 06-15 + GSC self-feed empty on unverified domain), topic pulled from `competitor-gaps.md` Gap 1 = next per-city permit (Mississauga, larger of the two named cities). **Phase 0 `seo-keyword-qualifier` = GO WITH MODIFIER:** focus kw `Mississauga tree removal permit` 40/mo + cluster ~120/mo (`tree permit mississauga` 40, `mississauga tree bylaw` 20) LOW comp, high CPC $6.24 to $10.76; SERP = City owns #1-2, positions 4 to 14 are small arborist blog explainers (torontotreeservices `/guides/mississauga`, toarborist penalty, tree-care.ca) we can match, no AI Overview, 1 Reddit #5; small-site competitor etv only ~3.3/3.5/0 (City absorbs head-term clicks) → modifier = target the bylaw CLUSTER + lead conversion angle + FAQPage schema. **Drafted via the 3-phase protocol** (1,201 body words + ~270 FAQ, operator voice, short paras + lists + bolded callouts). **Honesty (real business, no-fabrication):** ALL bylaw facts primary-source verified via WebFetch on mississauga.ca + attributed to **By-law 0021-2022** (15 cm DBH threshold vs Toronto's 30; $399.77 / $139.72 incl-HST 2026 fees; one replacement per 15 cm removed, native ≥1.8m conifer / ≥6cm deciduous; 30-business-day review; no fee for dead/dying; bylaw covers "injure OR remove" → honest pruning caveat); penalties kept accurate-but-general ("thousands per tree", NOT a specific unverified figure); funnels pruning + limb-removal only, full removal stated not-yet-offered, no pricing-as-our-rate/credential/insurance claim. Cross-links the Toronto permit post (bidirectional, Toronto post grew 27976B→28983B confirming the inbound link landed). **Phase 4 `seo-on-page-optimizer` = 16 PASS / 2 WARN (E-E-A-T org byline on regulatory topic; inbound links limited as 2-post corpus) / 0 FAIL → GO.** 3 JSON-LD blocks (BlogPosting wordCount 1201 + BreadcrumbList + FAQPage 5 Q); title 49c keyword-front-loaded; meta 151c; canonical self-ref; 6 unique internal links. **Verify:** build clean 12pg (was 11, +1 post); `/blog/` hub renders the new card (14.6KB); sitemap includes post w/ per-post lastmod 2026-06-19; sitewide dist em U+2014=0 / en U+2013=0 / 24-7=0; 0 slop/not-X-but-Y. Pushed `213674a..2cf6fdc` 1st attempt; wrangler direct-upload deploy `4ed48c1f`; apex content-match poll attempt 1 (~10s, needle "When You Need One, What It Costs"); 11-URL deploy-state sweep all HTTP 200 healthy BAD=0 + /nope-404-control → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (no script by design). No synthetic test (no quote form). Skills INVOKED: seo-keyword-qualifier (Phase 0, GO-WITH-MODIFIER) + seo-on-page-optimizer (Phase 4, 16/18). DfS spend ~$0.075 (cached volume + fresh SERP + traffic-est), under $1/run cap. Recorded in `blog-pipeline.md` + MORNING_TASKS FYI for Nathan. **Fee figures time-sensitive (City revises annually) — re-verify before any external campaign. Next per-city permit candidate per Gap 1: Oakville (or Hamilton/Burlington), staggered again.** |
| 2026-06-20 | **P3 internal-link equity: linked both permit guides from `/service-areas/` (commit `1ceee80`, wrangler deploy `2d03aed1`)** | **Closes the "inbound links limited as a 2-post corpus" WARN both permit-post Phase 4 audits flagged.** Site pick: tarzantrimming strict portfolio-min `[auto]`/24h (2; belleville/sudbury/fortmac 4, kamloops 6; arborist FROZEN). **P0 health audit run first = CLEAN** (last full P0 06-18 T10:18 >12h, due): 11/11 live URLs HTTP 200 Chrome-UA healthy bytes + /nope-404-control → 404; unique titles; theme-color `#2D4A36`, favicon.svg, phone `tel:+16472161874`; live homepage em=0/en=0/24-7=0. GSC pipeline (P0 step d): `[SEO] Daily GSC KPI` `uyBDgQqkyq0WNqOz` active=true, last 3 execs success (most recent 06-20 01:00Z); `seo_query_daily_kpi` portfolio MAX 2026-06-17 (3d, FRESH < 4d); `rank_tracking_results` (gsc) MAX 2026-06-15 = weekly tracker's on-cadence mid-week state (runs Mondays, next 06-22), NO false stale-flag. Confirmed tarzan absent from the GSC pull domain list (predecessor `theontarioarborist.ca` IS present) → all GSC-driven phases no-op on tarzan; already documented MORNING_TASKS L116/L53 (not GSC-verified), NO duplicate flag. GSC/cannibalization SITE sub-checks skipped (unverified domain, NULL `seo_*_daily_kpi`); qf/synthetic N/A (no quote form). **Cascade:** P-PIPELINE manual queue empty + **P-PIPELINE-AUTO BLOCKED** (Mississauga post committed 06-19 22:28 EDT = ~3h40m ago, trips BOTH the 4h same-task cooldown AND the 1-per-24h velocity cap; verified via git commit time, not ROUTINE_LOG which is missing that entry); P-EXPAND/P-REFRESH N/A (no GSC). **Fell to P3 internal-link audit:** the 2 permit posts (06-18/06-19) link OUT richly to both service pages + `/service-areas/` + cross-link each other, but NO service/area/guide page linked IN, the older pages predate the posts. **Deliverable:** added a dedicated "Tree permits, city by city" prose block to the `/service-areas/` main column with direct `<a>` links to the Toronto + Mississauga permit guides (chosen over the FAQ/blurb fields, which are escaped `{}` interpolation → can't carry links without set:html/JSON-LD-drift risk). Honest framing: positioned as a helper resource, restates our pruning+limb lane, no price-as-our-rate/cert/availability claim, no em/en dash, no 24/7. **Verify:** build clean 12pg; service-areas grew 22186B→22935B; dist sweep em U+2014=0 / en U+2013=0 / 24-7=0; both links + new H2 present in dist. Pushed `84cb6ef..1ceee80` 1st attempt; wrangler deploy `2d03aed1`; apex content-match poll attempt 1 (needle "Tree permits, city by city" + both blog links live); 11-URL deploy-state sweep all HTTP 200 healthy BAD=0 (`/quote/` 404 expected, no quote form); CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (no script by design; no new page, only a link on an existing page). No DfS spend ($0.00, no skill/API call this run). No new MORNING_TASKS (GSC-tracking gap already documented). |
| 2026-06-21 | **P3 internal-link: completed reciprocal sibling links across the 3-post permit cluster (commit `cee39be`, wrangler deploy `dffe9ed0`)** | **Closes a chronological-drafting link gap: the three "tree removal permit" city posts (Toronto 06-18, Mississauga 06-19, Oakville 06-21) were each drafted before their later siblings existed, so cross-linking was one-directional (Oakville→both, Mississauga→Toronto only, Toronto→neither). A topic cluster needs reciprocal links to pass full authority among its members.** Site pick: tarzantrimming co-min `[auto]`/24h (2, tied belleville; belleville verified full-cascade DRY by the T08:08 run 4h prior, tarzan won tie-break #1 most-open-issues 17 TODOs). **Cascade:** P0 skip (last 04:08 ~7.7h < 12h cooldown); P-PIPELINE-AUTO BLOCKED (3rd post published 04:08 today trips BOTH 1-per-24h AND 3-per-7d velocity caps: 06-18/06-19/06-21 all in window); P-EXPAND/P-REFRESH N/A (unverified ~23-day domain, no GSC data); P1 friend/Suroy-gated; P2 no structural gap → **P3.** **Deliverable (1 file, blog.json):** (1) Mississauga H2[0] cross-border comparison extended to link Oakville (same 15 cm bar + stricter replacement rule), (2) Toronto H2[0] threshold section got a new municipal-line-comparison sentence linking BOTH Oakville + Mississauga (15 cm = half Toronto's 30). Verified `/guide/when-to-prune-trees-southern-ontario/` is a REAL flagship page (HTTP 200, 25KB) before assuming the Toronto post's existing `/guide/` link was broken — it was not. Bumped Toronto + Mississauga `updatedAt` 06-18/06-19→06-21 so sitemap freshness propagates (`feedback_p_refresh_must_bump_lastupdated.md`). All sibling links contextual (cross-border threshold comparison), no spammy bolt-ons; honesty intact (no cert/price/availability claim, no em/en dash, no 24/7). **Verify:** JSON parses; build clean 13pg; reciprocal cluster now Oakville↔Toronto↔Mississauga all bidirectional (Mississauga body links 5→6, Toronto 5→7); sitemap lastmod both = 2026-06-21; sitewide dist sweep em U+2014=0 / en U+2013=0 / 24-7=0. Pushed `b2dd691..cee39be` 1st attempt; wrangler deploy `dffe9ed0`; apex content-match poll attempt 1 (~10s, needle "if your lot sits near a municipal line"); 12-URL deploy-state sweep all HTTP 200 healthy BAD=0 (`/quote/` 404 expected, no quote form); CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (no script by design; content-only, no new URLs). No DfS spend ($0.00). No new MORNING_TASKS. |
| 2026-06-21 | **P3 cross-link: bridged the pruning-timing GUIDE <-> the tree-removal-permit blog cluster (commit `7fc4285`, wrangler deploy `de0625d6`)** | **Closes the last untouched internal-link gap on the site: the flagship `/guide/when-to-prune-trees-southern-ontario/` linked to ZERO blog posts and the 3-post permit cluster never surfaced the timing guide (the blog [slug] "Read more" grid only pulls blog.json posts, and the guide isn't one). The two clusters are a natural prune-vs-remove decision pair, so equity should flow both ways.** Site pick: tarzantrimming co-min `[auto]`/24h (4, tied belleville; belleville verified full-cascade DRY 3× in 6h via T14:16/T17:08/T17:14 NO-SHIP runs, tarzan last-touched T11:58 ~6h prior). **Cascade:** P0 DUE (last 04:08 ~14h>12h) RUN + **CLEAN** (12/12 live URLs HTTP 200 healthy bytes + correct titles, 404 control 404, theme-color `#2D4A36` + SVG favicon + phone (647) 216-1874 tel-matches-display + CTAs present, no 24/7 on live home, GSC `[SEO] Daily GSC KPI` `uyBDgQqkyq0WNqOz` active=True; tarzan-domain GSC freshness + cannibalization N/A on unverified ~23-day domain). P-PIPELINE human `[x] approve` queue empty (all 5 seeded `[ ]`); P-PIPELINE-AUTO BLOCKED (Oakville published 04:08 trips 1-per-24h cap); P-EXPAND/P-REFRESH N/A (no GSC); P1 friend/Suroy-gated; P2 = new service line needs friend sign-off (Step E, not auto) → **P3.** Distinct from T11:58 sibling-link work (that was permit-post↔permit-post; this is guide↔cluster). **Deliverable (3 files):** (1) guide gains a "When the answer is removal, not pruning" callout (matches existing `border-l-4 border-forest` idiom) linking Toronto/Mississauga/Oakville permit posts, anchored off the guide's own storm-damage + hazard sections; (2) Oakville + Mississauga "you might not need a removal at all" sections each gain ONE reciprocal inline link to the guide (Toronto already had one at its cost section, left as-is to avoid over-linking) where pruning-as-alternative + seasonal-timing comes up naturally; (3) bumped guide `dateModified` 06-15→06-21 + sitemap PAGE_LASTMOD 06-15→06-21 (`feedback_p_refresh_must_bump_lastupdated.md`). No price/cert/availability claim, no em/en dash, no 24/7. **Verify:** blog.json parses (CRLF preserved); build clean 13pg 1.21s; guide callout + 3 city links render; guide dateModified + sitemap lastmod = 2026-06-21; Oakville/Mississauga/Toronto each render exactly 1 guide link; sitewide dist sweep em U+2014=0 / en U+2013=0 / 24-7=0. Pushed `5213ee8..7fc4285` 1st attempt; wrangler deploy `de0625d6`; apex content-match poll attempt 1 (~10s, needle "When the answer is removal, not pruning"); 12-URL deploy-state sweep all HTTP 200 healthy BAD=0; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (no script by design; content-only, no new URLs). No DfS spend ($0.00). No new MORNING_TASKS. |

---

## 2026-06-14 buildout (Suroy + Claude): photos, expanded content, phone wired, pruning guide, deployed live

Commit `9bc97ff`, deployed to CF Pages (direct-upload wrangler, `--branch=main` = production). Verified live on tarzantrimming.ca with Chrome UA.

**Contact wired:**
- **Phone: (647) 216-1874** wired into `site.json` contact (phoneE164 `+16472161874`). This lights up call AND text CTAs sitewide (Header, Hero, FinalCTA, Footer, PhoneFloat, ServicesGrid, contact page). **NUMBER NEEDS SUROY CONFIRMATION**: this is the cell TOA's tracked line forwards to (documented in `sites.forward_to_phone` for the-ontario-arborist as "friend cell, kept private"). Suroy chose "Nathan's personal cell" in the build decision, so this drops the privacy buffer TOA used. If the number is wrong, fix `site.json` contact block and redeploy (trivial).
- **Email still EMPTY (intentional).** `nathan@tarzantrimming.ca` has zero MX records (verified via DNS 2026-06-14); the Migadu mailbox was never set up. Putting it live would bounce customer email. Suroy chose "set up Migadu first." Once the mailbox is live and MX resolves, set `site.contact.email` to `nathan@tarzantrimming.ca` and the email CTAs auto-light.

**Migadu email setup (pending Suroy's dashboard step):**
1. admin.migadu.com -> Domains -> Add domain `tarzantrimming.ca` (Micro plan, would be 7th domain). Migadu shows a domain-specific verification record + the standard DNS set.
2. Standard Migadu records (confirm against the dashboard): MX `aspmx1.migadu.com` (10) + `aspmx2.migadu.com` (20); SPF TXT `v=spf1 include:spf.migadu.com -all`; 3 DKIM CNAMEs `key1/2/3._domainkey` -> `key1/2/3.tarzantrimming.ca._domainkey.migadu.com`; DMARC TXT `_dmarc` `v=DMARC1; p=quarantine;`; plus Migadu's ownership-verify TXT.
3. Claude can add all CF DNS records via the CF API (zone `8dccf00d34ed0f68f554bc24a66a54ad`) once Suroy pastes/sends the dashboard's exact values (especially the ownership-verify TXT, which is domain-specific).
4. Create mailbox `nathan@tarzantrimming.ca` in Migadu. Nathan hooks it to Gmail via "Check mail from other accounts" (IMAP) + "Send mail as" (SMTP) so he never leaves his inbox.

**Photos (real Unsplash, free commercial license, no attribution required):** all WebP, in `public/photos/`. Sourced + visually curated (viewed before use), NOT presented as Nathan's own jobs.
- `hero-canopy.webp` (Unsplash photo-1658615324653) maple canopy = Hero bg under a forest-dark/78 overlay (cream text stays legible).
- `about-trees.webp` (photo-1593976350425) tall trees, vertical = MiniAbout slot (decorative, no person, replaces "[Owner photo coming]"; swap for a real Nathan portrait later).
- `work-climb.webp` / `work-limb.webp` / `work-rigging.webp` (photo-1626828476637 / 1588878309774 / 1657730391002) = honest "the kind of work we do" band in PhotoGallery (replaced the dishonest "if your tree is in here, that means we did the work" copy with "representative photos of the trade ... real before-and-after shots will go here as the first projects wrap up").
- `accent-branches.webp` (photo-1654460883780) = guide page hero band. `accent-forest.webp` (photo-1441974231531) available, unused so far.
- Total photos payload ~2.0MB, lazy-loaded below the fold; hero is fetchpriority=high.

**Content expansion (research -> draft -> adversarial-verify workflow; sources below):**
- `services.json`: richer tree-pruning + limb-removal descriptions + new `seasonalNote` field (dormant-season Nov-Mar, oak wilt growing-season avoidance, maple/birch sap bleeders). Rendered via new "When to do this work" block in `services/[slug].astro`. Full-tree-removal stays comingSoon.
- `faq.json` (homepage): REMOVED the unverified "commercial general liability insurance" claim; rewrote to 12 accurate Qs (best time to prune, permits-vary-by-city, dangerous-limb signs, quote visit, winter pruning, pricing model, emergency, full removal not yet, cleanup + EAB ash-wood note, speed, payment, cities).
- `about.astro`: +4 deeper owner-led paragraphs (side-business rationale, single-point accountability, safety-driven scope, word-of-mouth). No fabricated credentials/insurance/years/job-counts.
- NEW `/guide/when-to-prune-trees-southern-ontario/`: flagship ~900-word educational guide (dormant season, dead-wood-anytime, sap bleeders, spring bloomers, oak wilt, storm damage, hazard signs, CTA) + Article + BreadcrumbList JSON-LD. Added to Header nav (label "Guide") + sitemap (2026-06-14).
- Honesty fix: removed "faster and cheaper than full tree work" from limb-removal copy (services.json description + the per-service FAQ in [slug].astro) per the verifier flag.
- Permit info kept GENERAL on purpose: several municipal private-tree bylaws are mid-revision (Toronto 30cm->20cm proposal, Hamilton harmonization in consultation), so no specific thresholds/fees are published; the copy says we confirm the rule per property before any cut.

**Verification:** `npm run build` clean (9 pages). Zero em/en dashes (swept). Browser preview confirmed hero legibility, 6-item desktop nav fits (859px needed vs 1136px budget, 277px headroom), 3 work images render, guide renders. Live curl (Chrome UA) confirmed: phone present, gallery reframed, insurance absent, guide 200 + in sitemap.

**Research sources (for the seasonal/permit/EAB claims):** Ontario.ca oak wilt; City of Toronto oak wilt (no-prune Apr 1 to Oct 31); Invasive Species Centre; CFIA emerald ash borer; MSU/Wisconsin extension pruning timing; RHS bleeding cuts; ISA storm-damage guidance.

**Still TODO (Nathan / Suroy):** confirm the published phone number; set up Migadu + flip email live; send real owner photo + real before/after job photos (then swap the decorative gallery); confirm insurance status if the FAQ should ever mention it again; confirm whether full-tree-removal stays listed as "coming soon".

---

## 2026-06-14 (later): nathan@tarzantrimming.ca email LIVE end-to-end

Migadu email fully set up and verified. The address is now on the site.

- **Mailbox:** `nathan@tarzantrimming.ca` created via Migadu API (sender name "Tarzan Trimming"). Password held by Suroy (generated, 22 chars). Account: Migadu "Suroy Holdings Inc." Micro plan, auth username `suroy10@gmail.com`. Added `MIGADU_ACCOUNT_EMAIL` to `.env` (key was `migadu_API_Key`).
- **DNS (Cloudflare zone `8dccf00d34ed0f68f554bc24a66a54ad`), all green in Migadu Diagnostics:** MX aspmx1/aspmx2.migadu.com; SPF `v=spf1 include:spf.migadu.com -all`; 3 DKIM CNAMEs key1/2/3._domainkey -> keyN.tarzantrimming.ca._domainkey.migadu.com; DMARC `v=DMARC1; p=quarantine;`; **ownership-verify TXT `hosted-email-verify=mp1zhzjn`** (this was the one blocker; the token is dashboard-only, not exposed by the API, Suroy read it off the Migadu Verification Instructions page and Claude added it via CF API).
- **Activation:** domain stayed `inactive` ~30+ min on Migadu's auto-check; flipped to active within seconds once Suroy clicked **Rerun Checks** after the verify TXT was added. `can_receive` + `can_send` both true.
- **End-to-end test PASSED:** SMTP send from nathan@ (smtp.migadu.com:465) delivered a test to suroy10@gmail.com AND a self-test; IMAP (imap.migadu.com:993) confirmed the self-test landed in the inbox. No new-mailbox send throttle hit. The earlier `535 authentication failed` was purely the domain being inactive (can_send=false), not a credential problem.
- **Site:** `site.contact.email` set to `nathan@tarzantrimming.ca`; email CTAs now render on FinalCTA, Footer, contact page. Built + deployed to CF Pages (production deployment, latest). CF cache purged.
- **Nathan's Gmail hookup (settings):** Send mail as -> smtp.migadu.com:465 SSL, user `nathan@tarzantrimming.ca` + password; Receive -> pop.migadu.com:995 SSL (or imap.migadu.com:993); verification code retrievable at webmail.migadu.com. He can change the password in webmail.

---

## 2026-06-14 (T04:14 cron): independent P0 health audit of the overnight buildout — CLEAN, no changes

First autonomous-routine pass after the Suroy+Claude overnight buildout (`9bc97ff` + email-live deploy). Run as a read-only health audit to independently verify the big manual change set shipped clean to production. **No site files changed** (nothing to fix; also avoids colliding with the active manual session, last commit 03:12 ~1h prior).

- **All 8 sitemap URLs HTTP 200** under a Chrome UA, each with a unique descriptive `<title>` and healthy byte size (home 38KB, /services/ 17KB, /services/tree-pruning/ 23KB, /services/limb-removal/ 23KB, /service-areas/ 22KB, /guide/when-to-prune-trees-southern-ontario/ 18KB, /about/ 13KB, /contact/ 11KB). Unmatched route `/nope-xyz123/` correctly returns **404** (custom 404 page working).
- **Head + brand markers:** `theme-color #2D4A36` present, `favicon.svg` linked, `og:image` -> `/photos/og-default.png` resolves 200, H1 "Honest tree work, owner-led, across south Ontario" carries niche + region.
- **Contact wiring verified live:** phone `tel:+16472161874` consistent sitewide (the newly-wired (647) 216-1874). Email `nathan@tarzantrimming.ca` now live per the section above.
- **Honesty sweep CLEAN:** em-dash (U+2014) = 0, en-dash (U+2013) = 0, banned 24/7-family = 0 across all 8 pages via a Python unicode scan (important because the buildout added the ~1060-word guide plus heavily expanded faq/about/services prose).
- **Image audit:** 6 `<img>` tags sitewide (first real photos on the site, previously SVG/CSS only). Only flag was homepage `hero-canopy.webp` with `alt=""`, which is **correct** for a decorative background image under a dark overlay (screen readers skip it); the other 5 (about-trees, work-climb, work-limb, work-rigging + one more) all carry descriptive alt + width + height (no CLS risk).
- **New `/guide/` page audit:** clean. 4 in-content internal links (home + both /services/ pages + /contact/), `Article` + `BreadcrumbList` + `Organization` JSON-LD, meta-description 153 chars (in the 140-155 SERP band), sitemap `lastmod` correctly 2026-06-14, ~1060 words.
- **GSC / cannibalization sub-checks intentionally skipped:** domain is ~16 days old and not GSC-verified, so NULL `seo_*_daily_kpi` = no-data-yet (expected), not a stale-pipeline failure. No false flag raised.
- **Cascade below P0** all N/A or blocked: P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system yet), P-EXPAND/P-REFRESH N/A (no GSC data on unverified domain), P1 friend/Suroy-gated. No structural-gap P2.
- **Drift corrected in MORNING_TASKS:** the L64 caveat claiming the site "converts zero leads / contact blank in site.json" is now stale (phone + email are live); fixed in place. Residual: the published phone number still needs Suroy's final confirmation (see the 2026-06-14 buildout section).

---

## 2026-06-14 (T05:08 cron P3): contextual internal links into the pruning guide from both service pages

Commit `7332cb3`, wrangler direct-upload deploy `5d7259e9` (GitHub auto-deploy still not connected). **P3 internal-linking deliverable — closes the orphan-content gap on the new `/guide/` page.**

- **Site pick:** tarzantrimming had the fewest `[auto]` commits in the last 24h (7; fortmac + belleville 8, kamloops + sudbury 9). Deterministic STEP 2 selection, no tie.
- **Cascade:** P0 skip (ran T04:14 ~54 min prior, 12h cooldown); P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (16-day domain, not GSC-verified, no `seo_*_daily_kpi` data); P1 friend/Suroy-gated; P2 no structural gap → **P3.**
- **Real gap found:** the flagship `/guide/when-to-prune-trees-southern-ontario/` page (built in the 2026-06-14 overnight buildout) had **zero in-content inbound links** — it was reachable only from the global Header nav and the sitemap. Nav links carry far less ranking weight than contextual body links, so the guide was effectively orphaned from the site's internal link graph. The guide already links OUT to both service pages + contact (its CTA box), but nothing linked back INTO it.
- **Fix (1 file, `src/pages/services/[slug].astro`):** added a slug-conditional contextual link into the guide inside the existing "When to do this work" block (which renders on both service pages, since both have a `seasonalNote`). Anchor text is slug-specific so it honestly matches the guide section each service relates to: **tree-pruning** → "For the full seasonal breakdown, see our guide on when to prune trees in southern Ontario, which covers the dormant season, sap bleeders like maple and birch, and oak wilt timing"; **limb-removal** → "Dealing with a limb after a storm? Our guide ... also walks through storm damage and the signs a limb may need to come down." Both reference sections that actually exist in the guide (no misleading anchors). Added a `guideHref` const + explanatory comment. The guide now has 2 honest contextual inbound links plus the nav link.
- **Verify:** `npm run build` clean 9pg 786ms; dist confirms each service page carries exactly its own blurb (slug isolation verified both ways: tree-pruning has the pruning blurb and NOT the limb blurb, limb-removal vice versa); Python unicode sweep em U+2014 = 0 / en U+2013 = 0 across all dist HTML; banned-24/7 visible-copy = 0. Pushed `b8b26cd..7332cb3` first attempt; wrangler deploy `5d7259e9`; apex content-match poll attempt 1 (~10s, needle "For the full seasonal breakdown"); 8-route deploy-state sweep all HTTP 200 healthy bytes (home 38KB, both service pages ~23KB) BAD=0 + /nope → 404 + sitemap 200; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only change, no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical internal-link placement, no single-keyword GSC target on the unverified 16-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged; published phone still pending Suroy confirmation per prior section).

---

## 2026-06-14 (T09:13 cron P3): homepage→guide + guide→service-areas internal links (completes the guide link graph)

Commit `38f75e2`, wrangler direct-upload deploy `0c627fda` (GitHub auto-deploy still not connected). **P3 internal-linking deliverable — completes the flagship guide's internal link graph from the two highest-value missing directions.**

- **Site pick:** tarzantrimming + belleville tied portfolio-min `[auto]`/24h at 7 (sudbury 9, fortmac 10, kamloops 8). Tie-break #1 (most unresolved open issues): tarzan has 17 launch/infra TODOs vs belleville's fewer → tarzan. Tie-break #2 (oldest last-touch) also favored tarzan (05:08 vs belleville 08:08), and belleville's 08:08 P3 was inside the HARD-RULE-9 4h window. Arborist FROZEN-excluded from selection.
- **Cascade:** P0 skip (ran T04:14 ~5h prior, 12h cooldown); P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system); P-EXPAND/P-REFRESH N/A (16-day domain, not GSC-verified, no `seo_*_daily_kpi` data); P1 friend/Suroy-gated; P2 no structural gap (2 services intentional, area hub by design, blog gated on Nathan approval per owner-led discipline) → **P3.**
- **Real gaps found (two, both genuine, both in the guide's link graph):** (1) the **homepage** — the site's highest-authority page — had **zero contextual body link** to the flagship `/guide/` page; the guide was reachable from the homepage only via the global Header nav, which passes far less equity than a contextual body link. The T05:08 run added the two service-page inbound links but the homepage was still missing. (2) The **guide's CTA box** named "across south Ontario" and listed all 7 service cities as **plain text** but never linked `/service-areas/`, the hub built specifically to serve that intent — a missing reciprocal outbound link.
- **Fix (2 files):** (a) `src/components/ServicesGrid.astro` (homepage-only component) — added one honest contextual sentence under the existing "Two things, done well" intro: "Timing matters as much as technique. Our [guide on when to prune trees in southern Ontario] covers the dormant season, sap bleeders like maple and birch, and oak wilt timing." Everything stated is genuinely in the guide. Schema-safe (not inside any JSON-LD or `faq.json` answer, so no FAQPage text drift). (b) `src/pages/guide/...astro` — wrapped the existing inline "across south Ontario" anchor target in the CTA box with `<a href="/service-areas/">` (JSX inline HTML, sits beside the existing tree-pruning/limb-removal/contact anchors). Guide now links OUT to both service pages + service-areas + contact, and has 3 contextual inbound links (homepage + both service pages) plus nav.
- **Verify:** `npm run build` clean 9pg 937ms; dist confirms homepage carries `/guide/when-to-prune-trees-southern-ontario/` body link AND guide carries `href="/service-areas/"`; Python unicode sweep em U+2014 = 0 / en U+2013 = 0 across all dist HTML; banned-24/7 = 0. Pushed `998eb58..38f75e2` first attempt; wrangler deploy `0c627fda`; apex content-match poll attempt 1 (~10s, needle "guide on when to prune trees in southern Ontario"); 11-route deploy-state sweep all HTTP 200 healthy bytes (home 38KB, services ~23KB, guide 18KB) BAD=0 + /nope → 404 + /quote → 404 (no form by design) + sitemap 200 (978B small XML expected); CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only change, no new URLs). No synthetic test (no quote form). Skills not invoked (mechanical internal-link placement, no single-keyword GSC target on the unverified 16-day domain, per `feedback_seo_skill_invocation_required.md` exemption). No new MORNING_TASKS (all tarzan blockers already flagged; published phone still pending Suroy confirmation per prior section).

---

## 2026-06-14 (T19:15 cron): P0 health audit — CLEAN, no changes

Read-only health audit. **No site files changed** (nothing to fix; the content cascade below P0 is genuinely exhausted on this site this run, so the due P0 is the run's deliverable — same pattern as the T04:14 clean-audit). This is the per-site 12h-cooldown anchor; last tarzan P0 was T04:14 (~15h prior, past the 12h cooldown, so P0 was due).

- **Site pick:** belleville was portfolio-min `[auto]`/24h (4; sudbury + tarzan 5, kamloops + fortmac 6; arborist FROZEN-excluded). Belleville cascade verified exhausted (P0 12h-cooled T13:07; P-PIPELINE topics 1+4 drafted, 2/3/5 unapproved; P-EXPAND `[]` live-verified; P-REFRESH all content candidates inside 60d cooldown — quinte-west 06-08, water-heater 06-09, sewer-line 06-04, renovations 05-26, west-hill/cannifton 05-20, drain-cleaning 06-05, thurlow 06-08, bayshore 06-09, foxboro 06-14; P1 #3/#4/#5 Suroy-blocked; P2 Trenton area-cluster gap is a Suroy targeting decision per the 11:15 P5 note, cannibalization risk vs /areas/quinte-west/; P3 verified clean 11:15). Per the documented divert-to-next-fewest-with-headroom precedent, pivoted to the next-fewest tie (sudbury 5 = tarzan 5); tie-break #1 most-open-issues → tarzan (17 launch/infra TODOs), which also has a genuinely-due P0 (headroom). Tarzan last touch T09:13 (~10h, clean HARD-RULE-9 headroom).
- **All 8 sitemap URLs HTTP 200** under a Chrome UA, each with a unique descriptive `<title>` and healthy byte size (home 38.5KB, /about/ 12.7KB, /contact/ 11.3KB, /services/ 16.7KB, /services/tree-pruning/ 23.2KB, /services/limb-removal/ 23.2KB, /service-areas/ 22.3KB, /guide/when-to-prune-trees-southern-ontario/ 17.9KB), no CF home-fallback. `/sitemap.xml` 200 (977B XML). Unmatched route `/nope-xyz/` correctly returns **404**.
- **Head + brand markers:** `theme-color #2D4A36` present, `favicon.svg` linked, canonical `https://tarzantrimming.ca/` (zero vicjimassage leftover), `og:url` present, H1 "Honest tree work, owner-led, across south Ontario" carries niche + region, hours label "By appointment. Evenings and weekends." present (honest, no 24/7).
- **Contact wiring verified live:** phone `(647) 216-1874` + `tel:+16472161874` present sitewide; Migadu email `nathan@tarzantrimming.ca` live per the buildout section.
- **Honesty sweep CLEAN:** em-dash (U+2014) = 0, en-dash (U+2013) = 0, banned 24/7-family (24/7, 24 hours, round-the-clock, always available) = 0 across all 8 live pages.
- **Deploy-drift check (site-specific, because tarzan has NO GitHub auto-deploy — wrangler direct-upload only):** live homepage carries 3 `/guide/` internal links = local `dist/index.html` (3) = git HEAD `8256a9c` (latest 09:11 internal-link commit). No drift between git, dist, and production. Working tree clean.
- **GSC / cannibalization sub-checks intentionally skipped:** domain ~16 days old, not GSC-verified, so NULL `seo_*_daily_kpi` = no-data-yet (expected), not a stale-pipeline failure. No false flag raised. Quote-form checks (qf-email/markFieldError) N/A by design (phone/text/email funnel, no form).
- **Cascade below P0** all N/A or blocked: P-PIPELINE skip (5 candidates all `[ ] approve`, no blog system), P-EXPAND/P-REFRESH N/A (no GSC data on unverified domain), P1 friend/Suroy-gated, P2 no structural gap (2 services intentional, area hub by design, blog gated on Nathan approval), P3 saturated across the 06-13/06-14 runs. No deliverable manufactured (anti-busywork per `feedback_p0_no_recurring_synthetic_lead_test.md` spirit).
- **No new MORNING_TASKS:** all tarzan blockers already flagged (L64 GitHub integration, L68 blog-topic approvals + published-phone confirmation, L72 service-areas friend-verify) and the portfolio-wide www→apex 301 (L17, flagged today). No duplication.

---

## 2026-06-15 (T06:09 cron P3): FAQ accordion + FAQPage schema on the /guide/ page (closes the last FAQ-less indexable page)

Commit `69ee09a`, pushed to GitHub `fe9d41a..69ee09a`, wrangler direct-upload deploy `10afce42` (GitHub auto-deploy still not connected). **P3 structured-data + content-parity deliverable — the flagship guide was the one indexable page on the site WITHOUT an FAQ accordion + FAQPage schema.**

- **Site pick:** tarzantrimming was the only ACTIVE site outside the HARD-RULE-9 4h cooldown. `[auto]`/24h counts: fortmac 2 (min) but run T04:08 ~2h ago (uncommitted NO-SHIP Build Log note in its working tree, 2nd consecutive dry run, content engine documented-exhausted), belleville 3 (run T02:19 ~3h50m ago, NO-SHIP), kamloops 4 (T03:23), sudbury 4 (T05:18) — all four inside the 4h window. tarzan 3 [auto], last touched T19:15 yesterday (~11h ago), eligible. Arborist FROZEN-excluded.
- **Cascade:** P0 skip (last tarzan P0 T19:15 ~11h ago, under 12h cooldown, next due ~T07:15). **P-PIPELINE-AUTO ran** (human `[x] approve` queue empty): pool = 5 seeded `blog-pipeline.md` topics + GSC self-feed (none, 17-day unverified domain). **No clean eligible candidate** — the pipeline is now partially stale because the flagship `/guide/when-to-prune-trees-southern-ontario/` page (built 2026-06-14, AFTER the pipeline was seeded 2026-06-10) covers seeded topic 1 (when-to-prune) verbatim (fails gate G6 no-duplication) and substantially overlaps topics 2 (hazard/dangerous-limb signs), 3 (oak wilt timing), and 5 (storm-limb cleanup) — auto-publishing any would cannibalize the guide on a young domain. Topic 4 (pruning cost) fails tarzan-hardened gate G5 (pricing) → Step-E escalate. **AUTO self-rejected correctly + flagged the pipeline-vs-guide staleness to MORNING_TASKS** for Suroy/Nathan re-sequencing (genuinely-distinct topics only, e.g. EAB ash-tree management which the guide only mentions in passing). Also: building a separate `/blog/` system was deliberately NOT auto-triggered — tarzan's proven content pattern is `/guide/<slug>.astro` standalone pages (Article+Breadcrumb schema), and scaffolding a redundant blog collection on a 17-day site is an architecture call for Suroy, not the cron. P-EXPAND/P-REFRESH N/A (no GSC data). P1 friend/Suroy-gated. P2 no structural gap → **P3.**
- **Real gap found:** every other indexable page (homepage, both service detail pages, `/service-areas/`) carries an FAQ accordion + `FAQPage` JSON-LD, but the guide (built 06-14, after the 06-13 site-wide FAQ-parity rollout that claimed "every indexable page now carries FAQ + FAQPage") had only `Article` + `BreadcrumbList`. The guide is ~1,526 words and the site's highest-value content page, so FAQ rich-result eligibility there has real CTR upside.
- **Fix (2 files):** (a) `src/pages/guide/when-to-prune-trees-southern-ontario.astro` — added a `faqs` array driving BOTH a visible `<details>` accordion (exact styling/markup from `/service-areas/`) AND `FAQPage` JSON-LD from one source array (schema/visible parity guaranteed). 5 timing-focused PAA-style Q&As (best month, winter safety, summer pruning, oak no-prune window, pruning frequency) — deliberately distinct from the service-technique FAQs on the two service pages and the coverage FAQs on `/service-areas/`; Q5 (frequency) adds info not in the body. Honest/owner-led: no certs, no 24/7, quote-only pricing, grounded in the same verified sources (City of Toronto oak Apr 1–Oct 31 window etc.). Bumped Article `dateModified` to 2026-06-15 (introduced a `dateModified` const; `datePublished` stays 06-14). (b) `src/pages/sitemap.xml.ts` — bumped the guide's `PAGE_LASTMOD` 06-14 → 06-15 per the freshness-on-edit rule.
- **Verify:** `npm run build` clean 9pg 987ms; dist guide FAQPage JSON-LD parses = 5 Questions + 5 acceptedAnswers, 5 visible FAQ `<details>` (+1 mobile-nav = 6, matches site convention), all 5 answer texts present on page (full parity), Article + BreadcrumbList intact, `dateModified` 2026-06-15 live; sitewide dist sweep em U+2014 = 0 / en U+2013 = 0; banned-24/7 in guide visible copy = 0; sitemap guide `lastmod` 2026-06-15. Pushed 1st attempt; wrangler deploy `10afce42`; apex content-match poll attempt 3 (~30s, needle "Questions about pruning timing"); live FAQPage = 5 questions confirmed on apex; 9-route deploy-state sweep all HTTP 200 healthy bytes BAD=0 (guide grew 18KB → 25.2KB with FAQ content, sitemap 978B small XML expected) + `/nope-xyz123/` → 404; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (site has no indexnow script by design; content-only, no new URL). No synthetic test (no quote form). Skills not invoked (FAQ-schema-parity author-task mirroring the proven sister-page pattern, no single-keyword GSC target on an unverified 17-day domain, per `feedback_seo_skill_invocation_required.md` exemption). **Every indexable tarzan page now carries an FAQ + FAQPage schema again.**
- **New MORNING_TASKS:** (1) blog-pipeline.md is stale vs the live guide (topic 1 duplicate, 2/3/5 overlap) — needs re-sequencing to genuinely-distinct topics before any approval; (2) central `rank-and-rent/ROUTINE_LOG.md` has not been appended since 2026-05-26 even though per-site HANDOFF Build Logs + git commits show daily runs (the 4h-cooldown check now relies on git, not the central log).


---

## 2026-06-15 (T13:24 cron P-PIPELINE-AUTO): Phase 0 validation of the blog pipeline, NO candidate auto-publishable (NO-SHIP; deliverable = pipeline annotation)

Commit `3d315f5`, pushed `410337c..3d315f5` (1st attempt). Doc-only change to `blog-pipeline.md` (repo root, not under `src/` or `public/`), so NO build, deploy, or cache purge needed.

- **Site pick (divert with headroom):** strict portfolio-min was fortmac (0 `[auto]`/24h) but P-EXPAND live-verified `[]` (5th consecutive dry run, content engine exhausted). The next-min cluster (kamloops/belleville/sudbury, all 4 `[auto]`/24h) was also saturated: belleville all 13 content pages inside 60-day refresh cooldown; kamloops P-EXPAND head-terms-only (covered) + all 4 page-1 blog posts already carry FAQ schema (done 06-13) + P-REFRESH cooled (sewer-line 06-04); sudbury refresh candidates are documented per-appliance SERP ceilings. So diverted to tarzantrimming as the only site with genuine NEW-content headroom (blog system built T10:22, "next run drafts post 1").
- **P-PIPELINE-AUTO:** human `[x] approve` queue empty, so the AUTO pass ran. GSC self-feed empty (domain not GSC-verified). Pool = the 5 seeded `blog-pipeline.md` topics. Ran the mandatory Phase 0 `seo-keyword-qualifier` (DataForSEO, location_code 2124 Canada) on the candidates. **Result: no candidate is auto-publishable.**
  - **C1 (when to prune trees in Ontario):** strong keyword quality (parent "when to prune trees" 260/mo LOW comp, top SERP is small arborist blogs we can win, best competitor page etv ~339 across 34 kw, no AI Overview) BUT it **duplicates the live flagship `/guide/when-to-prune-trees-southern-ontario/`** (gate G6). Disqualified.
  - **C2 (signs a tree branch is dangerous):** NULL Canadian volume + top-3 competitor etv 0.3/0/0. NO-GO (US traffic, not Canadian).
  - **C3 (oak):** "oak wilt Ontario" 90/mo but the SERP is a government/authority wall (CFIA, Invasive Species Centre, ontario.ca, City of Toronto), unwinnable for a young site; the "when to prune oak" head (50/mo) duplicates the guide's oak section. NO-GO.
  - **C4 (tree pruning cost):** the ONLY candidate with real demand ("tree pruning cost"/"tree trimming cost" both 210/mo MEDIUM). Held out of auto-publish by the tarzan-hardened G5 honesty gate (needs a price). Routed to Step E (Suroy/Nathan call).
  - **C5 (storm cleanup):** NULL volume. NO-GO.
- **Why NO-SHIP (not a busywork edit):** all 4 rank-and-rent sites are saturated and the only available edits there are FAQ-parity on pos-30+/0-impression posts (busywork by the documented anti-churn precedent, 06-14/06-15 NO-SHIP runs). Tarzan's blog pipeline is blocked until a candidate decision lands. Manufacturing a churn edit would violate Suroy's anti-busywork standard, so none was made.
- **Deliverable (genuine):** (1) annotated `blog-pipeline.md` with a "Phase 0 keyword validation (cron, 2026-06-15)" section recording the per-candidate verdicts + data, so future cron runs do not re-spend DataForSEO budget re-validating the same dead ends and the C1/guide duplication is on record. (2) MORNING_TASKS: a Step-E decision item for C4 (cost, 210/mo, with two draft-path options) + confirmation that the broader pipeline-staleness flag (L251) is now backed by hard keyword data; tightened to avoid triplicating the existing L99/L251 tarzan-pipeline entries.
- **Skills invoked:** `seo-keyword-qualifier` (Phase 0, all 5 candidates). DataForSEO spend ~$0.05 (cached SERP + volume + traffic-estimation wrappers), well under the $1/run cap.
- **Verify:** `blog-pipeline.md` em U+2014 = 0 / en U+2013 = 0; doc-only (no build/deploy). Open Issues unchanged.
- **Next run:** the highest-leverage tarzan content move is now a Suroy/Nathan decision on C4 (cost) or a pipeline re-seed (the EAB ash-tree topic from L251 is the best un-validated candidate, but should itself be keyword-checked before drafting).

---

## 2026-06-21 (T04:08 cron P-PIPELINE-AUTO): 3rd Gap-1 per-city permit post, Oakville tree removal permit

Commit `5c0db4d`, pushed to GitHub `f60c7dc..5c0db4d`, wrangler direct-upload deploy `bc52a3c5` (GitHub auto-deploy still not connected, wrangler fallback used per SKILL). **P-PIPELINE-AUTO competitor-gap self-feed: the staggered 3rd Gap-1 tree-permit explainer, after Toronto (06-18) and Mississauga (06-19).**

- **Blog corpus is now 3 posts** (the stale L101 "No blog" note predates the 06-15 blog-system build; `src/data/blog.json` now holds Toronto + Mississauga + Oakville permit explainers). NOTE: the 06-18/06-19/06-20 tarzan runs (blog system build, Toronto + Mississauga posts, internal-link P3s) shipped without HANDOFF changelog entries; this section closes that gap for the Oakville post. The per-run detail for the earlier posts lives in `rank-and-rent/ROUTINE_LOG.md` + `blog-pipeline.md`.
- **Site pick:** tarzantrimming was portfolio-min `[auto]`/24h at **0** (kamloops 3, fortmac 4, sudbury 2, belleville 2; the-ontario-arborist FROZEN-excluded), last touched ~26h ago.
- **Cascade:** P0 ran first (last tarzan P0 2026-06-19T22:30, ~30h ago > 12h cooldown) and was **CLEAN** (11 live URLs HTTP 200 with healthy byte sizes + a proper 404, unique brand-correct titles, `theme-color #2D4A36`, canonicals all on tarzantrimming.ca apex with zero vicjimassage leftover, honesty sweep 0 em / 0 en / 0 24-7). GSC/cannibalization sub-checks N/A (domain ~23 days, not GSC-verified, NULL `seo_*_daily_kpi` = no-data-yet, not a stale pipeline). Content cascade NOT exhausted, so P-PIPELINE-AUTO is the run's deliverable (not a clean-P0 log).
- **P-PIPELINE-AUTO:** human `[x] approve` queue empty, GSC self-feed empty (unverified domain), so the candidate came from `competitor-gaps.md` Gap 1 (per-city permit content, the #1 claim-free autonomous lever). Velocity check: under the 1/24h (last publish ~30h ago) and 3/7d (2 posts in 7d) caps.
- **Phase 0 `seo-keyword-qualifier` INVOKED** (~$0.05 DfS, cached, under $1 cap): **GO WITH MODIFIER.** "Oakville tree removal permit" 70/mo LOW comp $5.12 CPC (higher demand than Mississauga's 40/mo) + ~210/mo permit/bylaw cluster. SERP: City of Oakville + Town GIS own #1-5, but #6 (almileytree.com) and #8 (torontotreeservices.ca) are matchable small-arborist blog explainers; no AI Overview; 1 Reddit at #7 (under the 2+ threshold). Measured small-site etv only ~5/0 (City absorbs head clicks, same pattern as Mississauga). Modifier: target the bylaw cluster + conversion angle, FAQPage schema; commercial head "tree removal oakville" 110/mo HIGH comp stays on the service pages.
- **No-fabrication gate (hardened G5, real business):** every bylaw fact primary-source verified via WebFetch on oakville.ca and attributed to the Private Tree Protection By-law (By-law 2017-38, updated 2024). 15 cm DBH threshold at 137 cm (stricter than Toronto's 30 cm), fees $65 first tree / $425 additional or >24 cm / no fee for dead-high-risk-ash-buckthorn, 1 replacement per 10 cm healthy DBH + $300 deposit, inspector contact within 5 business days, non-compliance under Administrative Penalty By-law 2021-038. Funnels pruning + limb-removal only, full removal stated not-yet-offered, no pricing-as-our-rate / credential / insurance claim. Fee + replacement figures flagged time-sensitive in MORNING_TASKS.
- **Phase 4 `seo-on-page-optimizer` INVOKED: 18 PASS / 1 WARN / 0 FAIL → GO.** WARN = org byline on a regulatory topic (consistent with the Toronto + Mississauga posts; honest for an owner-led business). Title 47c keyword-front-loaded, meta 149c, 1 exact-match H1, 5 descriptive content H2s, 1,308 body words, 9 internal links to money pages, BlogPosting + FAQPage + BreadcrumbList + LocalBusiness schema, self-canonical, OG + twitter card. Direct answer in first 100 words for the no-AIO permit SERP.
- **Build + ship:** `npm run build` clean (13 pages, 1.42s). Sitewide dist sweep: 0 em (U+2014), 0 en (U+2013), 0 banned 24-7. New page renders 30.6KB with full schema; sitemap.xml includes the new post; siblings now auto-cross-link IN to it. Pushed 1st attempt; wrangler deploy `bc52a3c5` 1st attempt; CF apex content-match poll LIVE on attempt 1 (~10s); **11-URL deploy-state sweep CLEAN** (all 200, healthy sizes, no 500s/small bodies); CF zone cache purged (success). No IndexNow (tarzan has no indexnow script; sitemap-based discovery).
- **MORNING_TASKS:** Oakville FYI added under "Auto-published this cycle" (incl. the time-sensitive fee re-verify flag). `blog-pipeline.md` updated under "Auto-approved + drafted (competitor-gap self-feed)" (60-day cooldown anchor).
- **Next run:** next Gap-1 per-city permit candidate is Hamilton or Burlington, but watch the 3/7d auto velocity cap (3 GTA permit posts now live on a ~23-day domain). The standing higher-leverage moves remain Nathan decisions: C4 cost post (210/mo), GBP claim (Gap 8), and the area-page build once the real coverage radius is confirmed (Gap 3).

---

## 2026-06-22 (T05:08 cron P3): link the Oakville permit guide from /service-areas/ (completes the areas-hub permit-cluster links)

Commit `e918683`, pushed `b91a3d6..e918683` (1st attempt), wrangler direct-upload deploy `b4c363e1` (GitHub auto-deploy still not connected, wrangler fallback per SKILL).

- **Site pick:** tarzantrimming co-min `[auto]`/24h at 4 (tied fortmac 4; belleville 5, sudbury 6, kamloops 7; the-ontario-arborist FROZEN-excluded). Tie-break to tarzan on both rules: more unresolved Open Issues than fortmac (whose 3 are all human-gated GSC/citations/competitor) AND oldest last-touch (tarzan ~11h vs fortmac ~3h, P3-churned).
- **Cascade:** P0 NOT due (last tarzan P0 06-21T18:22, ~11h < 12h cooldown, next ~T06:22). P-PIPELINE human `[x] approve` queue empty (5/5 seeded `[ ]`, all NO-GO/Step-E per the recorded Phase-0 validation). **P-PIPELINE-AUTO BLOCKED by the 3/7d auto velocity cap** (Toronto 06-18, Mississauga 06-19, Oakville 06-21 = 3 auto-posts in the rolling 7d window on a ~24-day domain), a correct anti-thin-content brake, not an error. P-EXPAND/P-REFRESH N/A (unverified domain, NULL `seo_*_daily_kpi`). P1 friend/Suroy-gated. P2 new service/area pages = claim/decision = Step E. → P3.
- **Deliverable (1 file, `src/pages/service-areas.astro`):** the "Tree permits, city by city" block linked only Toronto + Mississauga (added by `1ceee80` 06-19) but the Oakville post `5c0db4d` published AFTER that, leaving the newest permit post orphaned from the areas hub while its 2 siblings were linked. Added the Oakville permit-guide link to complete the trio, with a contextual hook to the page's own Oakville blurb ("which carries one of the stricter private-tree bylaws in the region"). Genuine internal-PageRank into the newest post + high topical relevance; follows the established `1ceee80` pattern, not churn. Claim-free (no price/cert/availability/capability assertion).
- **Verify:** build clean (13 pages, exit 0); dist sweep 0 em (U+2014) / 0 en (U+2013) / 0 banned 24-7 (Python scan, grep -P unreliable on this box's locale); all 3 `tree-removal-permit-{toronto,mississauga,oakville}` hrefs render on `/service-areas/`. Push 1st attempt; wrangler `b4c363e1` 1st attempt; CF apex content-match poll LIVE attempt 1 (~10s, needle `tree-removal-permit-oakville`); **12-URL deploy-state sweep CLEAN** (all 200, 11.2-38.4KB healthy, real 404 control, BAD=0); CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purged success:true. No IndexNow (tarzan has no script; sitemap-based discovery). No quote-form/api touched -> no synthetic test. DfS $0.00.
- **Next run:** P-PIPELINE-AUTO unblocks once the 06-18 Toronto post ages out of the 7d window (~06-25); next Gap-1 city is Hamilton or Burlington. Standing higher-leverage moves remain Nathan/Suroy decisions: C4 cost post (210/mo), GBP claim, GSC verification, confirmed coverage radius for real per-city area pages.

---

## 2026-06-22 (T08:08 cron P0): scheduled health audit, full CLEAN, content cascade dry/blocked, NO-SHIP

No commit (read-only health verification, no code change warranted).

- **Site pick:** fortmac was portfolio-min `[auto]`/24h (4; belleville 5, sudbury 5, tarzantrimming 6, kamloops 8; the-ontario-arborist FROZEN-excluded), BUT fortmac's full cascade was independently re-verified DRY at T06:11 (~2h ago) with P0 on cooldown until ~T11:08, so any phase would repeat that NO-SHIP inside the 4h HARD-RULE-9 window. Sudbury (5) was likewise cascade-verified-dry at T04:20 (~3.8h, inside 4h). Belleville (5) P0 not due (last T00:08) and its content lever is the flagged FAQPage-parity churn to stop. **tarzantrimming was the lowest-`[auto]`-count ACTIVE site with a genuinely DUE, non-repeated, high-value deliverable: its P0 was last run T06-21 18:22 (~13.8h ago, >12h cooldown).** As a real owner-led business, a health regression here matters most, so the due P0 is the correct pick.
- **P0 health audit = CLEAN.** 12/12 canonical live URLs HTTP 200 (Chrome UA) with healthy byte sizes (11.2KB to 38.4KB) and unique brand-correct `<title>`s; `/services/full-tree-removal/` correctly 404s (coming-soon, intentionally excluded from sitemap + getStaticPaths, and verified NO internal link points to it); `/nope-404-control-xyz/` returns a real 404 page (not a home-page 200 fallback). Homepage: `theme-color #2D4A36` (= brand), favicon `/favicon.svg`, `tel:+16472161874` + `(647) 216-1874` (= site.json brand block), H1 "Honest tree work, owner-led, across south Ontario." (niche + region present). Sitewide honesty sweep across all 12 pages: em U+2014 = 0, en U+2013 = 0, banned 24-7/always-available = 0. Meta-description length audit (P3 spot-check before NO-SHIP): all 12 pages 140 to 153 chars, 0 short/long/missing.
- **P0 step d (GSC pipeline health, portfolio-wide):** `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) active=true; `seo_query_daily_kpi` global MAX(metric_date) = 2026-06-19 (3d lag, within the 4d threshold = FRESH); `rank_tracking_results` source=gsc MAX(checked_at) = 2026-06-22T10:00Z (the weekly GSC tracker ran on schedule today, Monday). No flags. tarzantrimming `seo_query_daily_kpi` rows = 0 (the unverified-GSC-property blocker, already flagged in MORNING_TASKS 2026-06-21 line 33; NOT re-flagged).
- **Cascade beyond P0:** P-PIPELINE human `[x] approve` queue empty AND P-PIPELINE-AUTO BLOCKED by the 3/7d auto velocity cap (Toronto 06-18, Mississauga 06-19, Oakville 06-21 = 3 auto-posts in the rolling 7d window on a ~24-day domain; unblocks ~06-25 when the Toronto post ages out). P-EXPAND / P-REFRESH N/A (no GSC data on the unverified domain, NULL `seo_*_daily_kpi`). P1 launch/infra TODOs all friend/Suroy-gated. P2 new service/area pages = claim/coverage decision = Step E (no auto-build). P3 internal-link work exhausted by the T05:08 (areas-hub permit-cluster) + T06-21 18:22 runs; meta-desc audit clean (above). → genuinely DRY.
- **Outcome:** the DUE P0 health audit IS this run's deliverable (real-business health confirmed CLEAN); content cascade dry/blocked, so NO-SHIP (no commit). The standing higher-leverage moves remain Nathan/Suroy decisions: GSC property verification (unlocks P-EXPAND/P-REFRESH/AUTO self-feed), C4 cost post, GBP claim, confirmed coverage radius for real per-city area pages.

---

## 2026-06-22 (T12:08 cron P3): lead images on the 3 permit blog posts (BlogPosting JSON-LD image + CLS-safe header)

Commit pending; wrangler direct-upload deploy pending (GitHub auto-deploy still not connected).

- **Site pick:** tarzantrimming was the strict portfolio-min `[auto]`/24h (4; sudbury 5, fortmac 6, kamloops 7, belleville 7; the-ontario-arborist FROZEN-excluded). Read full HANDOFF before picking.
- **Cascade:** P0 skip (last full P0 T08:08 ~4h ago, <12h cooldown). P-PIPELINE human `[x] approve` queue empty AND P-PIPELINE-AUTO BLOCKED by the 3/7d velocity cap (Toronto 06-18, Mississauga 06-19, Oakville 06-21 = 3 auto-posts in the rolling 7d window on a ~24-day domain; unblocks ~06-25). P-EXPAND/P-REFRESH N/A (no GSC on the unverified domain). P1 friend/Suroy-gated. P2 new service/area pages = Step E. P3 internal-link work saturated → looked past link-shuffling for a genuine non-churn, non-new-URL on-page gap.
- **Real gap found:** the 3 permit blog posts (the site's 3 newest + highest buyer-intent pages) carried ZERO in-content imagery while every other page on the site has photos, and all 3 shared a single generic `og-default.png` in their BlogPosting JSON-LD `image`. Media presence + a topical lead image is a real on-page/engagement signal, and per-post `image` strengthens rich-result eligibility.
- **Deliverable (2 files):** (1) `src/data/blog.json` (CRLF preserved) gains a `heroImage` object per post (`src`/`alt`/`width`/`height`) using existing licensed decorative tree photography from `public/photos`. Toronto uses accent-forest.webp (1200x799), mississauga accent-branches.webp (1200x675), oakville hero-canopy.webp (1366x911). (2) `src/pages/blog/[slug].astro` renders an optional CLS-safe lead image (native width/height reserved, `loading="eager"` `decoding="async"`, rounded card, `max-h-[420px] object-cover`) between the title band and body, and the BlogPosting JSON-LD `image` now uses the per-post photo (falls back to og-default when absent). **Honesty:** photos are decorative/representative tree imagery (the same framing the rest of the site already uses), NOT presented as our own job photos; alt text describes the image generically with no city/job/credential claim; no price/cert/availability assertion; no em/en dash; no 24/7.
- **Verify:** `npm run build` clean (13 pages, exit 0); all 3 posts render the `<img>` with correct src/alt/width/height; BlogPosting JSON-LD `image` = per-post webp with correct dimensions (3/3); sitewide dist sweep em U+2014 = 0 / en U+2013 = 0 / banned 24-7 = 0; all 3 photos present in `dist/photos/`. Folded in the prior T08:08 NO-SHIP run's orphaned (uncommitted) HANDOFF changelog row.
- **Next run:** P-PIPELINE-AUTO unblocks ~06-25 (next Gap-1 city = Hamilton or Burlington). Standing higher-leverage moves remain Nathan/Suroy decisions: GSC property verification, C4 cost post, GBP claim, confirmed coverage radius for real per-city area pages.

---

## 2026-06-22 (T21:08 cron P0): add missing favicon.svg (was 404 sitewide), real P0 fix

Commit `023eb03`; wrangler direct-upload deploy `e09a0d82` (GitHub auto-deploy still not connected).

- **Site pick:** fortmac was strict portfolio-min `[auto]`/24h (4; sudbury 5, tarzantrimming 5; kamloops/belleville/arborist FROZEN-excluded), BUT fortmac's full cascade was independently NO-SHIP'd at T19:08 (~2h ago) — re-running a fortmac cascade now repeats that task inside HARD RULE 9's 4h window. Diverted. Between the sudbury/tarzan tie (5 each): tie-break to older last-touch (tarzan T16:21 vs sudbury T20:08 P3) AND tarzan's P0 was newly DUE (last full P0 T08:08, ~13h ago > 12h cooldown) while sudbury's P0 is on cooldown (T17:08) and it shipped a real P3 1h prior. So tarzan = a genuinely-due, non-repeated P0.
- **P0 audit found a REAL defect (not the usual clean pass).** `BaseLayout.astro:28` references `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />` on every page, but `public/` contained only `photos/` + `robots.txt` — no favicon asset at all. Live `https://tarzantrimming.ca/favicon.svg` returned **HTTP 404** (CF served the 10KB custom 404 HTML), so the browser-tab icon was broken sitewide and Google's favicon crawler got a 404. The OG image (`/photos/og-default.png`) was verified fine (200, 40735B). This is the inherited-from-vicjimassage layout referencing a favicon that was never created for this brand.
- **Fix (1 new file):** created `public/favicon.svg` (372B) — a brand-coherent tree mark: forest `#2D4A36` rounded tile (matches `theme-color`) with a cream `#FAF7F0` three-lobe canopy + trunk, on-niche for an arborist. No external font/asset dependency; flat geometry, legible at 16px. No em/en dash, no claims.
- **Rest of P0 CLEAN:** 12/12 canonical live URLs HTTP 200 (Chrome UA) with healthy bytes (11.9KB to 39.1KB); `/nope-control-404/` correctly 404s; `/services/full-tree-removal/` correctly absent (comingSoon=true, filtered from getStaticPaths + sitemap); theme-color `#2D4A36`, title carries niche+region, phone `(647) 216-1874` (6x display + 6x tel:) matches site.json canonical, hero H1 + "Get a free quote"/"Call" CTAs present, 0 live 24/7 claims, 0 em/en dash on homepage. No quote form on this site (form checks N/A).
- **Verify:** `npm run build` clean (13 pages, exit 0); `dist/favicon.svg` present (372B); sitewide dist sweep em U+2014 = 0 / en U+2013 = 0 / banned 24-7 = 0. Pushed `b5b8a62..023eb03` 1st attempt; wrangler deploy `e09a0d82`; apex content-match confirmed live favicon = HTTP 200, 372B, `image/svg+xml`, exact SVG match (was 404 pre-deploy); 11-URL deploy-state sweep all HTTP 200 healthy bytes BAD=0; CF zone `8dccf00d34ed0f68f554bc24a66a54ad` purge_everything success:true (HARD RULE 8, static asset added). No IndexNow (site has no IndexNow script by design; not a content URL). No synthetic test (no quote form).
- **Lesson saved to memory:** P0 health audits must curl head-referenced static assets (favicon, og-image, manifest), not only the page-URL list — a missing favicon 404s silently and never shows up in an ALL_PAGES sweep.

---

## 2026-06-23 (T09:08 cron P0): scheduled health audit, full CLEAN, content cascade dry/blocked, NO-SHIP

No commit (read-only health verification, no code change warranted).

- **Site pick:** fortmac was strict portfolio-min `[auto]`/24h (3; sudbury 5, tarzantrimming 5; kamloops/belleville/the-ontario-arborist FROZEN-excluded), BUT fortmac just ran a P0 CLEAN at T08:58 (~10 min prior) on GSC data identical to now (daily pull refreshes ~3am, no change) — any fortmac move now is a foregone NO-SHIP inside HARD RULE 9's 4h window. Diverted. Between the sudbury/tarzan tie (5 each): tie-break to most unresolved Open Issues (tarzan's launch/infra TODOs > sudbury's 2) AND oldest last-touch (tarzan T21:16 vs sudbury T23:14). **tarzan's P0 was newly DUE** (last full P0 T21:08 06-22, ~12h ago = 12h cooldown boundary) and, as a real owner-led business, a health regression here matters most. Sudbury independently confirmed content-dry this run (AUTO self-feed = only `gas stove repair near me` 18 [G2 near-me] + `appliance repair sudbury` 17 [G6 home-owned]; P-REFRESH best lever fridge-repair 110imp/pos26.5 mid-60d-cooldown to ~08-03; P-EXPAND `[]`).
- **P0 health audit = CLEAN.** 12/12 canonical live URLs HTTP 200 (Python urllib + Chrome UA per the T08:58/T21:08 Windows-curl-flake lesson) with healthy bytes (12.4KB to 39.6KB) and unique brand-correct `<title>`s (U+00B7 middle-dot separator, not a banned dash). `/services/full-tree-removal/` correctly 404s (comingSoon=true, filtered from getStaticPaths + sitemap); `/nope-404-control-xyz/` returns a real 404 (no home-page 200 fallback). Head-referenced static assets: `/favicon.svg` 200 (372B, the T21:08 fix holding) + `/photos/og-default.png` 200 (40,735B). Homepage: `theme-color #2D4A36` (= brand), `(647) 216-1874` + `tel:+16472161874` (= site.json), H1 "Honest tree work, owner-led, across south Ontario." (niche + region), 0 em (U+2014) / 0 en (U+2013) / 0 banned 24-7. BAD count = 0.
- **P0 step d (GSC pipeline health, portfolio-wide):** `[SEO] Daily GSC KPI` (uyBDgQqkyq0WNqOz) active=true (confirmed T08:58, not re-pinged within the hour); `seo_query_daily_kpi` global MAX(metric_date) = 2026-06-20 (3d lag < 4d = FRESH); `rank_tracking_results` source=gsc MAX(checked_at) = 2026-06-22T10:00Z (1d, weekly Monday tracker on schedule). tarzan `seo_query_daily_kpi` rows = 0 (the unverified-GSC-property blocker, already on file in MORNING_TASKS; NOT re-flagged). No flags.
- **P0 step e (cannibalization):** structurally N/A for tarzan (0 GSC query rows on the unverified property = no data to detect a service-vs-blog mismatch).
- **Cascade beyond P0:** P-PIPELINE human `[x] approve` queue empty (5/5 seeded NO-GO/Step-E) AND P-PIPELINE-AUTO BLOCKED by the 3/7d auto velocity cap (Toronto 06-18, Mississauga 06-19, Oakville 06-21 = 3 auto-posts in the rolling 7d window on a ~25-day domain; unblocks ~06-25 when the Toronto post ages out — a correct anti-thin-content brake, not an error). P-EXPAND / P-REFRESH N/A (no GSC data on the unverified domain). P1 launch/infra TODOs friend/Suroy-gated. P2 new service/area pages = claim/coverage decision = Step E. P3 internal-link work exhausted (permit cluster cross-linked + areas-hub linked T05:08 + footer "Helpful guides" column T16:21 + per-post lead images T12:08). → genuinely DRY.
- **Outcome:** the DUE P0 health audit IS this run's deliverable (real-business health confirmed CLEAN); content cascade dry/blocked → NO-SHIP, no commit/deploy/purge. DfS $0.00. No synthetic test (no quote form on this site). No new MORNING_TASKS (GSC property verification, the self-resolving velocity cap, and the Nathan/Suroy launch TODOs are all already on file). The standing higher-leverage moves remain Nathan/Suroy decisions: GSC verification (unlocks P-EXPAND/P-REFRESH/AUTO self-feed), C4 cost post (210/mo), GBP claim, confirmed coverage radius for real per-city area pages. Cooldown anchor: tarzan P0 → T09:08, next eligible ~T21:08 06-23.

---

## 2026-06-30 (T23:08 cron P3): de-orphan the new oak-pruning post with 2 contextual inbound links (commit db13d93, wrangler deploy 2e391ec3, LIVE)

**Site pick:** fortmac was strict portfolio-min `[auto]`/24h (3; tarzan 6, sudbury 7) but ran a full-cascade NO-SHIP at T22:46 (~22 min prior) → re-run barred by HARD RULE 9's 4h window. Diverted to next-min + oldest-touch = tarzan (6 commits, last substantive run T13:08 ~10h ago).

**Cascade:** P0 not due (last T13:08, +12h ~T01:08 07-01). P-PIPELINE human `[x] approve` queue empty (candidates 1/2/4/5 all `[ ]`). P-PIPELINE-AUTO BLOCKED by the 1-post/24h velocity cap (the oak post itself auto-published T13:08, ~10h ago). P-EXPAND / P-REFRESH N/A (tarzantrimming.ca still has 0 rows in seo_query_daily_kpi / seo_page_daily_kpi / seo_keyword_discovered — the known unverified-GSC-property blocker on file in MORNING_TASKS; verified first-hand again this run via Supabase REST). P1 launch/infra TODOs friend/Suroy-gated. → landed on P3.

**Deliverable (P3 internal-link equity):** the T13:08 auto-published post `/blog/when-to-prune-oak-trees-ontario/` was a contextual orphan — no keyword-anchored inbound links from any authority page (only the generic auto related-posts module on other blog posts). Added 2 exact-match contextual inbound links:
1. **Pruning guide** (`/guide/when-to-prune-trees-southern-ontario/`) — added a per-section `moreLink` rendered in-context directly under the "Oaks Are the Big Exception" H2 (the single most topically-relevant spot on the site). Anchor: "Read the full breakdown: when to prune oak trees in Ontario".
2. **Tree-pruning service page** (`/services/tree-pruning/`) — extended the existing "When to do this work" oak-wilt sentence with an exact-match anchor "when to prune oak trees in Ontario" (money-adjacent page → new post, passing commercial-context link equity).

**Verify:** `npm run build` clean (15 pages, 1.11s). Dist sweep: 0 em (U+2014) / 0 en (U+2013) / 0 banned 24-7. Post-deploy content-match poll: oak link LIVE on guide immediately. 11-URL deploy-state sweep (Python urllib + Chrome UA): all HTTP 200, payloads 12.4KB–39.6KB (all >> 5KB broken-state floor) → no May-22-class CF deploy-state break. CF zone cache purged (success=True). No IndexNow (no new pages; tarzan has no indexnow script). DfS $0.00. No synthetic test (no quote form on this site).

**Cooldown anchors:** tarzan P3 internal-link → T23:08 06-30; P0 next eligible ~T01:08 07-01; P-PIPELINE-AUTO 1/24h unblocks ~T13:08 07-01.

**Note (drift):** this changelog was behind live state before this entry (recent SHIPs T22:08 06-29 c602c9c, T02:08 06-30 0d877a0, T13:08 06-30 cf1fbf2 P-PIPELINE-AUTO oak post were logged to the central ROUTINE_LOG but not backfilled here). Not backfilled retroactively to avoid transcription error; the central ROUTINE_LOG remains the authoritative per-run record.

## 2026-07-13 (T23:08 cron P0): scheduled health audit (genuinely due ~13h since T10:12), FULLY CLEAN a/c/d (b skip no-src-drift; e N/A GSC-blind) + full ACTIVE cascade re-confirmed DRY first-hand, NO-SHIP

Read-only, $0 DfS, doc-only (this Changelog entry + central ROUTINE_LOG row; no source/build/deploy/CF-purge/IndexNow/synthetic/new-MORNING_TASKS). Site pick: STEP-2 ACTIVE `[auto]`/24h portfolio-min via first-hand git log = tarzan 2 (fewest) < fortmac 4 < sudbury 6 (3 FROZEN excluded); outside HR9 4h window (last-touch T18:08 ~5h). P0 genuinely due (last P0 T10:12 07-13 ~13h > 12h).

**P0 CLEAN first-hand:** (a) 14/14 live sitemap URLs HTTP 200 via spaced Python urllib + Chrome UA (per curl-000 canonical), healthy bytes 12,387B-39,565B all >> 5KB broken-state floor, 14 distinct correct brand titles (no CF home-fallback), unknown URL hard-404s (better than the 4 RR soft-404 sites); title separator = U+00B7 middot (cp1252 console `?` mojibake false-alarm re-ruled-out); dist-wide sweep em U+2014=0 / en U+2013=0 / banned-24-7=0. (c) theme-color #2D4A36 (forest brand), favicon /favicon.svg present (200 image/svg+xml), tel:+16472161874 = (647) 216-1874 canonical matching site.json, H1 "Honest tree work, owner-led, across south Ontario" (niche+region); no web form by design (owner-led phone/email, qf-email/markFieldError N/A). (b) HEAD 4de7047 = origin/main, tree clean, 0 src drift -> screenshot skipped per precedent. (d) tarzan GSC-blind re-confirmed LIVE (Supabase SHI REST seo_query_daily_kpi + seo_keyword_discovered both Content-Range */0), appliance daily pull alive (fortmacappliance.ca MAX metric_date 07-11, 2-3d GSC lag PASS); (e) cannibalization N/A GSC-blind.

**Cascade beyond P0 DRY first-hand:** P-PIPELINE human queue 0 undrafted `[x] approve` (grep: C1/C2/C4/C5 all `[ ] approve`, C3 oak + 4-city permit cluster all `[x] drafted`); P-PIPELINE-AUTO velocity-clear (last auto-pub oak 06-30 >7d) but pool exhausted (C1 dup-guide G6, C2/C5 durable NULL-Canada-vol Phase-0 NO-GO no data change, C4 pricing -> Step-E on file, permit cluster HARD-STOPPED at 4 cities pending GSC demand) + GSC self-feed structurally empty (blind domain); P-EXPAND/P-REFRESH GSC-dead (0 rows); P1 Nathan/Suroy-gated launch TODOs; P2 no claim-free structural gap + no measured demand; P3/P4 saturated (full P0-class audit ran this run, well-maintained); P5 competitor-gaps fresh 06-18. Honest NO-SHIP -- DUE P0 audit IS the deliverable, real-business health CONFIRMED CLEAN. Binding constraint = GSC verification (MORNING_TASKS L33) + Nathan launch/pricing/city decisions + off-page authority (GBP/operator, L20) the cron cannot build, ALL already on file -> no new MORNING_TASKS (no-dup). No lastmod-bump/speculative-seed (forbidden). Anchor: tarzan P0 CLEAN T23:08 07-13, next eligible ~T11:08 07-14.

### 2026-07-14 T19:08 — Full ACTIVE cascade re-confirmed DRY first-hand → read-only NO-SHIP

Site pick: STEP-2 ACTIVE `[auto]`/24h portfolio-min via first-hand git log = fortmac 3 (fewest) < tarzan 4 < sudbury 5 (3 FROZEN excluded); fortmac HR9-barred (full cascade just walked T18:08 ~1h ago <4h), sudbury HR9-barred (T16:08 ~3h), so pick falls to **tarzan** (last-touch T15:08 = exactly 4h, clears HR9). No ACTIVE P0 due (fortmac T10:08, sudbury T12:08, tarzan T11:08 all <12h). No FROZEN health-check due (kamloops T13:08 6h, belleville T20:08 07-13 ~23h, arborist T17:08 2h, all <24h).

**First-hand dry-confirm (not parroted):** (1) git HEAD 64e1f58 = origin/main after fetch, tree clean, 0 src drift. (2) tarzan GSC re-confirmed BLIND live — Supabase SHI REST `seo_query_daily_kpi?site_domain=eq.tarzantrimming.ca` = 0 rows (root blocker MORNING_TASKS L33 unchanged); appliance pull alive but frozen (sudbury + fortmac MAX metric_date both 07-11, 3d GSC lag, unchanged since prior runs; next advance ~03:00 07-15). (3) P-PIPELINE human queue = 0 undrafted `[x] approve` (grep: C1/C2/C4/C5 `[ ] approve`, C3 oak + 4-city permit cluster all `[x] drafted`). (4) P-PIPELINE-AUTO no eligible candidate — seeded pool exhausted per 06-15 Phase-0 (C1 dup-of-guide G6, C2/C5 durable NULL-Canada-vol, C3 gov-wall+guide-covered, C4 real-demand-but-pricing→Step-E on file), competitor-gap Gap-1 permit cluster HARD-STOPPED at 4 cities pending GSC demand (Hamilton held, "do not re-spend DfS"), GSC self-feed structurally empty (blind domain). (5) P-EXPAND/P-REFRESH GSC-dead (0 rows). P1 Nathan/Suroy-gated; P2 no claim-free structural gap + no measured demand; P3/P4 saturated; P5 competitor-gaps fresh.

Honest NO-SHIP. Binding constraint = tarzan GSC verification (MORNING_TASKS L33) + Nathan C4 pricing/launch decisions + off-page authority (GBP/operator, L20) the cron cannot build — ALL already on file → no new MORNING_TASKS (no-dup). No lastmod-bump / speculative-seed (forbidden per bump-lastupdated/faq-churn/no-ship-subset). Read-only, $0 DfS, doc-only. Anchor: tarzan dry T19:08 07-14; next content lever = GSC advance past 07-11 (~03:00 07-15) or Nathan decision.

---

### 2026-07-16 T11:08 cron P0 + P3-doc: health audit FULLY CLEAN; stale TODO sections corrected against reality (SHIPPED, doc-only)

**Site pick:** STEP-2 ACTIVE `[auto]`/24h first-hand `git log` = **tarzan 0** (fewest) < sudbury 1 < fortmac 2 (3 FROZEN excluded). Last tarzan touch T19:08 07-14 (~40h), HR9 4h cooldown clear.

**P0 health audit (genuinely due, ~48h since T11:08 07-14): FULLY CLEAN, no fix needed.**
- **(a) URL sweep:** all **14/14** sitemap URLs HTTP 200 via Chrome UA, spaced 1.2s (per `curl-000`). Sizes 12.4KB to 39.6KB, zero THIN/500 (no CF unbuilt-home fallback, no deploy-state break). Note: tarzan has no `scripts/indexnow_submit.py`, so the URL list was taken first-hand from the live `sitemap.xml` rather than `ALL_PAGES`.
- **Dash sweep:** the `·` separator in every `<title>` was verified by codepoint as **U+00B7 middle dot**, NOT a banned dash (Git Bash console renders it as a replacement char, which is a false alarm worth not re-chasing). Homepage em-dash U+2014 count 0, en-dash U+2013 count 0.
- **(c) Markers:** theme-color `#2D4A36` matches brand; `/favicon.svg` 200; `/robots.txt` 200; `/sitemap.xml` 200; H1 "Honest tree work, owner-led, across south Ontario." carries niche + region; banned-claim scan (24/7, "24 hours", "always available", "round the clock") = **0 hits**. Phone `tel:+16472161874` consistent live vs `site.json`. `/quote/` form checks N/A (v1 is phone/text funnel by design).
- **(d) GSC pipeline:** `[SEO] Daily GSC KPI` (`uyBDgQqkyq0WNqOz`) **active=True**, last 2 executions **success** (72369 @ 07-16T01:00Z, 71227 @ 07-15T01:00Z). Appliance freshness MAX `metric_date` = **07-13** for both sudbury + fortmac = normal 3d GSC lag, inside the 4-day rule, NOT stale. **tarzan `seo_query_daily_kpi` = 0 rows (GSC-blind, root blocker unchanged).**
- **(e) Cannibalization:** N/A on this site (GSC-blind, no query data to scan).

**Cascade walked first-hand, all dry:** P-EXPAND + P-REFRESH GSC-dead (0 rows). P-PIPELINE human queue **0 undrafted `[x] approve`** (grepped: C1/C2/C4/C5 `[ ] approve`; C3 oak + the 4-city permit cluster all `[x] drafted`). P-PIPELINE-AUTO no eligible candidate (seeded pool exhausted per the 06-15 Phase-0 record: C1 dup-of-guide G6, C2/C5 durable NULL-Canada-volume, C3 gov-wall + guide-covered, C4 real-demand-but-pricing routed to Step E; Gap-1 permit cluster HARD-STOPPED at 4 cities pending GSC demand, "do not re-spend DfS" on Hamilton; self-feed structurally empty on a blind domain). P1 Nathan/Suroy-gated. P2 no measured demand + no claim-free structural gap.

**P3 on-page audit run fresh (not parroted), site is genuinely saturated:** all 14 live pages measured for title length, meta-desc length, H1 count, and heading-skip. **Titles 23 to 60 chars (0 over 60). Meta-descs 136 to 153 chars (0 over 160, 0 under 70, 0 missing). Exactly one H1 per page. Zero H2-to-H4 skips.** Nothing shippable in P3 on-page.

**SHIPPED (the deliverable, doc-only, no site source touched):** corrected two substantially stale HANDOFF sections against first-hand system state, per the CLAUDE.md drift rule (trust the system, update the doc). This mattered because the doc was actively misleading:
- "TODOs **before launch**" was still framed pre-launch although the site went **live 2026-06-14**. Retitled to post-launch framing.
- **Item 1 (phone) named `(647) 788-8733`**, TOA's number, which was never published on this site. Actual published number is **(647) 216-1874**, wired 06-13 and verified live today. Struck through, residual privacy-buffer ask preserved.
- **Items 8/9 (owner photo, job photos)** described placeholders (`[Owner photo coming]`, "6 placeholder cards") that the 06-14 buildout replaced with licensed stock. Struck through, honest "swap for real shots later" ask preserved.
- **Infra items 1 to 5** (GitHub repo, CF zone/Pages, DNS, Migadu email, `site.json` email) were all verified **DONE** first-hand (git remote, live apex, `site.json`, `TODO_EMAIL` grep = no hits). Struck through.
- **Infra item 6 (GSC) confirmed STILL OPEN and promoted to a named binding blocker, with the evidence chain, and corrected mid-run to name BOTH independent blockers rather than just verification.** First pass wrote "the domain is simply not verified" off a `src/`+`public/` grep alone. That was an overreach on two counts, so it was checked properly before the commit landed: (1) GSC verification is commonly done by **DNS TXT**, which that grep cannot see, so the zone was queried directly via the CF API. Result: **no `google-site-verification` TXT exists** (only `_dmarc`, Migadu `hosted-email-verify`, SPF), which together with the absent HTML file + meta tag makes "unverified" well-supported rather than assumed. (2) MORNING_TASKS L33 names a **second, independent** blocker the first pass glossed: `gsc_discovery_sweep.py`'s `SITE_TO_GSC_URL` dict lists six domains and **omits `tarzantrimming.ca`**, verified first-hand. So verification alone would NOT produce data. Item 6 now documents both, because fixing one and expecting rows would waste a cycle. Item 3 (pricing) cross-linked to blog Candidate 4, the only candidate with real measured demand.

No build/deploy/CF-purge/IndexNow needed (zero source or `dist` change). No synthetic lead test (per `no-synthetic`). **$0 DfS.** No new MORNING_TASKS: GSC verification (L33), Nathan pricing/launch decisions, and off-page authority are all already on file, so no duplicate. Verified 0 em/en dashes in the edited region.

**Anchor:** tarzan P0 CLEAN + TODO-drift correction T11:08 07-16; next P0 due ~T23:08 07-16. Next content lever is unchanged and NOT time-based: **GSC verification of this domain**, or a Nathan decision on pricing (C4).

### 2026-07-16 T21:08 cron P1: GSC pipeline pre-wired for tarzan (the cron-doable half of the site's #1 blocker)

**SHIPPED (tooling, not site source — no commit here: the four scripts live in
the workspace `.tmp/`, which is not a git repo; tarzan's own tree is untouched
and clean at `fa98dc5`).**

**Site pick:** ACTIVE `[auto]`/24h first-hand git = tarzan **3** < sudbury **5**
= fortmac **5** (3 FROZEN excluded). HR9 clear (last tarzan touch T14:08, ~7h).
P0 not due (last T11:08 today, ~10h < 12h).

**Cascade:** P-PIPELINE dry (human `[x] approve` queue empty; C1 `[x] skip` as of
T14:08, C2/C5 demand-dead, C3 drafted 06-30 in cooldown, C4 parked on Nathan's
price call). P-EXPAND / P-REFRESH / AUTO self-feed all structurally dead: the
domain is GSC-blind. So instead of logging a sixth NO-SHIP against that blocker,
this run **attacked the blocker itself**.

**Audits run first (all clean, which is why the run moved on):** rebuilt 15 pages
green; dist audit = 0 findings across meta-desc length, title length, H1 count,
heading-skip, `img` alt + width/height, em/en dashes, and the banned 24/7 family;
link-graph audit = 0 orphans (bar `/404.html`, expected), 0 broken internal hrefs,
both money pages at 11 in-content inbound links, sitemap 14/14 with per-page
`lastmod`. Sibling `schema-domain` defect: tarzan already confirmed clean (0
findings) by the fortmac T17:08 sweep, so no port was owed.

**Deliverable:** added `tarzantrimming.ca` → `sc-domain:tarzantrimming.ca` to the
`SITE_TO_GSC_URL` dict in `.tmp/gsc_discovery_sweep.py`, `.tmp/gsc_daily_kpi.py`,
`.tmp/gsc_rank_tracker.py` and `.tmp/portfolio_ranker.py` (6 → 7 domains each,
4/4 `py_compile` OK), each with an inline comment stating the entry is expected
to fail until verification so a future reader does not misread the skip as a
broken pipeline.

**Verified first-hand, not assumed:** (1) every script isolates per-site failures
with `try/except → continue`, so an unverified domain cannot poison the appliance
sites' pulls; (2) live read-only probe of the real GSC API reproduced the exact
loop — `tarzantrimming.ca` → *handled* `HttpError 403` (property does not exist),
`sudburyappliancerepair.ca` → **262 rows** in the same pass, loop completed. No
Supabase writes, no DfS spend ($0.00), no n8n mutation.

**Blocker (a) re-confirmed first-hand, not trusted from prose:** CF zone
`8dccf00d34ed0f68f554bc24a66a54ad` holds only `_dmarc`, Migadu
`hosted-email-verify=mp1zhzjn` and SPF. No `google-site-verification` TXT, so the
domain is genuinely unverified and the 403 is the correct, expected response.

**Net:** Suroy's task drops from two steps to one (paste the GSC TXT value; I add
the DNS record). The n8n daily-KPI workflow `uyBDgQqkyq0WNqOz` still omits tarzan
and stays hand-gated per HARD RULE 2. No build/deploy/wrangler/CF-purge/IndexNow:
zero site source changed, so all would have been no-ops.

### 2026-07-16 T23:08 cron P0: scheduled health audit FULLY CLEAN, cascade dry, NO-SHIP (doc-only)

**Site pick:** ACTIVE `[auto]`/24h first-hand git log = **tarzan 4** < fortmac 5 <
sudbury 6 (3 FROZEN excluded); tarzan strict min, no tie. HR9 clear (last tarzan
task was P1 at T21:08, ~2h, different phase). **P0 genuinely DUE** (last P0
T11:08 today = exactly 12h) and IS the deliverable.

**P0 CLEAN first-hand (a/c/d; b skip no-src-drift; e N/A GSC-blind):**
- (a) **14/14 live sitemap URLs HTTP 200** via spaced (1.2s) Python urllib +
  Chrome UA. Home 39,579B; service pages 25.6-25.8KB; `/services/` hub 17,816B;
  `/service-areas/` 24,315B; guide 27,545B; `/blog/` 18,654B; 5 blog posts
  31.3-33.1KB; smallest `/contact/` 12,391B (all >> the 5KB broken-state floor).
  **14 distinct correct-brand titles (0 dup, so no CF home-fallback)**. BAD=0,
  SMALL=0. 404-control `/zzz-no-such-page-xyz/` returns a real **404** (10,651B
  custom page), not a soft-200.
- **PATH GOTCHA (recorded so no future run misreads it):** tarzan's sitemap is
  **`/sitemap.xml`** (from `src/pages/sitemap.xml.ts`), NOT the appliance sites'
  `/sitemap-index.xml`. This run's first probe hit `/sitemap-index.xml` and got a
  correct **404**. That 404 is EXPECTED and is **not** a P0 finding. `robots.txt`
  line 4 already points at the right file. Do not "fix" it.
- (c) Home `theme-color #2D4A36` (forest brand), `/favicon.svg` present,
  `tel:+16472161874` = canonical Nathan cell in `site.json` (not a new flag),
  H1 "Honest tree work, owner-led, across south Ontario." carries niche + geo,
  CTA visible. Live home em-dash U+2014 = **0**, en-dash U+2013 = **0**, banned
  family (`24/7`, `24 hours`, `always available`, `around the clock`) = **0**.
  No web form by design, so `qf-email` / `markFieldError` are structurally N/A.
- (b) Screenshot skipped: HEAD `605628b` = origin/main, working tree clean, **0
  src drift** (last `src/`+`public/` commit is `db13d93`, 06-30). Same markers
  passed at T11:08 today; a screenshot would add nothing.
- (d) **GSC pipeline healthy first-hand:** `[SEO] Daily GSC KPI`
  (uyBDgQqkyq0WNqOz) **active=True**; last 3 executions all **success** (latest
  2026-07-17T01:00Z). Portfolio `seo_query_daily_kpi` MAX `2026-07-14` (2d < 4d
  → FRESH). `rank_tracking_results` gsc MAX `2026-07-13T10:00Z` = weekly Monday
  feed, PASS per the weekly rule (not a staleness flag).
  **tarzantrimming.ca returns 0 KPI rows = GSC-blind, expected**, root blocker
  unchanged (MORNING_TASKS L33, needs Suroy to paste the GSC TXT value).
- (e) Cannibalization scan N/A: GSC-blind, so there is no query x page data to
  scan.

**Cascade DRY (re-confirmed, no new input since T21:08 ~2h ago):**
- P-EXPAND / P-REFRESH / P-PIPELINE-AUTO **GSC self-feed**: structurally dead
  while the domain is GSC-blind (0 rows to select from).
- P-PIPELINE human `[x] approve` queue: **empty** (C1 `[x] skip`, C2/C5
  demand-dead per the 07-16 Phase 0 data, C3 drafted 06-30 in 60d cooldown, C4
  parked on Nathan's pricing call).
- P-PIPELINE-AUTO **competitor-gap self-feed**: the "On-deck next Gap-1 permit
  post" section is **RESOLVED, not actionable** — Burlington was the pre-validated
  winner and was already drafted + auto-published 06-25 (`5586cd3`); it is live
  and 200 in this run's sweep. The 4-city permit cluster (Toronto, Mississauga,
  Oakville, Burlington) is complete, and the file explicitly gates the 5th city on
  real GSC impressions rather than blind DfS spend on a low-volume municipality
  (Hamilton held at 10/mo). So the same GSC blocker gates this path too.
- P1 Open Issues: 0 cron-doable (the cron-doable half of the GSC blocker was
  already shipped at T21:08; the remaining half is Suroy's TXT paste).
- P2 no structural gap; P3/P4 saturated (T21:08 dist audit = 0 findings across
  meta-desc, title, H1, heading-skip, img alt/dims, dashes, banned family;
  link-graph = 0 orphans / 0 broken hrefs; sitemap 14/14 with per-page lastmod)
  and **0 src drift since**, so re-auditing would return the same zeros.

**Net:** read-only NO-SHIP. $0.00 DfS, no Supabase writes, no n8n mutation, no
source/build/deploy/wrangler/CF-purge/IndexNow, no synthetic lead, no new
MORNING_TASKS (every blocker already on file). Repo untouched and clean.

**Anchors:** tarzan P0 CLEAN T23:08 07-16; next tarzan P0 due ~T11:08 07-17;
next content lever = GSC verification TXT from Suroy.

### 2026-07-20 T16:08 cron P4: Call/Text CTA label-action parity (commit `b9950fc`, wrangler `c4a0b5c2`, LIVE)

**Pick:** ACTIVE `[auto]`/24h first-hand git = tarzan **2** (min) < sudbury 4 <
fortmac 6, so the rule pointed here with no deviation needed. P0 not due (last
T12:08, ~4h < 12h). P-PIPELINE human queue verified empty first-hand (C1 + C3
both `[x] drafted`; C2/C4/C5 still `[ ] approve`, C4 durably blocked on Nathan's
pricing call). P-EXPAND / P-REFRESH / P-PIPELINE-AUTO self-feed all structurally
dead while the site is GSC-blind (blocker 6a, Suroy's DNS TXT). P1 remaining half
is that same TXT paste. P2 no structural gap. P3 barred by HR9 (tarzan P3 T12:08).
Cascade landed honestly on **P4**.

**Defect found (label vs actual behaviour, same class as `[form-required-parity]`
but on a link CTA):** the sitewide `PhoneFloat` — the single highest-frequency
conversion control on the site, present on all 15 pages at every scroll position —
was labelled **"Call or text"** while its only `href` was `tel:`. The text CTA it
advertised did not exist. Worse, that label was `hidden sm:inline`, so on mobile
it collapsed to an **unlabelled icon-only circle** on exactly the viewport where
it matters most. Second, smaller finding: the Hero offered **call only**, while
every other contact surface on the site (`FinalCTA`, `Footer`, `/contact/`)
offers call **and** text. For an owner-led business whose lower-friction path is
a homeowner texting a photo of the tree, that is a real lead leak.

**Fix:**
- `PhoneFloat` split into **two separate, always-labelled actions** — a cream
  "Text" pill (`sms:`) and a forest "Call" pill (`tel:`) — each with an
  `aria-label` matching what it actually does. No hidden labels at any breakpoint.
- `Hero` gains a **"Text a photo"** outline CTA (`sms:`) matching `FinalCTA`'s
  existing treatment; "See what we do" demoted from an outline pill to an
  underlined text link so hierarchy stays Call > Text > browse.
- `Footer` inner padding `py-14` → `pt-14 pb-28 sm:pb-14` so the now-wider float
  never sits on the copyright row on small screens (per `[callbar-overlap]`).
- Homepage `sitemap.xml.ts` lastmod bumped `2026-07-18` → `2026-07-20` per
  `[bump-lastupdated]`. Chrome-only changes (footer, float) did not bump other
  leaves.

**Verify:** build clean **1st attempt**, 15 pages, 910ms. Scripted dist audit:
**15/15** pages carry the dual-action float with label/action parity asserted
per-anchor (sms block contains only "Text", tel block only "Call"); **0**
occurrences of the stale "Call or text" string; em U+2014 = **0**, en U+2013 =
**0**; banned 24/7-family regex = **0**. Mobile screenshot confirms both float
pills labelled and the hero three-CTA hierarchy reading correctly.

**Deploy:** push 1st attempt (`ee3c965..b9950fc`); wrangler direct-upload
(project not GitHub-linked) → `c4a0b5c2`, 17 files; apex content-match on the
net-new "Text a photo" marker **LIVE at first poll** per `[poll-marker]`;
**12-URL deploy-state sweep = 11× HTTP 200 [13,090-40,974B], BAD(5xx)=0,
SMALL=0**, dual-float verified live on all 11. `/quote/` 404 is **expected and
correct** (this site has no quote form by design, documented in "Things this site
does NOT have"); it served the real 404 page, not a CF home fallback. CF zone
purged `success:true`. No IndexNow (no new pages).

**Sibling-fix scan** per `[sibling-fix]`: grepped all six other site repos for
the same mislabelled CTA — **0 hits**. The defect was introduced in the
vicjimassage → tarzan rebrand when `WhatsAppFloat` became `PhoneFloat`, so it is
tarzan-specific. No cross-site flag needed.

**Net:** DfS $0.00, no Supabase writes, no n8n mutation, no synthetic lead, no
new MORNING_TASKS (every remaining blocker already on file).

**Anchors:** tarzan P4 4h → T20:08; next tarzan P0 due ~T00:08 07-21; next
content lever remains the GSC verification TXT from Suroy.

---

### 2026-07-20 T18:08 cron P3: LocalBusiness + ContactPage JSON-LD on /contact/ (commit `93b68b7`, wrangler `8dddbf1c`, LIVE)

**Pick:** ACTIVE `[auto]`/24h first-hand git = tarzan **4** (min) < sudbury 6 = fortmac 6 (3 FROZEN excluded). Last tarzan touch T16:08 (P4), so HR9 blocks another P4 this run.

**Cascade walked first-hand:**
- **P0** not due (T12:08, ~6h < 12h). Next ~T00:08 07-21.
- **P-PIPELINE** human `[x] approve` queue empty (all ticked entries already drafted). **P-PIPELINE-AUTO** dry: seeded pool durably resolved on 2026-06-15 Phase 0 (C1 skip/duplicates the live `/guide/`, C2 NULL Canadian volume, C3 published 06-30, C4 Step-E Suroy-gated on pricing, C5 NULL volume); GSC self-feed unavailable.
- **P-EXPAND / P-REFRESH** GSC-blind, re-verified live this run: `seo_query_daily_kpi` returns `[]` for `tarzantrimming.ca` (portfolio max = 07-17, normal 3-day lag, so the pull itself is healthy — tarzan is simply not GSC-verified yet).
- **P1** remaining Open Issues are all Nathan/Suroy-gated (GSC TXT, pricing, cities, photos).
- **P2** no structural gap: both offered services have pages, full removal is `comingSoon`.
- **P3 SHIPPED** (below). P4 blocked by HR9.

**Defect found (real, not busywork):** a 15-page dist schema audit (meta length, H1 count, img alt/dimensions, JSON-LD types) came back clean on every axis except one — `/contact/`, the page that carries the entire NAP in visible copy (phone, text, email, service area, hours label), emitted **only** a `BreadcrumbList`. No business entity at all. The site's most entity-dense page was structurally anonymous to Google.

**Fix:** added two JSON-LD nodes to `src/pages/contact.astro`:
- `LocalBusiness` reusing the homepage `@id` (`https://tarzantrimming.ca/#business`) so both pages reconcile to ONE entity instead of Google inferring two businesses. Carries `telephone`, `email` (valid Organization property the homepage node omits but this page displays), `areaServed` (7 cities), `address` (region + country, no street — mobile service business), `image`, `priceRange`.
- `ContactPage` with `mainEntity: {'@id': businessId}` pointing at it.

**Deliberately NOT added:** `openingHoursSpecification`. Real availability is "by appointment, evenings and weekends" around Nathan's Davey day job, which does not map honestly onto a fixed weekly schedule. Inventing one would assert hours he has not confirmed and would trip the `jsonld-hours` 24/7-sweep precedent.

**Verify:** build clean 15 pages, 1st attempt. All 3 `/contact/` ld+json blocks parse; `@id` string-matches the homepage node. Dist sweep em(U+2014)=0 / en(U+2013)=0 / 0 banned availability phrases. Content-match poll found `"@type":"ContactPage"` live on first probe. **11-URL deploy-state sweep all 200, all ≥15KB, 0 problems.** CF zone cache purged (`success:true`). No IndexNow (no new URLs).

**Net:** DfS $0.00, no Supabase writes, no n8n mutation, no synthetic lead, no new MORNING_TASKS (every remaining blocker already on file).

**Anchors:** tarzan P3 4h → T22:08; next tarzan P0 due ~T00:08 07-21; next content lever remains the GSC verification TXT from Suroy.
